---
phase: 07-iteration-prompt-engineering
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/unit/oracle-phase-transitions.test.js
  - tests/bash/test-oracle-phase.sh
autonomous: true
requirements: [LOOP-02, LOOP-03, INTL-02, INTL-03]

must_haves:
  truths:
    - "Phase transition logic is tested: survey->investigate, investigate->synthesize, synthesize->verify transitions fire at correct thresholds"
    - "Phase transition logic stays in current phase when thresholds are not met"
    - "Iteration counter increment produces correct state.json after simulated updates"
    - "build_oracle_prompt produces different output for each of the 4 phases"
    - "Edge cases are tested: zero questions, all answered, boundary confidence values"
  artifacts:
    - path: "tests/unit/oracle-phase-transitions.test.js"
      provides: "Ava unit tests for determine_phase and build_oracle_prompt"
      min_lines: 80
    - path: "tests/bash/test-oracle-phase.sh"
      provides: "Bash integration tests for iteration counter and phase transitions"
      min_lines: 60
  key_links:
    - from: "tests/unit/oracle-phase-transitions.test.js"
      to: ".aether/oracle/oracle.sh"
      via: "Invokes determine_phase and build_oracle_prompt via bash -c with test fixtures"
      pattern: "oracle.sh"
    - from: "tests/bash/test-oracle-phase.sh"
      to: ".aether/oracle/oracle.sh"
      via: "Sources or calls oracle.sh functions with temp state/plan files"
      pattern: "oracle.sh"
---

<objective>
Write comprehensive tests for the Phase 7 oracle additions: phase transition logic (determine_phase), prompt construction (build_oracle_prompt), and iteration counter management.

Purpose: Ensure the phase transition thresholds fire correctly (survey->investigate at 25% avg or all touched, investigate->synthesize at 60% avg, synthesize->verify at 80% avg), the iteration counter increments reliably, and the prompt construction injects the correct phase directive. These tests provide regression safety for Phase 8 (convergence tuning).

Output: Ava unit test file (tests/unit/oracle-phase-transitions.test.js) and bash integration test file (tests/bash/test-oracle-phase.sh).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-iteration-prompt-engineering/07-01-SUMMARY.md
@.aether/oracle/oracle.sh
@tests/unit/oracle-state.test.js
@tests/bash/test-oracle-state.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ava unit tests for phase transitions and prompt construction</name>
  <files>tests/unit/oracle-phase-transitions.test.js</files>
  <action>
Create tests/unit/oracle-phase-transitions.test.js following the pattern established in tests/unit/oracle-state.test.js (same helpers: createTmpDir, exec via bash -c, JSON fixtures).

The test approach: since determine_phase and build_oracle_prompt are bash functions inside oracle.sh, test them by extracting and calling them via `bash -c 'source ...; functionname args'`. If oracle.sh's `set -e` and main-loop execution make sourcing difficult, use an alternative approach: write the jq transition logic directly in test fixtures (the jq queries are the real logic under test) and test oracle.sh's functions by calling them through a wrapper that sources the script safely.

**Recommended pragmatic approach:** Test the jq logic directly. The determine_phase function is essentially jq queries against plan.json and state.json. Write tests that:
1. Create temp state.json and plan.json files with specific conditions
2. Run the same jq queries that determine_phase uses
3. Assert the expected phase

**Tests to write (minimum 8):**

determine_phase tests:
1. **survey stays survey when no questions touched**: All questions have empty iterations_touched, avg confidence 0 -> remains "survey"
2. **survey -> investigate when all questions touched**: All questions have non-empty iterations_touched -> transitions to "investigate"
3. **survey -> investigate when avg confidence >= 25**: Avg confidence 30%, not all touched -> transitions to "investigate"
4. **investigate stays investigate when avg confidence < 60 and 2+ below 50**: Avg confidence 45%, 3 questions below 50% -> remains "investigate"
5. **investigate -> synthesize when avg confidence >= 60**: Avg confidence 65% -> transitions to "synthesize"
6. **investigate -> synthesize when fewer than 2 below 50**: Only 1 question below 50%, avg confidence 55% -> transitions to "synthesize"
7. **synthesize -> verify when avg confidence >= 80**: Avg confidence 82% -> transitions to "verify"
8. **synthesize stays synthesize when avg confidence < 80**: Avg confidence 70% -> remains "synthesize"

build_oracle_prompt tests:
9. **survey prompt contains SURVEY directive**: Phase is "survey" -> output contains "SURVEY"
10. **investigate prompt contains INVESTIGATE directive**: Phase is "investigate" -> output contains "INVESTIGATE"
11. **prompt includes oracle.md content**: Output contains "Oracle Ant" (from oracle.md base prompt)

Use `t.teardown(() => fs.rmSync(dir, { recursive: true, force: true }))` for cleanup, matching existing test patterns.

For testing oracle.sh functions, use this pattern:
```javascript
const result = execSync(
  `bash -c 'set +e; ORACLE_SH="${ORACLE_SH}"; source <(sed -n "/^determine_phase()/,/^}/p" "$ORACLE_SH"); determine_phase "${stateFile}" "${planFile}"'`,
  { encoding: 'utf8', cwd: PROJECT_ROOT }
).trim();
```

If sourcing is problematic, extract the jq logic directly into the test commands -- the transition logic IS the jq queries.
  </action>
  <verify>
Run: `npx ava tests/unit/oracle-phase-transitions.test.js` (all tests pass)
Run: `grep -c "^test(" tests/unit/oracle-phase-transitions.test.js` (returns 8+)
  </verify>
  <done>
Ava test file exists with 8+ tests covering all phase transition thresholds (survey->investigate, investigate->synthesize, synthesize->verify) and prompt construction. All tests pass. Tests follow existing oracle-state.test.js patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write bash integration tests for iteration counter and phase transitions</name>
  <files>tests/bash/test-oracle-phase.sh</files>
  <action>
Create tests/bash/test-oracle-phase.sh following the pattern in tests/bash/test-oracle-state.sh (same test helpers: run_test, setup_tmpdir, cleanup_tmpdir, TESTS_RUN/PASSED/FAILED counters).

**Tests to write (minimum 5 test functions with sub-assertions):**

1. **test_iteration_increment**: Create state.json with iteration 0. Run the jq increment command (same as oracle.sh uses). Verify iteration is now 1. Run it again. Verify iteration is 2. Verify last_updated is a valid ISO-8601 timestamp.

2. **test_phase_transition_survey_to_investigate**: Create state.json with phase "survey" and plan.json where all questions have non-empty iterations_touched. Run the determine_phase jq logic. Verify output is "investigate". Then create state.json where avg confidence >= 25 (but not all touched). Verify output is still "investigate".

3. **test_phase_transition_investigate_to_synthesize**: Create state.json with phase "investigate" and plan.json where avg confidence is 65%. Run determine_phase logic. Verify output is "synthesize".

4. **test_phase_stays_when_threshold_not_met**: Create state.json with phase "survey" and plan.json where no questions are touched and avg confidence is 10%. Verify remains "survey". Create investigate scenario with avg confidence 40% and 3 questions below 50%. Verify remains "investigate".

5. **test_phase_transition_updates_state_file**: Create state.json with phase "survey". Run the transition logic (determine + update). Read state.json. Verify the phase field has been updated to the new phase in the actual file. This tests the full jq read -> compare -> write cycle.

Since oracle.sh functions may be hard to source directly (set -e, variable dependencies), test the jq logic directly in bash -- the jq commands ARE the logic. Use the same jq queries that oracle.sh's determine_phase uses.

Make the script executable (chmod +x). Include the same header structure as test-oracle-state.sh (counters, colors, helpers). Exit 0 on all pass, exit 1 on any failure.
  </action>
  <verify>
Run: `bash tests/bash/test-oracle-phase.sh` (all tests pass, exit code 0)
Run: `grep -c "^test_" tests/bash/test-oracle-phase.sh` (returns 5)
Run: `bash tests/bash/test-oracle-state.sh` (still passes -- no regression)
  </verify>
  <done>
Bash test file exists with 5 test functions covering iteration increment, phase transitions at each threshold, staying in phase when thresholds not met, and full file update cycle. All tests pass. Existing oracle-state bash tests still pass.
  </done>
</task>

</tasks>

<verification>
1. `npx ava tests/unit/oracle-phase-transitions.test.js` -- all 8+ ava tests pass
2. `bash tests/bash/test-oracle-phase.sh` -- all 5 bash test functions pass
3. `npx ava tests/unit/oracle-state.test.js` -- existing Phase 6 tests still pass (no regression)
4. `bash tests/bash/test-oracle-state.sh` -- existing Phase 6 bash tests still pass (no regression)
5. `npm test` -- full test suite passes
</verification>

<success_criteria>
- tests/unit/oracle-phase-transitions.test.js has 8+ ava tests for phase transition thresholds and prompt construction
- tests/bash/test-oracle-phase.sh has 5 bash test functions for iteration counter and phase transitions
- All new tests pass
- All existing Phase 6 tests pass (no regressions)
- Full test suite (npm test) passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-iteration-prompt-engineering/07-02-SUMMARY.md`
</output>
