---
phase: 01-instinct-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/integration/instinct-pipeline.test.js
autonomous: true
requirements:
  - LEARN-02
  - LEARN-03

must_haves:
  truths:
    - "instinct-create with confidence >= 0.7 creates instinct entries in COLONY_STATE.json"
    - "instinct-create with matching trigger+action boosts confidence instead of creating duplicate"
    - "instinct-read returns clean JSON with filtered instincts sorted by confidence"
    - "instinct-read returns empty array (not double JSON) when no instincts exist"
    - "pheromone-prime includes domain-grouped instincts in prompt_section"
    - "colony-prime prompt_section contains instincts when they exist and omits them when empty"
  artifacts:
    - path: "tests/integration/instinct-pipeline.test.js"
      provides: "End-to-end integration tests for instinct pipeline"
      min_lines: 100
  key_links:
    - from: "tests/integration/instinct-pipeline.test.js"
      to: ".aether/aether-utils.sh instinct-create"
      via: "execSync calling aether-utils.sh"
      pattern: "instinct-create"
    - from: "tests/integration/instinct-pipeline.test.js"
      to: ".aether/aether-utils.sh colony-prime"
      via: "execSync calling aether-utils.sh"
      pattern: "colony-prime"
---

<objective>
Add integration tests verifying the complete instinct pipeline: create instincts during continue -> read them back via colony-prime -> verify domain-grouped output in prompt_section.

Purpose: Proves LEARN-02 and LEARN-03 work end-to-end. Tests the full chain from instinct-create through pheromone-prime to colony-prime's prompt_section output. Catches regressions in the pipeline.
Output: New test file tests/integration/instinct-pipeline.test.js with 6-8 tests covering the pipeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-instinct-pipeline/01-01-SUMMARY.md
@.planning/phases/01-instinct-pipeline/01-02-SUMMARY.md
@tests/integration/learning-pipeline.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create instinct pipeline integration tests</name>
  <files>tests/integration/instinct-pipeline.test.js</files>
  <action>
Create `tests/integration/instinct-pipeline.test.js` following the exact same test patterns as `tests/integration/learning-pipeline.test.js`:
- Use `ava` test runner with `test.serial` for all tests
- Use the same `createTempDir`, `cleanupTempDir`, `runAetherUtil`, and `setupTestColony` helper functions (copy from learning-pipeline.test.js)
- Each test creates a temp directory, sets up a test colony, runs commands, asserts results, cleans up

Write these tests:

**Test 1: "instinct-create creates a new instinct in COLONY_STATE.json"**
- Setup: Create test colony + write COLONY_STATE.json with `{"memory":{"instincts":[]}}` (plus required fields: goal, state, current_phase, plan, session_id)
- Run: `instinct-create --trigger "when tests timeout" --action "increase timeout to 30s" --confidence 0.7 --domain "testing" --source "phase-1" --evidence "3 timeout failures"`
- Assert: result.ok is true, result.result.action is "created", result.result.confidence is 0.7
- Read COLONY_STATE.json and verify memory.instincts has 1 entry with correct trigger, action, domain, confidence

**Test 2: "instinct-create boosts confidence for duplicate trigger+action"**
- Setup: Test colony with one existing instinct in COLONY_STATE.json (confidence 0.7)
- Run: instinct-create with same trigger+action
- Assert: result.result.action is "updated", new confidence is 0.8 (0.7 + 0.1 boost)
- Read COLONY_STATE.json and verify still only 1 instinct (not duplicated)

**Test 3: "instinct-read returns empty array with single JSON line when no instincts"**
- Setup: Test colony with empty memory.instincts array
- Run: instinct-read
- Assert: stdout contains exactly 1 line (not 2 -- validates the fallthrough bug fix)
- Parse the single line and verify result.result.instincts is empty array, result.result.total is 0

**Test 4: "instinct-read filters by min-confidence and domain"**
- Setup: Test colony with 3 instincts at confidence 0.5, 0.7, 0.9 in domains "testing" and "architecture"
- Run: instinct-read --min-confidence 0.7
- Assert: only 2 instincts returned (0.7 and 0.9)
- Run: instinct-read --domain "testing"
- Assert: only instincts in testing domain returned

**Test 5: "pheromone-prime groups instincts by domain"**
- Setup: Test colony with pheromones.json + COLONY_STATE.json containing instincts in "testing" and "architecture" domains
- Run: pheromone-prime
- Assert: result.result.prompt_section contains "Testing:" and "Architecture:" as separate groups
- Assert: result.result.instinct_count matches number of instincts

**Test 6: "colony-prime includes instincts in prompt_section"**
- Setup: Test colony with QUEEN.md, pheromones.json, and COLONY_STATE.json with instincts
- Run: colony-prime --compact
- Assert: result.result.prompt_section contains "INSTINCTS (Learned Behaviors)"
- Assert: result.result.prompt_section contains the instinct trigger and action text

**Test 7: "colony-prime omits instincts section when none exist"**
- Setup: Test colony with QUEEN.md, pheromones.json, COLONY_STATE.json with empty instincts
- Run: colony-prime --compact
- Assert: result.result.prompt_section does NOT contain "INSTINCTS"

**Test 8: "complete pipeline: create -> read -> prime"**
- Setup: Clean test colony
- Run: instinct-create for two instincts in different domains
- Run: colony-prime --compact
- Assert: prompt_section contains both instincts grouped by domain
- This is the end-to-end smoke test proving LEARN-02 flows into LEARN-03

For the setupTestColony helper, extend it to also create a minimal COLONY_STATE.json:
```json
{
  "session_id": "colony_test",
  "goal": "test",
  "state": "BUILDING",
  "current_phase": 1,
  "plan": {"phases": []},
  "memory": {
    "instincts": [],
    "phase_learnings": [],
    "decisions": []
  },
  "errors": {"flagged_patterns": []},
  "events": []
}
```

And create pheromones.json:
```json
{"signals": [], "version": "1.0.0"}
```

Run all tests with: `npx ava tests/integration/instinct-pipeline.test.js --serial --timeout=30s`
  </action>
  <verify>
Run: `npx ava tests/integration/instinct-pipeline.test.js --serial --timeout=30s`
All 8 tests should pass.
Run: `npx ava tests/integration/instinct-pipeline.test.js --serial --timeout=30s 2>&1 | grep -c "passed"` should show 8 passed.
  </verify>
  <done>8 integration tests pass covering: instinct creation, dedup/confidence boost, instinct-read empty case (no double JSON), confidence/domain filtering, domain-grouped pheromone-prime output, colony-prime instinct inclusion, colony-prime instinct omission, and the full create-to-prime pipeline.</done>
</task>

</tasks>

<verification>
1. `npx ava tests/integration/instinct-pipeline.test.js --serial --timeout=30s` -- all tests pass
2. `npm test` -- all existing tests still pass (no regressions)
3. Test 3 specifically validates the instinct-read fallthrough bug fix (single JSON line)
4. Test 5 specifically validates domain grouping (LEARN-03 formatting requirement)
5. Test 8 validates the end-to-end pipeline (LEARN-02 -> LEARN-03)
</verification>

<success_criteria>
- All 8 integration tests pass
- No regressions in existing test suite (npm test)
- Tests cover both LEARN-02 (instinct creation with >= 0.7 threshold) and LEARN-03 (instinct reading via colony-prime)
- Tests validate the instinct-read fallthrough bug fix
- Tests validate domain-grouped output in pheromone-prime
</success_criteria>

<output>
After completion, create `.planning/phases/01-instinct-pipeline/01-03-SUMMARY.md`
</output>
