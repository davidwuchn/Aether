---
phase: 02-command-audit-data-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md"
  - ".claude/commands/ant/*.md (any commands requiring fixes)"
autonomous: true
requirements: [INST-02, INST-03]

must_haves:
  truths:
    - "Every slash command has been read and its references verified"
    - "An audit document exists cataloging each command's status (pass/fail/warning)"
    - "Commands with broken references or stale content have been fixed"
  artifacts:
    - path: ".planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md"
      provides: "Complete audit results for all 37 slash commands"
      contains: "pass|fail|warning"
  key_links:
    - from: ".claude/commands/ant/*.md"
      to: ".aether/aether-utils.sh"
      via: "subcommand references"
      pattern: "aether-utils\\.sh [a-z-]+"
    - from: ".claude/commands/ant/*.md"
      to: ".aether/docs/command-playbooks/*.md"
      via: "playbook file references"
      pattern: "command-playbooks/"
---

<objective>
Audit all 37 slash commands for correctness and fix any broken ones.

Purpose: Establish confidence that every command a user might invoke has valid references, doesn't mention eliminated features, and has correct structure. Fix anything found broken.
Output: An audit document (02-01-AUDIT.md) cataloging every command's status, plus fixes to any broken commands.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-purge/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 37 slash commands</name>
  <files>.planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md</files>
  <action>
Read every .md file in `.claude/commands/ant/` (37 files total). For each command, verify:

1. **Structure**: Has valid YAML frontmatter with `name` and `description` fields
2. **Subcommand references**: Every `aether-utils.sh <subcommand>` call references a subcommand that actually exists (grep the case statement in aether-utils.sh to confirm)
3. **File references**: Every file path referenced in the command (playbooks, templates, data files, agent definitions) exists on disk
4. **No eliminated features**: No references to `runtime/`, no references to files/directories that were removed in architecture v4.0
5. **Stale content**: Flag commands that reference superseded concepts (e.g., migrate-state references v1->v2.0 migration but state is already v3.0; verify-castes references LiteLLM proxy which is setup-specific)
6. **Agent references**: If the command spawns agents (Task tool), verify the agent name exists in `.claude/agents/ant/`

Categorize each command as:
- **pass**: All references valid, structure correct, no stale content
- **warning**: Works but has stale/misleading content (e.g., outdated model names, setup-specific assumptions)
- **fail**: Has broken references (file doesn't exist, subcommand doesn't exist) or structural issues

Write results to `.planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md` as a markdown table with columns: Command, Status, Issues Found (or "None").

Note: These are Claude Code slash commands (AI prompt files), NOT executable scripts. "Auditing" means reading and checking references, not running them.
  </action>
  <verify>
The audit file exists at `.planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md` and contains an entry for all 37 commands. Run `grep -c "^|" .planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md` — should show 38+ lines (header + 37 entries). Every command in `ls .claude/commands/ant/` appears in the audit.
  </verify>
  <done>All 37 slash commands have been read, their references checked, and their status documented as pass/warning/fail in the audit file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix commands that failed or have warnings</name>
  <files>.claude/commands/ant/*.md (only files with issues from Task 1)</files>
  <action>
For each command marked "fail" in the audit, fix the broken reference or structural issue. For each command marked "warning", apply judgment:

**Fix if:**
- References a file/subcommand that doesn't exist (replace with correct reference or remove the reference)
- Has structural issues (missing frontmatter fields, malformed YAML)
- References an eliminated feature (remove the reference)

**Document but do NOT fix if:**
- Command is intentionally stale (e.g., migrate-state is a one-time migration tool — it's fine that it references v1/v2.0 since that's its purpose)
- Warning is informational only (e.g., verify-castes mentions LiteLLM — that's documentation of a specific setup, not a bug)

After applying fixes, update the audit document to reflect the new status of each fixed command (change "fail" to "pass" and note what was fixed).

If zero commands need fixing (all passed), update the audit document with a summary line: "All commands passed audit — no fixes required."
  </action>
  <verify>
After fixes, re-read each previously-failing command file and confirm the broken reference is resolved. Run `grep -c "fail" .planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md` — should return 0 (no remaining failures). All existing tests still pass: `npm test`.
  </verify>
  <done>Zero commands remain in "fail" status. Any broken references have been fixed. The audit document reflects final status of all 37 commands.</done>
</task>

</tasks>

<verification>
- `.planning/phases/02-command-audit-data-tooling/02-01-AUDIT.md` exists and documents all 37 commands
- No command has "fail" status in the final audit
- `npm test` passes (no tests broken by command fixes)
- Every subcommand reference in `.claude/commands/ant/*.md` resolves to a real subcommand in `aether-utils.sh`
</verification>

<success_criteria>
All 37 slash commands have been audited, every broken reference has been fixed, and an audit document exists recording the findings.
</success_criteria>

<output>
After completion, create `.planning/phases/02-command-audit-data-tooling/02-01-SUMMARY.md`
</output>
