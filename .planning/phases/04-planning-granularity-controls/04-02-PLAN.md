---
phase: 04-planning-granularity-controls
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - .claude/commands/ant/plan.md
  - .aether/commands/claude/plan.md
  - .aether/agents/aether-route-setter.md
  - .aether/agents-claude/aether-route-setter.md
  - .claude/agents/ant/aether-route-setter.md
  - .opencode/agents/aether-route-setter.md
autonomous: true
requirements: [PLAN-02, PLAN-03]

must_haves:
  truths:
    - "Route-setter agent receives dynamic min/max phase bounds instead of hardcoded 3-6"
    - "Plan command reads persisted granularity from COLONY_STATE.json before planning"
    - "Plan command asks user for granularity if none is persisted and no --granularity flag provided"
    - "Plan command injects dynamic 'Maximum N phases' into route-setter prompt"
    - "Out-of-range plans trigger a warning with accept/adjust/replan options"
    - "All 4 agent definition files (agents, agents-claude, .claude/agents, .opencode/agents) have matching changes"
  artifacts:
    - path: ".aether/agents/aether-route-setter.md"
      provides: "Dynamic phase bounds replacing hardcoded 3-6"
      contains: "{granularity_min}"
    - path: ".aether/agents-claude/aether-route-setter.md"
      provides: "Mirror of agents/ route-setter"
      contains: "{granularity_min}"
    - path: ".claude/agents/ant/aether-route-setter.md"
      provides: "Claude Code agent mirror"
      contains: "{granularity_min}"
    - path: ".opencode/agents/aether-route-setter.md"
      provides: "OpenCode agent mirror"
      contains: "{granularity_min}"
    - path: ".claude/commands/ant/plan.md"
      provides: "Plan command with --granularity flag and dynamic bounds"
      contains: "granularity"
  key_links:
    - from: ".claude/commands/ant/plan.md"
      to: "COLONY_STATE.json"
      via: "reads plan_granularity field"
      pattern: "plan_granularity|granularity"
    - from: ".claude/commands/ant/plan.md"
      to: ".aether/agents/aether-route-setter.md"
      via: "injects min/max bounds into route-setter prompt"
      pattern: "granularity_min|granularity_max"
---

<objective>
Wire the PlanGranularity enum into the plan command and route-setter agent definitions. Replace all hardcoded "3-6 phases" references with dynamic bounds from the selected granularity. Add the --granularity flag, the "always ask" behavior, out-of-range validation with warn+choose, and update all 4 agent definition files.

Purpose: This is the LLM prompt engineering layer. Without this, the route-setter ignores the user's granularity choice and always generates 3-6 phases regardless of selection.
Output: Dynamic phase bounds in plan command and route-setter agents; out-of-range warn+choose UX.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-planning-granularity-controls/04-CONTEXT.md
@.planning/phases/04-planning-granularity-controls/04-RESEARCH.md
@.planning/phases/04-planning-granularity-controls/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (executor must use these): -->

From pkg/colony/colony.go:
```go
type PlanGranularity string
const (
    GranularitySprint    PlanGranularity = "sprint"
    GranularityMilestone PlanGranularity = "milestone"
    GranularityQuarter   PlanGranularity = "quarter"
    GranularityMajor     PlanGranularity = "major"
)
```

From pkg/colony/granularity.go:
```go
func GranularityRange(g PlanGranularity) (min int, max int)
// Returns: sprint=1,3  milestone=4,7  quarter=8,12  major=13,20  default=1,3
```

From cmd/colony_cmds.go:
```go
// CLI: aether plan-granularity get -> {"granularity": "...", "source": "...", "min": N, "max": N}
// CLI: aether plan-granularity set --granularity <value>
```

Current hardcoded references to replace (verified via grep):

.aether/agents/aether-route-setter.md:
  Line 21: "Create phase structure -- 3-6 phases with observable outcomes"
  Line 106: "Verify phase count is reasonable: 3-6 phases for most goals"
  Line 113: 'phase_count_justification: "{if outside 3-6 range}"'

.aether/agents-claude/aether-route-setter.md:
  Line 25: "Decompose into 3-6 phases with observable outcomes"
  Line 36: "3-6 phases for most goals"
  Line 55: "3-6 phases for most goals. If the plan has fewer than 3..."
  Line 113: "3-6 for most goals"
  Line 120: 'phase_count_justification: "{if outside 3-6 range}"'

.claude/agents/ant/aether-route-setter.md:
  Line 25: "Decompose into 3-6 phases"
  Line 36: "3-6 phases for most goals"
  Line 55: "3-6 phases for most goals. If the plan has fewer than 3..."
  Line 113: "3-6 for most goals"
  Line 120: 'phase_count_justification: "{if outside 3-6 range}"'

.opencode/agents/aether-route-setter.md:
  Line 21: "Create phase structure -- 3-6 phases"
  Line 106: "3-6 phases for most goals"
  Line 113: 'phase_count_justification: "{if outside 3-6 range}"'

.aether/commands/claude/plan.md:
  Line 477: "1. If no plan exists, create 3-6 phases with concrete tasks"
  Line 485: "Maximum 6 phases. Maximum 4 tasks per phase."

.claude/commands/ant/plan.md:
  Line 477: "1. If no plan exists, create 3-6 phases with concrete tasks"
  Line 485: "Maximum 6 phases. Maximum 4 tasks per phase."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire granularity into plan command and route-setter agents</name>
  <files>.claude/commands/ant/plan.md, .aether/commands/claude/plan.md, .aether/agents/aether-route-setter.md, .aether/agents-claude/aether-route-setter.md, .claude/agents/ant/aether-route-setter.md, .opencode/agents/aether-route-setter.md</files>
  <read_first>
    - .claude/commands/ant/plan.md (full file -- understand Step 2 depth selection, Step 3-5 flow, route-setter prompt at lines 470-488)
    - .aether/commands/claude/plan.md (same structure, verify parity with .claude version)
    - .aether/agents/aether-route-setter.md (full file -- understand structure and all "3-6" references)
    - .aether/agents-claude/aether-route-setter.md (full file -- mirror of agents/ version)
    - .claude/agents/ant/aether-route-setter.md (full file -- Claude Code version)
    - .opencode/agents/aether-route-setter.md (full file -- OpenCode version)
  </read_first>
  <action>
Per D-03: /ant:plan always asks for granularity if none is set.
Per D-04: Subsequent calls use persisted value unless overridden.
Per D-07: --granularity bounds injected into route-setter prompt.
Per D-08: Dynamic "Maximum N phases" in output constraints.

This task modifies markdown command and agent definition files. These are LLM prompts, not Go code. Changes are textual replacements.

### Part A: Plan command changes (.claude/commands/ant/plan.md and .aether/commands/claude/plan.md)

Both files must receive identical changes. Apply to both.

**1. Add granularity parsing in Step 1 (Read State), after extracting goal/milestone/plan.phases:**

After the existing `Parse $ARGUMENTS` block (around line 11-13), add:
```
- If contains `--granularity sprint|milestone|quarter|major`: Set `granularity_override = <value>`
- Otherwise: `granularity_override = ""`
```

**2. Add granularity resolution in Step 2 (Check Existing Plan), before the planning depth selection:**

Insert a new sub-step between the `Parse $ARGUMENTS` block and the `Select planning depth` block. This must come BEFORE the planning depth selection (currently around line 72):

```markdown
Select planning granularity:
- If `plan.phases` has entries (plan already exists): skip granularity selection entirely
- If `granularity_override` is set: use that value
- Otherwise, read persisted granularity:
  Run: `aether plan-granularity get`
  - If `source == "state"` (persisted value exists): use that value
  - If `source == "default"` (no persisted value): ask user:
    `Planning granularity? 1) Sprint (1-3 phases) 2) Milestone (4-7 phases) 3) Quarter (8-12 phases) 4) Major (13-20 phases)`
    Map user choice: 1->sprint, 2->milestone, 3->quarter, 4->major. Default to `milestone` on unclear input.
- Persist selected granularity:
  Run: `aether plan-granularity set --granularity {selected_value}`
- Extract bounds:
  Run: `aether plan-granularity get`
  Set `granularity_min = .min`, `granularity_max = .max`, `granularity_label = .granularity`
```

**3. Replace hardcoded "3-6" in the route-setter prompt (Step 4 instructions, around line 477):**

Change:
```
1. If no plan exists, create 3-6 phases with concrete tasks
```
To:
```
1. If no plan exists, create {granularity_min}-{granularity_max} phases with concrete tasks
```

**4. Replace hardcoded "Maximum 6 phases" in output constraints (around line 485):**

Change:
```
Maximum 6 phases. Maximum 4 tasks per phase.
```
To:
```
Maximum {granularity_max} phases. Maximum 4 tasks per phase.
```

### Part B: Route-setter agent definition changes (4 files)

Apply identical structural changes to all 4 route-setter files. The specific wording varies between the two formats (.aether/agents/ vs .aether/agents-claude/) so adjust text to match each file's style.

**For .aether/agents/aether-route-setter.md:**

Line 21: Change `2. Create phase structure -- 3-6 phases with observable outcomes` to:
`2. Create phase structure -- {granularity_min}-{granularity_max} phases with observable outcomes (bounds provided by plan command)`

Line 106: Change `3. Verify phase count is reasonable: 3-6 phases for most goals; if outside this range, add justification.` to:
`3. Verify phase count is reasonable: aim for {granularity_min}-{granularity_max} phases; if outside this range, add justification.`

Line 113: Change `phase_count_justification: "{if outside 3-6 range}"` to:
`phase_count_justification: "{if outside {granularity_min}-{granularity_max} range}"`

**For .aether/agents-claude/aether-route-setter.md and .claude/agents/ant/aether-route-setter.md (same content):**

Line 25: Change `Decompose into 3-6 phases with observable outcomes` to:
`Decompose into {granularity_min}-{granularity_max} phases with observable outcomes (bounds provided by plan command)`

Line 36: Change `3-6 phases for most goals. If outside this range, justify in the plan.` to:
`{granularity_min}-{granularity_max} phases (bounds provided by plan command). If outside this range, justify in the plan.`

Line 55: Change `3-6 phases for most goals. If the plan has fewer than 3, the goal may be too small to need decomposition. If more than 6, the goal may need to be split into sub-goals. Justify if outside range.` to:
`{granularity_min}-{granularity_max} phases (bounds from plan command). If the plan has fewer than {granularity_min}, the goal may be too small to need decomposition. If more than {granularity_max}, the goal may need to be split into sub-goals. Justify if outside range.`

Line 113: Change `3-6 for most goals. If outside range, add justification to plan.` to:
`{granularity_min}-{granularity_max} (bounds from plan command). If outside range, add justification to plan.`

Line 120: Change `phase_count_justification: "{if outside 3-6 range}"` to:
`phase_count_justification: "{if outside {granularity_min}-{granularity_max} range}"`

**For .opencode/agents/aether-route-setter.md:**

Line 21: Change `Create phase structure -- 3-6 phases with observable outcomes` to:
`Create phase structure -- {granularity_min}-{granularity_max} phases with observable outcomes (bounds provided by plan command)`

Line 106: Change `3-6 phases for most goals; if outside this range, add justification.` to:
`{granularity_min}-{granularity_max} phases (bounds provided by plan command); if outside this range, add justification.`

Line 113: Change `phase_count_justification: "{if outside 3-6 range}"` to:
`phase_count_justification: "{if outside {granularity_min}-{granularity_max} range}"`

### Verification

After all changes, run these grep checks:
```bash
# Verify NO remaining hardcoded "3-6" in route-setter files
grep -rn "3-6" .aether/agents/aether-route-setter.md .aether/agents-claude/aether-route-setter.md .claude/agents/ant/aether-route-setter.md .opencode/agents/aether-route-setter.md || echo "PASS: no hardcoded 3-6"

# Verify NO remaining hardcoded "Maximum 6" in plan command
grep -rn "Maximum 6" .claude/commands/ant/plan.md .aether/commands/claude/plan.md || echo "PASS: no hardcoded Maximum 6"

# Verify dynamic placeholders are present
grep -c "granularity_min\|granularity_max" .aether/agents/aether-route-setter.md .aether/agents-claude/aether-route-setter.md .claude/agents/ant/aether-route-setter.md .opencode/agents/aether-route-setter.md
```

Also verify agents-claude mirror parity:
```bash
# These two files should have matching changes (same placeholder count)
diff <(grep -c "granularity" .aether/agents/aether-route-setter.md) <(grep -c "granularity" .aether/agents-claude/aether-route-setter.md) || echo "MISMATCH: agents and agents-claude must match"
```
  </action>
  <acceptance_criteria>
    - `.aether/agents/aether-route-setter.md` contains `{granularity_min}` and `{granularity_max}`
    - `.aether/agents-claude/aether-route-setter.md` contains `{granularity_min}` and `{granularity_max}`
    - `.claude/agents/ant/aether-route-setter.md` contains `{granularity_min}` and `{granularity_max}`
    - `.opencode/agents/aether-route-setter.md` contains `{granularity_min}` and `{granularity_max}`
    - `grep -rn "3-6" .aether/agents/aether-route-setter.md` returns 0 matches
    - `grep -rn "3-6" .aether/agents-claude/aether-route-setter.md` returns 0 matches
    - `grep -rn "3-6" .claude/agents/ant/aether-route-setter.md` returns 0 matches
    - `grep -rn "3-6" .opencode/agents/aether-route-setter.md` returns 0 matches
    - `grep -rn "Maximum 6" .claude/commands/ant/plan.md` returns 0 matches
    - `grep -rn "Maximum 6" .aether/commands/claude/plan.md` returns 0 matches
    - `.claude/commands/ant/plan.md` contains `--granularity`
    - `.aether/commands/claude/plan.md` contains `--granularity`
    - `.claude/commands/ant/plan.md` contains `granularity_min`
    - `.claude/commands/ant/plan.md` contains `granularity_max`
    - `.claude/commands/ant/plan.md` contains `aether plan-granularity set`
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "3-6" .aether/agents/aether-route-setter.md .aether/agents-claude/aether-route-setter.md .claude/agents/ant/aether-route-setter.md .opencode/agents/aether-route-setter.md; grep -rn "Maximum 6" .claude/commands/ant/plan.md .aether/commands/claude/plan.md; echo "---"; grep -c "granularity_min" .aether/agents/aether-route-setter.md .aether/agents-claude/aether-route-setter.md .claude/agents/ant/aether-route-setter.md .opencode/agents/aether-route-setter.md; grep -c "granularity" .claude/commands/ant/plan.md .aether/commands/claude/plan.md</automated>
  </verify>
  <done>All 4 route-setter agent definitions use {granularity_min}/{granularity_max} instead of hardcoded 3-6. Both plan command files have --granularity flag, "always ask" behavior, and dynamic prompt injection. Zero remaining "3-6" or "Maximum 6" hardcoded references in route-setter or plan files.</done>
</task>

<task type="auto">
  <name>Task 2: Add out-of-range plan validation to plan command</name>
  <files>.claude/commands/ant/plan.md, .aether/commands/claude/plan.md</files>
  <read_first>
    - .claude/commands/ant/plan.md (full file -- find Step 4 loop exit and Step 5 finalize to insert validation between them)
    - .aether/commands/claude/plan.md (same structure)
  </read_first>
  <action>
Per D-05: Out-of-range plans show a warning with accept/adjust/replan options. No silent auto-trimming or hard rejection.
Per PLAN-03: User warned and asked to approve or adjust.

This task adds the out-of-range validation to the plan command markdown files. The validation goes between the route-setter loop (Step 4) and the plan finalization (Step 5 or later step that writes to COLONY_STATE.json).

**Insert the following section in both .claude/commands/ant/plan.md and .aether/commands/claude/plan.md:**

Find the point where the plan loop exits with an accepted plan (after the route-setter produces the final JSON) and before the plan is written to COLONY_STATE.json. Insert this validation block:

```markdown
### Step 4.5: Granularity Range Validation

After the route-setter produces the accepted plan, validate phase count against selected granularity:

1. Count phases in the accepted plan: `actual_phases = count of plan.phases`
2. Compare against granularity bounds:
   - If `actual_phases >= granularity_min AND actual_phases <= granularity_max`: proceed to finalization (no action needed)
   - If outside range: display warning and present options:

```
WARNING: Plan has {actual_phases} phases, but {granularity_label} granularity expects {granularity_min}-{granularity_max} phases.

Options:
1) Accept as-is -- the plan may be better than the range suggests
2) Adjust granularity -- change granularity to fit the plan
3) Replan -- regenerate with current {granularity_label} granularity

Choose (1/2/3):
```

3. Handle user choice:
   - **Option 1 (Accept):** Continue to finalization. No changes to plan or granularity.
   - **Option 2 (Adjust):** Determine best-fitting granularity:
     - If actual_phases <= 3: set to sprint
     - If actual_phases <= 7: set to milestone
     - If actual_phases <= 12: set to quarter
     - Otherwise: set to major
     - Run: `aether plan-granularity set --granularity {best_fit}`
     - Continue to finalization.
   - **Option 3 (Replan):** Return to Step 4 (route-setter loop) with current granularity. Display: `Replanning with {granularity_label} granularity ({granularity_min}-{granularity_max} phases)...`
```

Find the correct insertion point by locating where the plan confidence loop ends and the plan gets finalized. The exact step numbers may differ between the two files -- insert logically between "plan accepted" and "plan saved to state".
  </action>
  <acceptance_criteria>
    - `.claude/commands/ant/plan.md` contains `Step 4.5: Granularity Range Validation` or equivalent granularity validation section
    - `.claude/commands/ant/plan.md` contains `actual_phases`
    - `.claude/commands/ant/plan.md` contains `granularity_min` and `granularity_max`
    - `.claude/commands/ant/plan.md` contains all 3 options: Accept, Adjust, Replan
    - `.claude/commands/ant/plan.md` contains `aether plan-granularity set --granularity` in the adjust option
    - `.aether/commands/claude/plan.md` contains identical granularity range validation section
    - Both files have the validation placed between plan acceptance and plan finalization
  </acceptance_criteria>
  <verify>
    <automated>grep -c "granularity_min" .claude/commands/ant/plan.md .aether/commands/claude/plan.md; grep -c "Accept as-is" .claude/commands/ant/plan.md .aether/commands/claude/plan.md; grep -c "Adjust granularity" .claude/commands/ant/plan.md .aether/commands/claude/plan.md; grep -c "Replan" .claude/commands/ant/plan.md .aether/commands/claude/plan.md</automated>
  </verify>
  <done>Plan command files include out-of-range validation between plan acceptance and finalization. Users see actual vs. expected count, and can accept, adjust granularity, or replan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> plan command | --granularity flag value validated via aether plan-granularity set |
| LLM output -> plan validation | Route-setter output validated against granularity range before acceptance |
| Plan command -> route-setter prompt | Dynamic bounds injected from validated granularity (not user raw input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | S | plan --granularity flag | accept | Flag value routed through Go enum validation before use |
| T-04-07 | I | route-setter prompt injection | accept | Dynamic bounds are integer constants from GranularityRange, not freeform user input |
| T-04-08 | D | out-of-range validation | accept | Warn+choose preserves user agency; no silent modification |
| T-04-09 | E | route-setter agent definitions | mitigate | All 4 files updated consistently; grep verification confirms zero stale hardcoded values |
</threat_model>

<verification>
1. `grep -rn "3-6" .aether/agents/aether-route-setter.md .aether/agents-claude/aether-route-setter.md .claude/agents/ant/aether-route-setter.md .opencode/agents/aether-route-setter.md` returns empty
2. `grep -rn "Maximum 6" .claude/commands/ant/plan.md .aether/commands/claude/plan.md` returns empty
3. All 4 agent files contain `{granularity_min}` and `{granularity_max}`
4. Both plan command files contain granularity selection, dynamic bounds, and out-of-range validation
5. `go test ./... -race -count=1` passes (no Go code changes, but verify no regressions)
</verification>

<success_criteria>
- All "3-6" and "Maximum 6" hardcoded values replaced with dynamic placeholders
- Plan command has --granularity flag, "always ask" behavior, and dynamic prompt injection
- Out-of-range validation with 3 options (accept/adjust/replan) added to plan command
- All 4 route-setter agent definition files updated consistently
- Zero remaining hardcoded phase count constraints in any file
</success_criteria>

<output>
After completion, create `.planning/phases/04-planning-granularity-controls/04-02-SUMMARY.md`
</output>
