---
phase: 04-planning-granularity-controls
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - .aether/commands/claude/run.md
  - .opencode/commands/ant/run.md
autonomous: true
requirements: [PLAN-05]

must_haves:
  truths:
    - "Autopilot reads persisted plan_granularity from COLONY_STATE.json at startup"
    - "Autopilot warns if the plan has more phases than the selected granularity range allows"
    - "Warning does not block execution -- plan was already accepted by user during /ant:plan"
    - "Both Claude and OpenCode autopilot commands have identical granularity checks (parity)"
  artifacts:
    - path: ".aether/commands/claude/run.md"
      provides: "Autopilot granularity check in Step 0 (Claude)"
      contains: "plan_granularity"
    - path: ".opencode/commands/ant/run.md"
      provides: "Autopilot granularity check in Step 0 (OpenCode)"
      contains: "plan_granularity"
  key_links:
    - from: ".aether/commands/claude/run.md"
      to: "COLONY_STATE.json"
      via: "reads plan_granularity field in Step 0"
      pattern: "plan_granularity|granularity"
    - from: ".opencode/commands/ant/run.md"
      to: "COLONY_STATE.json"
      via: "reads plan_granularity field in Step 0"
      pattern: "plan_granularity|granularity"
---

<objective>
Add granularity awareness to the autopilot loop. The autopilot reads the persisted granularity from COLONY_STATE.json and warns if the plan exceeds the range -- but continues execution since the plan was already accepted during /ant:plan.

Both the Claude (.aether/commands/claude/) and OpenCode (.opencode/commands/ant/) run.md files must be updated to maintain parity per CLAUDE.md agent parity requirements.

Purpose: Without this, the autopilot ignores the user's granularity preference and runs all phases regardless of scope, breaking the sprint/quarter/major workflow.
Output: Autopilot checks and warns on granularity mismatch in both Claude and OpenCode command files.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-planning-granularity-controls/04-CONTEXT.md
@.planning/phases/04-planning-granularity-controls/04-RESEARCH.md

<interfaces>
<!-- Autopilot reads COLONY_STATE.json, which now contains plan_granularity: -->

From pkg/colony/colony.go (ColonyState struct):
```go
PlanGranularity    PlanGranularity   `json:"plan_granularity,omitempty"`
```

From pkg/colony/granularity.go:
```go
func GranularityRange(g PlanGranularity) (min int, max int)
// Returns: sprint=1,3  milestone=4,7  quarter=8,12  major=13,20
```

From cmd/colony_cmds.go:
```go
// CLI: aether plan-granularity get -> {"granularity": "...", "source": "...", "min": N, "max": N}
```

Current autopilot Step 0 (.aether/commands/claude/run.md lines 65-75):
```markdown
### Step 0: Initialize

1. Read `.aether/data/COLONY_STATE.json`; validate goal + plan.phases exist
   - If `milestone` == `"Crowned Anthill"`: output "..." and stop
2. Determine remaining incomplete phases; apply `--max-phases` cap
3. Set `phases_completed = 0`, `autopilot_start = $(date +%s)`
4. Record pre-build blocker count: `bash .aether/aether-utils.sh flag-check-blockers {phase}`
5. If `--headless` flag is present:
   - Run: `bash .aether/aether-utils.sh autopilot-set-headless true`
6. Display: `AUTOPILOT ENGAGED | Goal: {goal} | Phase {N} | Max: {max or "all"}`
```

OpenCode Step 0 (.opencode/commands/ant/run.md lines 65-75):
```markdown
### Step 0: Initialize

1. Read `.aether/data/COLONY_STATE.json`; validate goal + plan.phases exist
   - If `milestone` == `"Crowned Anthill"`: output "..." and stop
2. Determine remaining incomplete phases; apply `--max-phases` cap
3. Set `phases_completed = 0`, `autopilot_start = $(date +%s)`
4. Record pre-build blocker count: `aether flag-check-blockers {phase}`
5. If `--headless` flag is present:
   - Run: `aether autopilot-set-headless true`
6. Display: `AUTOPILOT ENGAGED | Goal: {goal} | Phase {N} | Max: {max or "all"}`
```

KEY DIFFERENCE between Claude and OpenCode run.md:
- Claude uses `bash .aether/aether-utils.sh` prefix for CLI calls
- OpenCode uses `aether` directly for CLI calls
- The granularity check text itself is identical in both files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add granularity check to autopilot Step 0 (both Claude and OpenCode)</name>
  <files>.aether/commands/claude/run.md, .opencode/commands/ant/run.md</files>
  <read_first>
    - .aether/commands/claude/run.md (full file -- understand Step 0 initialization)
    - .opencode/commands/ant/run.md (full file -- same structure, different CLI invocation pattern)
  </read_first>
  <action>
Per D-09: /ant:run reads persisted granularity from COLONY_STATE.json and respects the phase count. If the plan has more phases than the range allows, the autopilot warns but continues.
Per PLAN-05: Autopilot respects selected planning granularity across all phases.

This task modifies BOTH autopilot command markdown files. Add a granularity check in Step 0 after reading COLONY_STATE.json to both files.

**In `.aether/commands/claude/run.md`, modify Step 0:**

After item 1 (read COLONY_STATE.json, validate goal + plan.phases) and before item 2 (determine remaining phases), insert a new sub-step:

```markdown
1b. Read planning granularity and validate plan scope:
    Run: `aether plan-granularity get`
    - Extract: `granularity_label = .granularity`, `gran_min = .min`, `gran_max = .max`
    - If `granularity_label == "none"`: skip check (no granularity set)
    - If `granularity_label != "none"`:
      - Count total phases in `plan.phases`: `total_phases = length(plan.phases)`
      - If `total_phases > gran_max`:
        Display: `NOTE: Plan has {total_phases} phases but {granularity_label} granularity expects {gran_min}-{gran_max}. Plan was accepted during /ant:plan -- continuing.`
      - If `total_phases < gran_min`:
        Display: `NOTE: Plan has {total_phases} phases but {granularity_label} granularity expects {gran_min}-{gran_max}. Plan was accepted during /ant:plan -- continuing.`
```

Also update the Step 0 display line (item 6) to include granularity:

Change:
```
6. Display: `AUTOPILOT ENGAGED | Goal: {goal} | Phase {N} | Max: {max or "all"}`
```
To:
```
6. Display: `AUTOPILOT ENGAGED | Goal: {goal} | Phase {N} | Max: {max or "all"} | Granularity: {granularity_label or "none"}`
```

**In `.opencode/commands/ant/run.md`, apply IDENTICAL changes to Step 0:**

The granularity check sub-step text is exactly the same (it uses `aether plan-granularity get` directly, which is already the OpenCode convention -- no `bash .aether/aether-utils.sh` prefix needed). Insert the same 1b sub-step after item 1 and before item 2, with the same display line update to item 6.

Do NOT block execution on granularity mismatch in either file. The plan was already accepted by the user during /ant:plan (which has the out-of-range validation from Plan 02). The autopilot's role is informational only -- the NOTE messages are prominent warnings that display in the autopilot output, ensuring the user is aware of the mismatch without halting progress.
  </action>
  <acceptance_criteria>
    - `.aether/commands/claude/run.md` contains `plan-granularity get` or `plan_granularity`
    - `.aether/commands/claude/run.md` contains `gran_max` or `granularity`
    - `.aether/commands/claude/run.md` Step 0 includes granularity check after reading COLONY_STATE.json
    - `.aether/commands/claude/run.md` AUTOPILOT ENGAGED display includes `Granularity:`
    - `.aether/commands/claude/run.md` does NOT contain any blocking behavior for out-of-range plans (warn only)
    - `.opencode/commands/ant/run.md` contains identical `plan-granularity get` or `plan_granularity` references
    - `.opencode/commands/ant/run.md` contains identical `gran_max` or `granularity` references
    - `.opencode/commands/ant/run.md` Step 0 includes identical granularity check
    - `.opencode/commands/ant/run.md` AUTOPILOT ENGAGED display includes `Granularity:`
    - Both files have identical granularity check text (parity)
  </acceptance_criteria>
  <verify>
    <automated>echo "=== Claude run.md ===" && grep -c "plan-granularity\|plan_granularity\|granularity" .aether/commands/claude/run.md && grep -c "Granularity:" .aether/commands/claude/run.md && echo "=== OpenCode run.md ===" && grep -c "plan-granularity\|plan_granularity\|granularity" .opencode/commands/ant/run.md && grep -c "Granularity:" .opencode/commands/ant/run.md</automated>
  </verify>
  <done>Autopilot reads persisted plan_granularity in Step 0 in both Claude and OpenCode command files, warns on range mismatch (non-blocking), and displays granularity in the startup banner. Both files maintain parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| COLONY_STATE.json -> autopilot | Granularity read from state file; trust level is user-configured preference |
| Autopilot -> user | Warning is informational only; no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | E | autopilot granularity check | accept | Read-only check; no modification to plan or state based on granularity |
| T-04-11 | I | granularity display | accept | Displays what was persisted; no user input injection risk in read-only display |
</threat_model>

<verification>
1. `.aether/commands/claude/run.md` contains `plan-granularity get` reference
2. `.opencode/commands/ant/run.md` contains `plan-granularity get` reference
3. Both files' AUTOPILOT ENGAGED lines include Granularity field
4. Both files' granularity check text is identical (parity)
5. `go test ./... -race -count=1` passes (no Go code changes)
</verification>

<success_criteria>
- Autopilot reads plan_granularity from COLONY_STATE.json in Step 0 (both Claude and OpenCode)
- Warns on range mismatch without blocking execution
- Displays granularity label in startup banner
- Claude and OpenCode run.md files maintain parity
- No regressions in test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-planning-granularity-controls/04-03-SUMMARY.md`
</output>
