---
phase: 11-dead-code-deprecation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/aether-utils.sh
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "All 18 confirmed-dead subcommands emit a deprecation warning to stderr when invoked"
    - "Deprecated subcommands still execute normally after emitting the warning"
    - "The help JSON marks all 18 deprecated commands with [DEPRECATED] in their descriptions"
    - "A new Deprecated section in the help JSON lists all 18 deprecated commands"
    - "No dead-but-useful commands are extracted (all 18 are deprecate-only per research finding)"
  artifacts:
    - path: ".aether/aether-utils.sh"
      provides: "_deprecation_warning function and 18 deprecation calls"
      contains: "_deprecation_warning"
  key_links:
    - from: "_deprecation_warning function"
      to: "each deprecated subcommand case handler"
      via: "_deprecation_warning call as first line of handler"
      pattern: "_deprecation_warning"
---

<objective>
Add deprecation warnings to all 18 confirmed-dead subcommands in aether-utils.sh and update the help JSON to reflect their deprecated status.

Purpose: Mark dead code for future removal with a one-cycle confirmation period, without breaking any existing functionality.
Output: Modified aether-utils.sh with `_deprecation_warning` function, 18 deprecation calls, and updated help JSON.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dead-code-deprecation/11-RESEARCH.md
@.aether/aether-utils.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _deprecation_warning function and deprecation calls to all 18 dead subcommands</name>
  <files>.aether/aether-utils.sh</files>
  <action>
1. Add a `_deprecation_warning` helper function after the utility module sourcing block (after line 34, near the fallback error constants). The function:
   ```bash
   _deprecation_warning() {
     local cmd_name="$1"
     printf '[deprecated] %s -- will be removed in v3.0\n' "$cmd_name" >&2
   }
   ```
   This goes right after the `source` block (lines 26-34) and before the error constant fallbacks (line 36+). Place it with a comment: `# --- Deprecation warning helper ---`.

2. Add `_deprecation_warning "<subcommand-name>"` as the FIRST line inside each of these 18 case handlers (after the case label, before any existing code):

   | # | Subcommand | Approx Line |
   |---|-----------|-------------|
   | 1 | checkpoint-check | 6784 |
   | 2 | error-pattern-check | 1433 |
   | 3 | error-patterns-check | 1862 |
   | 4 | error-summary | 1442 |
   | 5 | learning-select-proposals | 6046 |
   | 6 | pheromone-export-eternal | 7133 |
   | 7 | semantic-context | 10349 |
   | 8 | session-clear-context | 10061 |
   | 9 | session-is-stale | 10029 |
   | 10 | session-summary | 10106 |
   | 11 | skill-index-read | 11675 |
   | 12 | skill-is-user-created | 11701 |
   | 13 | skill-manifest-read | 11690 |
   | 14 | suggest-clear | 10916 |
   | 15 | survey-clear | 6911 |
   | 16 | survey-verify-fresh | 6887 |
   | 17 | swarm-display-inline | 3893 |
   | 18 | swarm-display-render | 3877 |

   Pattern for each: Insert `_deprecation_warning "subcommand-name"` immediately after the `)` line of the case handler.

   IMPORTANT: Do NOT remove any code, do NOT redirect to replacement commands, do NOT change any behavior. The warning is additive only.
  </action>
  <verify>
Run each deprecated subcommand and verify:
1. It emits `[deprecated] <name> -- will be removed in v3.0` to stderr
2. It still produces its normal output/behavior on stdout
3. `set -e` does not kill the script (the warning must not cause a non-zero exit)

Quick verification:
```bash
# Test one subcommand emits warning to stderr
bash .aether/aether-utils.sh error-summary 2>&1 >/dev/null | grep -q '\[deprecated\]'
# Test it still runs (doesn't crash from errexit)
bash .aether/aether-utils.sh error-summary 2>/dev/null; echo "exit: $?"
```
  </verify>
  <done>All 18 dead subcommands emit a deprecation warning to stderr and continue to execute normally.</done>
</task>

<task type="auto">
  <name>Task 2: Update help JSON with deprecation markers and Deprecated section</name>
  <files>.aether/aether-utils.sh</files>
  <action>
In the `help)` case handler (starting at line 989), update the JSON output:

1. **Commands array** (line 992): No change needed -- the flat commands array lists command names only (no metadata). Leave it as-is for backward compatibility.

2. **Sections entries**: For each deprecated command that appears in a section, append ` [DEPRECATED]` to its description. The 5 commands currently in sections are:
   - `error-pattern-check` in a section (CHECK: may not be in sections -- only in commands array)
   - `error-summary` (CHECK: may not be in sections -- only in commands array)
   - `skill-index-read` in "Skills Engine" section (line 1117) -- add `[DEPRECATED]` to description
   - `skill-manifest-read` in "Skills Engine" section (line 1122) -- add `[DEPRECATED]` to description
   - `skill-is-user-created` in "Skills Engine" section (line 1125) -- add `[DEPRECATED]` to description

3. **Add a new "Deprecated" section** after the last section (after "Skills Engine", before the closing `}`). This section lists all 18 deprecated commands:
   ```json
   "Deprecated": [
     {"name": "checkpoint-check", "description": "Check dirty files against allowlist [DEPRECATED]"},
     {"name": "error-pattern-check", "description": "Check for error anti-patterns [DEPRECATED]"},
     {"name": "error-patterns-check", "description": "Scan for error handling anti-patterns [DEPRECATED]"},
     {"name": "error-summary", "description": "Summarize error handling patterns [DEPRECATED]"},
     {"name": "learning-select-proposals", "description": "Interactive learning proposal selector [DEPRECATED]"},
     {"name": "pheromone-export-eternal", "description": "Export pheromones to eternal memory format [DEPRECATED]"},
     {"name": "semantic-context", "description": "Semantic search context retrieval [DEPRECATED]"},
     {"name": "session-clear-context", "description": "Clear session context markers [DEPRECATED]"},
     {"name": "session-is-stale", "description": "Check session staleness [DEPRECATED]"},
     {"name": "session-summary", "description": "Generate session summary [DEPRECATED]"},
     {"name": "skill-index-read", "description": "Read cached skills index [DEPRECATED]"},
     {"name": "skill-is-user-created", "description": "Check if skill is user-created [DEPRECATED]"},
     {"name": "skill-manifest-read", "description": "Read skills manifest [DEPRECATED]"},
     {"name": "suggest-clear", "description": "Clear suggestion state [DEPRECATED]"},
     {"name": "survey-clear", "description": "Clear survey state [DEPRECATED]"},
     {"name": "survey-verify-fresh", "description": "Check survey freshness [DEPRECATED]"},
     {"name": "swarm-display-inline", "description": "Inline swarm display for Claude Code [DEPRECATED]"},
     {"name": "swarm-display-render", "description": "Terminal render wrapper for swarm display [DEPRECATED]"}
   ]
   ```

4. **Verify the JSON is valid** after edits by running `bash .aether/aether-utils.sh help | jq .` -- it must parse cleanly.

IMPORTANT: Read the existing descriptions from the current help JSON for commands already listed in sections. Use those exact descriptions with `[DEPRECATED]` appended. For commands not currently in any section, write a brief description based on their case handler comment.
  </action>
  <verify>
```bash
# Verify help JSON is valid
bash .aether/aether-utils.sh help | jq . > /dev/null
# Verify Deprecated section exists
bash .aether/aether-utils.sh help | jq '.result.sections.Deprecated | length'
# Should output: 18
# Verify skill-index-read is marked deprecated in Skills Engine section
bash .aether/aether-utils.sh help | jq '.result.sections["Skills Engine"][] | select(.name == "skill-index-read") | .description' | grep -q DEPRECATED
```
  </verify>
  <done>Help JSON parses cleanly, contains a Deprecated section with all 18 entries, and existing section entries for deprecated commands have [DEPRECATED] markers in their descriptions.</done>
</task>

</tasks>

<verification>
1. `bash .aether/aether-utils.sh help | jq .` parses without error
2. All 18 deprecated subcommands emit `[deprecated]` to stderr when invoked
3. Deprecated subcommands still return valid output (not killed by errexit)
4. `grep -c '_deprecation_warning' .aether/aether-utils.sh` returns 19 (1 function definition + 18 calls)
5. `bash .aether/aether-utils.sh help | jq '.result.sections.Deprecated | length'` returns 18
</verification>

<success_criteria>
- _deprecation_warning function exists in aether-utils.sh
- All 18 confirmed-dead subcommands call _deprecation_warning as their first action
- Help JSON has a Deprecated section with all 18 commands
- Skills Engine section entries for deprecated commands have [DEPRECATED] markers
- All existing tests still pass (no regressions from the function addition)
- QUAL-01 satisfied: audit complete (18 dead, rest documented as alive)
- QUAL-02 satisfied: deprecation warnings added to all confirmed-dead
- QUAL-03 satisfied: no dead-but-useful candidates identified for extraction (all 18 have alive alternatives)
</success_criteria>

<output>
After completion, create `.planning/phases/11-dead-code-deprecation/11-01-SUMMARY.md`
</output>
