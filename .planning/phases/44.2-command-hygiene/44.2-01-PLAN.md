---
phase: 44.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-medic.md
  - .aether/agents-claude/aether-medic.md
  - .aether/commands/*.yaml (50 files)
  - .claude/commands/ant/*.md (50 files)
  - .opencode/commands/ant/*.md (50 files)
  - .claude/agents/ant/*.md (10 files)
  - .opencode/agents/*.md (10 files)
  - .aether/agents-claude/*.md (10 files)
  - .aether/skills/**/*.md (7 files)
  - .aether/docs/**/*.md (30 files)
  - .aether/templates/* (4 files)
  - .aether/workers.md
  - CLAUDE.md
  - AGENTS.md
  - .claude/rules/aether-colony.md
  - .codex/CODEX.md
  - .opencode/OPENCODE.md
  - README.md
  - RUNTIME UPDATE ARCHITECTURE.md
  - CHANGELOG.md
  - cmd/codex_visuals.go
  - cmd/build_wrapper_ceremony_test.go
  - cmd/continue_wrapper_ceremony_test.go
  - cmd/medic_ceremony_test.go
  - cmd/platform_doc_hygiene_test.go
  - pkg/colony/session_test.go
autonomous: true
requirements:
  - PUB-03
  - REL-01

must_haves:
  truths:
    - "TestClaudeOpenCodeAgentContentParity passes -- aether-medic.md body matches between Claude and OpenCode"
    - "Zero /ant: references remain outside .planning/ directory"
    - "All 50 YAML source files have name: ant-X format (not ant:X)"
    - "All tests pass with go test ./... -race"
  artifacts:
    - path: ".opencode/agents/aether-medic.md"
      provides: "OpenCode medic agent with body matching Claude canonical"
      contains: "aether publish --channel dev"
    - path: ".aether/agents-claude/aether-medic.md"
      provides: "Packaging mirror byte-identical to .claude/agents/ant/aether-medic.md"
    - path: "cmd/codex_visuals.go"
      provides: "Go runtime help text with /ant-resume references"
      contains: "/ant-resume"
  key_links:
    - from: ".opencode/agents/aether-medic.md"
      to: ".claude/agents/ant/aether-medic.md"
      via: "body content parity"
      pattern: "aether publish --channel dev"
    - from: ".aether/agents-claude/aether-medic.md"
      to: ".claude/agents/ant/aether-medic.md"
      via: "byte-identical packaging mirror"
      pattern: "diff returns no output"
---

<objective>
Fix two independent hygiene issues in the Aether codebase:

1. **Medic agent parity** -- The OpenCode aether-medic.md agent body is out of sync with the Claude canonical version. The OpenCode version is missing workflow step 8 (`aether integrity` recommendation), uses an older recovery bullet (`aether install --package-dir` instead of `aether publish`), and is missing the `--deep` scan explanation bullet. Copy the Claude body to OpenCode, then sync the packaging mirror.

2. **Colon-to-hyphen rename** -- All Aether slash commands use colon format (`/ant:command`) but Claude Code convention requires hyphen format (`/ant-command`). This is a mechanical rename across ~341 files and ~1,894 individual references, excluding `.planning/` (historical records).

Purpose: Ensure test suite passes (PUB-03) and command naming complies with Claude Code conventions (REL-01).
Output: Updated agent file, updated command naming across entire repo, green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44.2-command-hygiene/44.2-RESEARCH.md
@.planning/phases/44.2-command-hygiene/44.2-VALIDATION.md

<interfaces>
<!-- Key differences between Claude and OpenCode medic bodies (verified by diff) -->

Claude body has these additions vs OpenCode:
1. Line after step 7: "8. **Recommend integrity check** -- For release pipeline validation..."
2. Expanded recovery bullet: "If hub publish integrity is broken, recommend `aether publish` as the primary recovery path..." (replaces simpler older version)
3. New bullet: "When running `aether medic --deep`, the scanner includes release integrity checks..."

Go production code references (cmd/codex_visuals.go):
- Line 339: `return "It's safe to clear your context now. Run \`/ant:resume\` to restore.\n"`
- Line 341: `return "It's safe to clear your context now. Run \`/ant:resume\` to restore.\n"`
Both must become `/ant-resume`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix aether-medic.md body parity between Claude and OpenCode</name>
  <files>
    .opencode/agents/aether-medic.md
    .aether/agents-claude/aether-medic.md
  </files>
  <read_first>
    .claude/agents/ant/aether-medic.md
    .opencode/agents/aether-medic.md
    .aether/agents-claude/aether-medic.md
  </read_first>
  <action>
The Claude canonical medic (`.claude/agents/ant/aether-medic.md`) is the source of truth. The OpenCode version (`.opencode/agents/aether-medic.md`) has stale body content. Fix it:

1. Read `.claude/agents/ant/aether-medic.md` and extract everything after the closing `---` of the frontmatter (the body content, starting with the blank line then `<role>`).

2. Read `.opencode/agents/aether-medic.md` and note its frontmatter (lines 1-13):
   ```
   ---
   description: "Use this agent when diagnosing and repairing colony health issues..."
   mode: subagent
   model: anthropic/claude-sonnet-4-20250514
   tools:
     write: true
     edit: true
     bash: true
     grep: true
     glob: true
     task: false
   color: "#ff0000"
   ---
   ```
   Preserve this frontmatter EXACTLY. Only replace the body (everything after line 13).

3. Write `.opencode/agents/aether-medic.md` with the OpenCode frontmatter (lines 1-13) followed by the Claude body content (from `<role>` through `</return_format>`).

4. Copy `.claude/agents/ant/aether-medic.md` to `.aether/agents-claude/aether-medic.md` to keep the packaging mirror byte-identical. Use `cp .claude/agents/ant/aether-medic.md .aether/agents-claude/aether-medic.md`.

5. Run `go test ./cmd/ -run TestClaudeOpenCodeAgentContentParity -v` and verify it passes.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/ -run TestClaudeOpenCodeAgentContentParity -v` passes with zero failures
    - `diff <(sed -n '15,$p' .claude/agents/ant/aether-medic.md) <(sed -n '14,$p' .opencode/agents/aether-medic.md)` produces NO output (bodies match after frontmatter offset)
    - `diff .claude/agents/ant/aether-medic.md .aether/agents-claude/aether-medic.md` produces NO output (byte-identical)
    - `grep -c 'aether publish --channel dev' .opencode/agents/aether-medic.md` returns 1 or more (new content present)
    - `grep -c 'Recommend integrity check' .opencode/agents/aether-medic.md` returns 1 (step 8 present)
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/ -run TestClaudeOpenCodeAgentContentParity -v</automated>
  </verify>
  <done>
    OpenCode medic body matches Claude canonical, packaging mirror is byte-identical, parity test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Colon-to-hyphen rename across entire repo</name>
  <files>
    .aether/commands/*.yaml (50 files)
    .claude/commands/ant/*.md (50 files)
    .opencode/commands/ant/*.md (50 files)
    .claude/agents/ant/*.md (10 files)
    .opencode/agents/*.md (10 files)
    .aether/agents-claude/*.md (10 files)
    .aether/skills/**/*.md (7 files)
    .aether/docs/**/*.md (30 files)
    .aether/templates/* (4 files)
    .aether/workers.md
    CLAUDE.md
    AGENTS.md
    .claude/rules/aether-colony.md
    .codex/CODEX.md
    .opencode/OPENCODE.md
    README.md
    RUNTIME UPDATE ARCHITECTURE.md
    CHANGELOG.md
    cmd/codex_visuals.go
    cmd/build_wrapper_ceremony_test.go
    cmd/continue_wrapper_ceremony_test.go
    cmd/medic_ceremony_test.go
    cmd/platform_doc_hygiene_test.go
    pkg/colony/session_test.go
  </files>
  <read_first>
    cmd/codex_visuals.go (lines 334-343)
    cmd/build_wrapper_ceremony_test.go (line 37)
    cmd/continue_wrapper_ceremony_test.go (lines 35, 38, 40, 71, 76, 83, 88, 95, 100)
    cmd/medic_ceremony_test.go (line 294)
    cmd/platform_doc_hygiene_test.go (lines 201, 463)
    pkg/colony/session_test.go (lines 13, 15)
    .aether/commands/build.yaml (verify name: field format)
    .aether/commands/init.yaml (verify name: field format)
  </read_first>
  <action>
Mechanical rename of all `/ant:` to `/ant-` and `name: ant:` to `name: ant-` across the entire repo, excluding `.planning/`. This MUST be done as a single atomic commit. Do NOT rename any files -- only change text content inside existing files.

Execute these replacement commands IN ORDER:

**Step 1: Replace `/ant:` with `/ant-` in all target files (excluding .planning/ and node_modules/)**

```bash
find . -type f \( -name '*.md' -o -name '*.go' -o -name '*.yaml' -o -name '*.json' -o -name '*.toml' \) \
  -not -path './.planning/*' -not -path './node_modules/*' -not -path './.git/*' \
  -exec sed -i '' 's|/ant:|/ant-|g' {} +
```

**Step 2: Replace `name: ant:` with `name: ant-` in YAML source frontmatter**

```bash
find .aether/commands -name '*.yaml' -exec sed -i '' 's|^name: ant:|name: ant-|' {} +
```

**Step 3: Replace `name: ant:` with `name: ant-` in command wrapper frontmatter**

```bash
find .claude/commands/ant .opencode/commands/ant -name '*.md' \
  -exec sed -i '' 's|^name: ant:|name: ant-|' {} +
```

**Step 4: Sync packaging mirrors to Claude canonical agents**

After the sed replacements, both `.claude/agents/ant/` and `.aether/agents-claude/` should already have matching content since both were processed by the same sed. Verify with diff, and if any mismatch exists, copy from canonical:

```bash
for f in .claude/agents/ant/*.md; do
  base=$(basename "$f")
  if ! diff -q "$f" ".aether/agents-claude/$base" > /dev/null 2>&1; then
    cp "$f" ".aether/agents-claude/$base"
  fi
done
```

**Step 5: Verify zero remaining colon references**

```bash
grep -rn '/ant:' --include='*.md' --include='*.go' --include='*.yaml' --include='*.json' --include='*.toml' | grep -v '.planning/' | grep -v 'node_modules/'
```

This MUST return zero results. If any results appear, apply targeted sed to those specific files.

**Step 6: Verify YAML name fields**

```bash
grep -rn '^name: ant:' .aether/commands/*.yaml
```

This MUST return zero results. If any results appear, apply sed to those specific files.

**Step 7: Run the full test suite**

```bash
go test ./... -race
```

All tests must pass. If any test fails, inspect the failure output to identify which files still have stale references and fix them.
  </action>
  <acceptance_criteria>
    - `grep -rn '/ant:' --include='*.md' --include='*.go' --include='*.yaml' --include='*.json' --include='*.toml' . | grep -v '.planning/' | grep -v 'node_modules/' | wc -l` returns 0
    - `grep -rn '^name: ant:' .aether/commands/*.yaml | wc -l` returns 0
    - `grep -n '/ant-resume' cmd/codex_visuals.go | wc -l` returns 2 (both lines 339 and 341 updated)
    - `grep -c '/ant:' cmd/codex_visuals.go` returns 0
    - `go test ./cmd/ -run TestCommandWrappersReferenceRealYamlSources -v` passes
    - `go test ./... -race` passes with zero failures
    - Packaging mirrors byte-identical: `for f in .claude/agents/ant/*.md; do diff -q "$f" ".aether/agents-claude/$(basename $f)"; done` produces no output
  </acceptance_criteria>
  <verify>
    <automated>grep -rn '/ant:' --include='*.md' --include='*.go' --include='*.yaml' --include='*.json' --include='*.toml' . | grep -v '.planning/' | grep -v 'node_modules/' | wc -l && go test ./... -race</automated>
  </verify>
  <done>
    All /ant: references replaced with /ant- across entire repo (excluding .planning/).
    All YAML name fields use ant-X format. All tests pass. Single atomic commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is a pure text replacement with no authentication, session management, access control, or input validation changes. No trust boundaries affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44.2-01 | Tampering | Command naming | accept | Rename does not affect authorization or access control. No security boundary crossed. |
| T-44.2-02 | Information Disclosure | .planning/ exclusion | accept | Historical planning files excluded from rename to preserve audit trail integrity. No sensitive data exposed. |
</threat_model>

<verification>
1. `go test ./cmd/ -run "TestClaudeOpenCodeAgentContentParity|TestCommandWrappersReferenceRealYamlSources" -v` -- both pass
2. `grep -rn '/ant:' --include='*.md' --include='*.go' --include='*.yaml' --include='*.json' --include='*.toml' . | grep -v '.planning/' | grep -v 'node_modules/'` -- returns zero results
3. `grep -rn '^name: ant:' .aether/commands/*.yaml` -- returns zero results
4. `go test ./... -race` -- full suite green
5. Packaging mirrors byte-identical to Claude canonical agents
</verification>

<success_criteria>
1. `TestClaudeOpenCodeAgentContentParity` passes (medic body parity between Claude and OpenCode)
2. Zero `/ant:` references outside `.planning/` directory
3. All 50 YAML source files have `name: ant-X` format
4. `go test ./... -race` passes with zero failures
5. All changes committed as a single atomic commit (or two commits: one for medic parity, one for rename)
</success_criteria>

<output>
After completion, create `.planning/phases/44.2-command-hygiene/44.2-01-SUMMARY.md`
</output>
