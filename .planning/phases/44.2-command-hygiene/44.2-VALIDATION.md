---
phase: 44.2
slug: command-hygiene
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-24
---

# Phase 44.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go testing (stdlib) |
| **Config file** | none |
| **Quick run command** | `go test ./cmd/ -run "TestClaudeOpenCodeAgentContentParity|TestCommandWrappersReferenceRealYamlSources" -v` |
| **Full suite command** | `go test ./... -race` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `go test ./cmd/ -run "TestClaudeOpenCodeAgentContentParity|TestCommandWrappersReferenceRealYamlSources" -v`
- **After wave merge:** Run `go test ./... -race`
- **Phase gate:** Full suite green

---

## Phase Requirements -> Test Map

| Req ID | Behavior | Test Type | Automated Command |
|--------|----------|-----------|-------------------|
| PUB-03 (R061) | Medic body parity passes | unit | `go test ./cmd/ -run TestClaudeOpenCodeAgentContentParity -v` |
| REL-01 (R059) | No stale colon refs remain | grep check | `grep -rn '/ant:' --include='*.md' --include='*.go' --include='*.yaml' --include='*.json' --include='*.toml' \| grep -v '.planning/'` (expect zero) |
| REL-01 (R059) | All YAML name fields use hyphen | grep check | `grep -rn '^name: ant:' .aether/commands/*.yaml` (expect zero) |
| PUB-03 (R061) | Full test suite passes | integration | `go test ./...` |

---

## Wave 0 Gaps

None — existing test infrastructure covers all phase requirements.

---

## Verification Checklist

- [ ] `TestClaudeOpenCodeAgentContentParity` passes
- [ ] `TestCommandWrappersReferenceRealYamlSources` passes
- [ ] Zero `/ant:` references outside `.planning/`
- [ ] Zero `name: ant:` in YAML frontmatter
- [ ] `go test ./... -race` passes with zero failures
