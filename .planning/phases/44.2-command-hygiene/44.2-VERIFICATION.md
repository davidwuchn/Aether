---
phase: 44.2-command-hygiene
verified: 2026-04-24T12:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
gaps: []
human_verification:
  - test: "Check site/index.html colon reference"
    expected: "No /ant: references remain; if site is still used, the reference at line 149 should be /ant-run"
    why_human: "site/ directory may be a development artifact destined for a separate repo (per MEMORY.md). Deciding whether to update requires knowing if this file is still active."
  - test: "Decide on .aether/chambers/ colon references in spawn-tree.txt"
    expected: "Either 2 tracked spawn-tree.txt files updated, or explicitly accepted as historical archive data not worth modifying"
    why_human: "Archived colony spawn trees contain 1607 /ant: references in tracked files. The plan targeted .md/.go/.yaml/.json/.toml but not .txt. Requires human decision on scope."
  - test: "Decide on .aether/schemas/examples/queen-wisdom-example.xml colon reference"
    expected: "Example XML updated to /ant-init, or accepted as schema example not requiring rename"
    why_human: "Tracked schema example file contains /ant:init at line 213. Outside planned rename scope (.xml not in target extensions). Requires scope decision."
---

# Phase 44.2: Command Hygiene and Agent Parity Verification Report

**Phase Goal:** Fix aether-medic.md agent body parity mismatch between Claude and OpenCode, and rename all Aether slash commands from colon format (`ant:command`) to hyphen format (`ant-command`) to comply with current Claude Code skill naming rules.
**Verified:** 2026-04-24T12:00:00Z
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `TestClaudeOpenCodeAgentContentParity` passes (medic body parity) | VERIFIED | `go test ./cmd/ -run TestClaudeOpenCodeAgentContentParity -v` passes. OpenCode medic body matches Claude canonical (step 8, aether publish recovery, --deep scan all present). |
| 2 | All 50 slash commands use hyphen format (e.g., `ant-build` not `ant:build`) | VERIFIED | `grep -rn '^name: ant-' .aether/commands/*.yaml` returns 50 results, `grep -rn '^name: ant:'` returns 0. All 50 YAML files use `name: ant-X` format. |
| 3 | All command references in YAML sources, wrappers, docs, and tests updated | VERIFIED | Zero `/ant:` references in any `.md`, `.go`, `.yaml`, `.json`, or `.toml` file outside `.planning/`. `TestCommandWrappersReferenceRealYamlSources` passes. `codex_visuals.go` lines 339,341 both show `/ant-resume`. |
| 4 | `go test ./...` passes with zero failures | VERIFIED | `go test ./... -race` passes all packages (cached, all green). |
| 5 | OpenCode medic agent body matches Claude canonical | VERIFIED | Bodies match after frontmatter offset. `aether publish --channel dev` present (1 hit). `Recommend integrity check` step 8 present (1 hit). `diff` of trimmed bodies shows no meaningful differences. |
| 6 | Zero `/ant:` references outside `.planning/` directory (for targeted file types) | VERIFIED | `grep -rn '/ant:' --include='*.md' --include='*.go' --include='*.yaml' --include='*.json' --include='*.toml' . | grep -v '.planning/' | wc -l` returns 0. |
| 7 | All 50 YAML source files have `name: ant-X` format | VERIFIED | 50/50 YAML files have `name: ant-` prefix. 0 files have `name: ant:` prefix. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.opencode/agents/aether-medic.md` | OpenCode medic with body matching Claude canonical | VERIFIED | Body synced. Step 8, `aether publish --channel dev`, `--deep` scan all present. |
| `.aether/agents-claude/aether-medic.md` | Byte-identical packaging mirror to Claude canonical | VERIFIED | `diff .claude/agents/ant/aether-medic.md .aether/agents-claude/aether-medic.md` returns no output. All 25 agent mirrors verified byte-identical. |
| `cmd/codex_visuals.go` | Go runtime help text with `/ant-resume` references | VERIFIED | Lines 339 and 341 both contain `/ant-resume`. Zero `/ant:` references remain. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `.opencode/agents/aether-medic.md` | `.claude/agents/ant/aether-medic.md` | body content parity | WIRED | Bodies match after frontmatter offset. `TestClaudeOpenCodeAgentContentParity` confirms. |
| `.aether/agents-claude/aether-medic.md` | `.claude/agents/ant/aether-medic.md` | byte-identical packaging mirror | WIRED | diff returns no output for all 25 agents. |

### Data-Flow Trace (Level 4)

N/A -- this phase is a text-replacement and parity fix. No dynamic data flows to trace.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Medic parity test passes | `go test ./cmd/ -run TestClaudeOpenCodeAgentContentParity -v` | PASS (cached) | VERIFIED |
| Command wrapper YAML reference test | `go test ./cmd/ -run TestCommandWrappersReferenceRealYamlSources -v` | PASS (cached) | VERIFIED |
| Full test suite with race detection | `go test ./... -race` | All packages pass | VERIFIED |
| No colon references in targeted types | `grep -rn '/ant:' --include='*.md' --include='*.go' --include='*.yaml' --include='*.json' --include='*.toml' \| grep -v '.planning/'` | 0 results | VERIFIED |
| All YAML name fields hyphen format | `grep -rn '^name: ant:' .aether/commands/*.yaml` | 0 results | VERIFIED |
| Codex visuals hyphen format | `grep -c '/ant:' cmd/codex_visuals.go` | 0 results | VERIFIED |
| Commits exist in git log | `git log --oneline` | 61c1eca3, d4f67fcd found | VERIFIED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PUB-03 (R061) | 44.2-01 | Downstream stale publish detection | SATISFIED | Command rename fixes test assertions, enabling `go test ./... -race` to pass. Full test suite green confirms publish integrity tests work. |
| REL-01 (R062) | 44.2-01 | Release integrity validation across surfaces | SATISFIED | All 50 YAML sources, wrappers, docs, tests, and Go production code use consistent hyphen format. `aether integrity` checks command naming consistency across companion-file surfaces. |

No orphaned requirements found. Both PUB-03 and REL-01 are properly traced in REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| N/A | N/A | No anti-patterns found | N/A | N/A |

### Out-of-Scope Colon References (Informational)

The rename targeted 5 file extensions (`.md`, `.go`, `.yaml`, `.json`, `.toml`) per the plan's acceptance criteria. The following tracked files outside `.planning/` still contain `/ant:` references in untargeted file types:

| File | Type | Count | Tracked | Context |
|------|------|-------|---------|---------|
| `.aether/chambers/*/spawn-tree.txt` (2 files) | `.txt` | 2 refs | Yes | Archived colony spawn trees (historical data) |
| `.aether/schemas/examples/queen-wisdom-example.xml` | `.xml` | 1 ref | Yes | Schema example with `/ant:init` |
| `site/index.html` | `.html` | 1 ref | Yes | Website code block with `/ant:run` |

These are outside the planned grep scope but within the broader claim of "zero /ant: references outside .planning/". Decision needed on whether to include these in scope (see Human Verification).

### Human Verification Required

### 1. site/index.html colon reference

**Test:** Open `site/index.html` and check line 149 for `/ant:run`. Decide if this should be `/ant-run`.
**Expected:** Either updated to `/ant-run` or explicitly accepted as out-of-scope (MEMORY.md notes site lives in a separate repo).
**Why human:** The `site/` directory may be a development artifact. Per MEMORY.md, the website lives in a separate repo (aetherantcolony.com). The human needs to decide if this tracked file should be updated or if it is stale/destined for removal.

### 2. Archived chambers spawn-tree.txt colon references

**Test:** Check if the 2 tracked `spawn-tree.txt` files in `.aether/chambers/` should have their `/ant:` references updated.
**Expected:** Either updated for consistency (SUMMARY claims chambers were included in scope) or explicitly accepted as historical archive data not worth modifying.
**Why human:** The SUMMARY claims "Included .aether/chambers/ in rename scope" but 1607 references remain across all chambers files and 2 are git-tracked. The `.txt` extension was not in the plan's grep target list. Requires scope decision.

### 3. Schema example XML colon reference

**Test:** Check `.aether/schemas/examples/queen-wisdom-example.xml` line 213 for `/ant:init`.
**Expected:** Either updated to `/ant-init` or accepted as schema example not requiring rename.
**Why human:** The `.xml` extension was not in the plan's target list. This is a tracked schema example file. Requires scope decision.

### Gaps Summary

No gaps found. All 7 must-have truths are verified. The rename was executed correctly for all targeted file types. Three minor out-of-scope colon references exist in tracked files (`.txt`, `.xml`, `.html`) that require human decisions on scope inclusion.

---

_Verified: 2026-04-24T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
