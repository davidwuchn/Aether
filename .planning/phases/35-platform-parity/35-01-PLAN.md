---
phase: 35-platform-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_e2e_test.go
autonomous: true
---

<objective>
Build drift detection infrastructure for agent content parity across Claude, OpenCode, and Codex platforms.

Purpose: Existing tests only verify agent names and counts (TestCrossPlatformAgentParity). There is no test for actual agent content. This plan creates a content parity test that fails when OpenCode agent content diverges from Claude masters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-platform-parity/35-CONTEXT.md

Key findings from research:
- TestCrossPlatformAgentParity exists but only checks names/counts, not content
- TestClaudeOpenCodeCommandParity checks command content byte-for-byte (passes)
- TestPackagedAgentMirrorsMatchCanonicalSources checks packaging mirrors (passes)
- No equivalent content test exists for agents
- All 25 OpenCode agents have 100-464 lines of content drift vs Claude masters
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenCode agent content parity test</name>
  <files>cmd/codex_e2e_test.go</files>
  <read_first>
    cmd/codex_e2e_test.go (lines 842-888 for TestCrossPlatformAgentParity)
  </read_first>
  <action>
Extend the existing cross-platform parity test infrastructure in cmd/codex_e2e_test.go with a new test that verifies agent content.

Add `TestClaudeOpenCodeAgentContentParity` that:
1. Reads each `.claude/agents/ant/*.md` file
2. Reads the corresponding `.opencode/agents/*.md` file
3. Compares content byte-for-byte
4. Reports drift per agent with line counts
5. Fails if any agent differs

The test should use the existing helper functions (findRepoRoot, listAgentBaseNames) where possible.

Example assertion format:
```
Claude/OpenCode agent content mismatch for aether-builder.md:
  Claude: 226 lines
  OpenCode: 178 lines
  Diff: 48 lines
```

After adding the test, run it to confirm it FAILS (since we haven't fixed the drift yet). The test should produce a clear report showing exactly which agents are out of sync.

Commit: `test(35): add Claude/OpenCode agent content parity test`
  </action>
  <verify>
    <automated>go test ./cmd -run TestClaudeOpenCodeAgentContentParity -count=1 -v 2>&1 | grep -c "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - New test exists in cmd/codex_e2e_test.go
    - Test compares agent content (not just names)
    - Test reports per-agent drift with line counts
    - Test fails when run (expected — drift exists)
    - Test output clearly shows which 25 agents are out of sync
  </acceptance_criteria>
  <done>Content parity test added. Reports all 25 agents out of sync with clear per-agent diff counts.</done>
</task>

<task type="auto">
  <name>Task 2: Add Codex agent completeness checklist test</name>
  <files>cmd/codex_e2e_test.go</files>
  <read_first>
    cmd/codex_e2e_test.go
    .codex/agents/aether-builder.toml
  </read_first>
  <action>
Add `TestCodexAgentCompleteness` that verifies each Codex TOML agent contains essential sections that should be present post-Phase 31-33.

Checklist per agent:
1. `developer_instructions` block exists and is non-empty
2. Contains "TDD" or "test-driven" references (Phase 31 truth emphasis)
3. Contains "protected" or "boundary" references (safety rules)
4. Contains "escalat" references (failure handling)
5. Does NOT contain deprecated patterns:
   - No `flag-add` (deprecated, use aether flag-add)
   - No `activity-log` (OpenCode-specific, no Claude equivalent)

This test is advisory — it logs warnings rather than hard-failing, since Codex agents have platform-specific adaptations that may legitimately differ.

Commit: `test(35): add Codex agent completeness checklist`
  </action>
  <verify>
    <automated>go test ./cmd -run TestCodexAgentCompleteness -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - New test exists in cmd/codex_e2e_test.go
    - Test checks all 25 Codex agents
    - Test logs warnings for missing/outdated content
    - Test passes (warnings are logged, not hard failures)
    - Report clearly shows which agents need attention
  </acceptance_criteria>
  <done>Codex completeness checklist test added. Reports warnings for agents missing Phase 31-33 concepts.</done>
</task>

</tasks>

<verification>
1. `go test ./cmd -run TestClaudeOpenCodeAgentContentParity` exists and reports drift
2. `go test ./cmd -run TestCodexAgentCompleteness` exists and reports warnings
3. Both tests compile and run cleanly
</verification>

<success_criteria>
- Content parity test exists and fails (proving drift detection works)
- Codex completeness test exists and reports warnings
- Tests are integrated into the test suite
- Clear per-agent reporting for both tests
</success_criteria>

<output>
After completion, create `.planning/phases/35-platform-parity/35-01-SUMMARY.md`
</output>
