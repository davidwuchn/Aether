---
phase: 35-platform-parity
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - .codex/agents/*.toml
autonomous: true
---

<objective>
Review and update all 25 Codex TOML agents to reflect Phase 31-33 runtime changes while preserving platform-specific adaptations.

Purpose: Codex agents are well-crafted for their platform (different tools, output format, features) but may be missing updates from recent phases. This plan adds missing content without rebuilding the agents from scratch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-platform-parity/35-CONTEXT.md

Key findings from research:
- Codex agents have NO references to Phase 31-33 runtime concepts:
  - No UpdateJSONAtomically (atomic state advancement, R051)
  - No FakeInvoker/NewWorkerInvokerOrError (honest invokers, R045)
  - No verified_partial/bypass concepts (continue truth, R047-R048)
  - No abandoned build detection (Phase 32)
  - No archaeologist caste (Phase 33)
- Codex watcher references `flag-add` (deprecated — should use `aether flag-add`)
- Codex agents have no resume/context-clear guidance
- Codex builder has good TDD/debugging content already
- Codex agents use `shell` and `apply_patch` tools (correct for Codex)
- Codex agents use JSON output format (correct for Codex)

What to preserve in Codex agents:
- TOML format with `developer_instructions` block
- `nickname_candidates` field
- Platform-specific tool references (`shell`, `apply_patch`)
- JSON output format in instructions
- Codex-specific failure handling tiers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 31-33 runtime concepts to core Codex agents</name>
  <files>.codex/agents/aether-builder.toml, .codex/agents/aether-watcher.toml, .codex/agents/aether-queen.toml, .codex/agents/aether-scout.toml</files>
  <read_first>
    .codex/agents/aether-builder.toml
    .codex/agents/aether-watcher.toml
    .codex/agents/aether-queen.toml
    .codex/agents/aether-scout.toml
    .claude/agents/ant/aether-builder.md (for reference on what Phase 31-33 content looks like in Claude agents)
  </read_first>
  <action>
Update the 4 most critical Codex agents with Phase 31-33 content. Focus on concepts that affect runtime behavior, not platform-specific details.

For each agent, add a new section within `developer_instructions`:

**aether-builder.toml — Add "Runtime Truth" section:**
- Reference: UpdateJSONAtomically for state saves before side effects
- Reference: FakeInvoker is blocked from production paths; use honest invokers
- Reference: State saved before reports/claims, not after
- Keep existing TDD/debugging content intact

**aether-watcher.toml — Add "Verification Truth" section:**
- Reference: verified_partial is not a pass — must verify all tasks
- Reference: Environmental dismissal removed — all failures produce honest summaries
- Reference: Git-verified claims required for in-repo builds
- Fix: Replace `flag-add` with `aether flag-add` in examples
- Keep existing verification flow intact

**aether-queen.toml — Add "Ceremony Integrity" section:**
- Reference: Abandoned build detection (all dispatches stuck >10 min)
- Reference: Stale report cleanup before continue verification
- Reference: Context-clear/resume guidance for session continuity
- Keep existing orchestration content intact

**aether-scout.toml — Add "Survey Completeness" section:**
- Reference: Archaeologist caste for git history analysis
- Reference: Colony-prime token budget and trim order
- Keep existing research content intact

Each addition should be a distinct markdown section within `developer_instructions` (since TOML triple-quoted strings can contain markdown).

After edits, verify:
```bash
for agent in .codex/agents/aether-{builder,watcher,queen,scout}.toml; do
  echo "=== $(basename $agent) ==="
  grep -c "UpdateJSONAtomically\|FakeInvoker\|verified_partial\|abandoned\|archaeologist\|context.*clear\|resume" "$agent" || echo "0 matches"
done
```

Commit per agent or as a batch: `fix(35): add Phase 31-33 runtime concepts to Codex {agent} agents`
  </action>
  <verify>
    <automated>go test ./cmd -run TestCodexAgentCompleteness -count=1 -v 2>&1 | grep -c "warning\|WARN"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 core Codex agents updated with Phase 31-33 content
    - No deprecated patterns (flag-add without aether prefix)
    - Platform-specific content preserved (tools, format, nicknames)
    - Codex completeness test shows reduced warnings
    - go test ./cmd -race -count=1 passes
  </acceptance_criteria>
  <done>4 core Codex agents updated with Phase 31-33 runtime concepts. Deprecated patterns removed.</done>
</task>

<task type="auto">
  <name>Task 2: Quick-review remaining 21 Codex agents</name>
  <files>.codex/agents/*.toml</files>
  <read_first>
    .codex/agents/*.toml (scan for deprecated patterns)
  </read_first>
  <action>
Do a quick scan of the remaining 21 Codex agents for the same issues:

1. **Find deprecated `flag-add` references:**
   ```bash
   grep -rn "flag-add" .codex/agents/*.toml | grep -v "aether flag-add"
   ```
   Replace bare `flag-add` with `aether flag-add` where found.

2. **Find missing Phase 31-33 concepts in specialist agents:**
   Check if specialist agents (auditor, gatekeeper, tracker, etc.) need updates:
   - Auditor: should reference git-verified claims, bypass detection
   - Gatekeeper: should reference FakeInvoker blocking, honest invokers
   - Tracker: should reference atomic state, UpdateJSONAtomically
   - Measurer: should reference concurrent write detection (race conditions)
   - Chaos: should reference bypass path testing

3. **Add minimal updates where critical:**
   Only add content to agents where the missing concept affects their core function.
   Don't add ceremony content to surveyors, don't add audit content to builders, etc.

4. **Preserve all platform-specific content:**
   - Keep `shell`/`apply_patch` tool references
   - Keep JSON output format
   - Keep `nickname_candidates`
   - Keep failure handling tiers

Commit: `fix(35): review and update remaining Codex agents for Phase 31-33`
  </action>
  <verify>
    <automated>go test ./cmd -run TestCodexAgentCompleteness -count=1 -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All deprecated `flag-add` references replaced with `aether flag-add`
    - Specialist agents updated where Phase 31-33 affects their domain
    - No platform-specific content accidentally removed
    - Codex completeness test passes with zero or minimal warnings
    - go test ./cmd -race -count=1 passes
  </acceptance_criteria>
  <done>All 25 Codex agents reviewed. Deprecated patterns fixed. Specialist agents updated where needed.</done>
</task>

</tasks>

<verification>
1. `go test ./cmd -run TestCodexAgentCompleteness` passes with zero warnings
2. `go test ./cmd -run TestCrossPlatformAgentParity` passes (names still match)
3. `go test ./cmd -race -count=1` passes clean
4. No deprecated `flag-add` without `aether` prefix remains
</verification>

<success_criteria>
- All 25 Codex agents reviewed for Phase 31-33 completeness
- Core agents (builder, watcher, queen, scout) have explicit runtime truth sections
- No deprecated patterns remain
- Platform-specific adaptations preserved
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/35-platform-parity/35-03-SUMMARY.md`
</output>
