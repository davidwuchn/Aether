---
phase: 64.1-fix-continue-watcher-timeout-blocking-advancement
plan: 01
subsystem: runtime
tags: [go, watcher, timeout, verification, continue]

requires:
  - phase: 64
    provides: continue ceremony with external watcher integration
provides:
  - attachExternalContinueWatcher timeout advisory behavior
affects:
  - continue ceremony
  - phase advancement gating

tech-stack:
  added: []
  patterns: [advisory vs blocking failure classification]

key-files:
  created: []
  modified:
    - cmd/codex_continue_finalize.go

key-decisions:
  - "External watcher timeout is advisory (warning only) when runtime verification passed"
  - "External watcher failure remains a hard block"
  - "Internal watcher timeout behavior is unchanged"

patterns-established:
  - "Status-specific gate logic: distinguish timeout (advisory) from failed (blocking) in external watcher results"

requirements-completed: [FIX-01, FIX-02, FIX-03]

duration: 5min
completed: 2026-04-27
---

# Phase 64.1: Fix continue watcher timeout blocking advancement — Summary

**External watcher timeout now treated as advisory warning instead of blocking advancement when runtime verification steps passed**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-27T22:00:00Z
- **Completed:** 2026-04-27T22:05:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Modified `attachExternalContinueWatcher()` in `cmd/codex_continue_finalize.go` to distinguish "timeout" from "failed" status
- Timeout now appends to `BlockingIssues` for visibility but leaves `Passed` and `ChecksPassed` as `true`
- Actual failures (status != "timeout") still set `Passed` and `ChecksPassed` to `false`
- All targeted tests pass; internal watcher path unchanged

## Task Commits

1. **Task 1: Fix attachExternalContinueWatcher timeout advisory behavior** — `7301cfa8` (fix)

## Files Created/Modified

- `cmd/codex_continue_finalize.go` — `attachExternalContinueWatcher()` now treats timeout as advisory

## Decisions Made

- Followed plan exactly — no deviations. Surgical change limited to `attachExternalContinueWatcher()` per D-06.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

- Full `go test ./cmd/...` suite shows 3 pre-existing failures in `TestMergeExternalContinueResultsToleratesMissing`, `TestExternalContinueReviewReportTimeoutNotBlocking`, and `TestExternalContinueReviewReportSkipMissing`. These failures exist on the base branch (unrelated `--skip-watchers` feature in progress) and are not caused by this change. Targeted tests for this fix all pass.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Continue ceremony advancement unblocked for external watcher timeout scenarios
- Ready for Phase 65

---
*Phase: 64.1-fix-continue-watcher-timeout-blocking-advancement*
*Completed: 2026-04-27*
