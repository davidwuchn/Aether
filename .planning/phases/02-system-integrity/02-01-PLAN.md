---
phase: 02-system-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/suggest.go
  - cmd/maintenance.go
  - cmd/helpers.go
  - cmd/maintenance_test.go
autonomous: true
requirements: [INTG-04, INTG-05, INTG-03]

must_haves:
  truths:
    - "A user pheromone with source='user' or source='cli' is never flagged as a test artifact by isTestArtifact, regardless of content"
    - "Running backup-prune-global without --confirm produces a dry-run preview and deletes nothing"
    - "Running temp-clean without --confirm produces a dry-run preview and deletes nothing"
    - "Error messages in modified files follow the prefix: description. remediation hint format"
  artifacts:
    - path: "cmd/suggest.go"
      provides: "Fixed isTestArtifact function with source field check"
      contains: "source.*user.*cli"
    - path: "cmd/maintenance.go"
      provides: "backup-prune-global and temp-clean with --confirm flags"
      contains: "confirm.*GetBool"
    - path: "cmd/helpers.go"
      provides: "Enhanced outputError with consistent format"
      contains: "remediation"
    - path: "cmd/maintenance_test.go"
      provides: "Tests for isTestArtifact fix and confirmation gates"
      exports: ["TestIsTestArtifact", "TestBackupPrune", "TestTempClean"]
  key_links:
    - from: "cmd/maintenance.go"
      to: "cmd/suggest.go"
      via: "isTestArtifact function call"
      pattern: "isTestArtifact"
    - from: "cmd/maintenance.go"
      to: "cmd/helpers.go"
      via: "outputError calls"
      pattern: "outputError"
---

<objective>
Fix data-loss bugs and add safety gates to destructive hygiene commands.

Purpose: The isTestArtifact function false-positives on user-created pheromones containing words like "test" or "demo", causing data-clean to delete legitimate user signals. The backup-prune-global and temp-clean commands delete files with no confirmation step, risking accidental data loss. Error messages lack consistent formatting.

Output: Fixed isTestArtifact with source-field check, confirmation gates on destructive commands, consistent error format in modified files, and passing tests for all changes.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-system-integrity/02-CONTEXT.md
@.planning/phases/02-system-integrity/02-RESEARCH.md
@.planning/phases/02-system-integrity/02-VALIDATION.md
@cmd/suggest.go
@cmd/maintenance.go
@cmd/helpers.go
@cmd/testing_main_test.go

<interfaces>
<!-- Key types and contracts the executor needs. -->

From cmd/suggest.go (current isTestArtifact):
```go
func isTestArtifact(signal map[string]interface{}) bool {
    // signal["id"] -> string (e.g., "test_123", "user_signal_abc")
    // signal["source"] -> string ("user", "cli", "auto", "promotion")
    // signal["content"] -> string OR map[string]interface{} with "text" key
}
```

From cmd/maintenance.go (data-clean --confirm pattern to replicate):
```go
confirm, _ := cmd.Flags().GetBool("confirm")
// ... compute removals ...
if confirm && removed > 0 {
    // actually delete
}
outputOK(map[string]interface{}{
    "removed": reportedRemoved,
    "dry_run": !confirm,
})
```

From cmd/helpers.go (current outputError):
```go
func outputError(code int, message string, details interface{})
func outputOK(result interface{})
func outputErrorMessage(message string) // convenience wrapper with code 1
```

From cmd/testing_main_test.go (test helpers):
```go
func saveGlobals(t *testing.T)   // save/restore package-level globals
func resetRootCmd(t *testing.T)   // reset rootCmd state and subcommand flags
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix isTestArtifact false-positive and add confirmation gates to destructive commands</name>
  <files>cmd/suggest.go, cmd/maintenance.go, cmd/maintenance_test.go</files>
  <read_first>
    - cmd/suggest.go (current isTestArtifact function at lines 62-84)
    - cmd/maintenance.go (all three hygiene commands: data-clean at 17-92, backup-prune-global at 94-168, temp-clean at 170-213)
    - cmd/testing_main_test.go (test infrastructure: saveGlobals, resetRootCmd, createTestStore)
  </read_first>
  <behavior>
    - Test 1: isTestArtifact returns false for signals with source "user" regardless of content (e.g., content contains "test signal")
    - Test 2: isTestArtifact returns false for signals with source "cli" regardless of content (e.g., id starts with "test_")
    - Test 3: isTestArtifact returns true for signals with source "auto" and content containing "test signal"
    - Test 4: isTestArtifact returns true for signals with source "auto" and id starting with "test_"
    - Test 5: isTestArtifact returns false for signals with source "user" and content "demo pattern"
    - Test 6: backup-prune-global without --confirm outputs dry_run:true and pruned:0
    - Test 7: backup-prune-global with --confirm actually prunes files beyond cap
    - Test 8: temp-clean without --confirm outputs dry_run:true and cleaned:0
    - Test 9: temp-clean with --confirm actually removes old temp files
  </behavior>
  <action>
    **Part A: Fix isTestArtifact (per D-01 in CONTEXT.md and INTG-04)**

    In cmd/suggest.go, modify the isTestArtifact function to check the signal's `source` field BEFORE checking content. The fix:

    1. Extract source from signal map: `source, _ := signal["source"].(string)`
    2. If source is "user" or "cli", return false immediately (user-created signals are never test artifacts regardless of content)
    3. Keep existing id-prefix and content-substring checks for non-user sources ("auto", "promotion", system, etc.)
    4. Add a comment explaining the source-field check prevents false positives on user-created pheromones

    The function signature stays the same: `func isTestArtifact(signal map[string]interface{}) bool`

    **Part B: Add --confirm flags to backup-prune-global and temp-clean (per INTG-05)**

    In cmd/maintenance.go:

    For backup-prune-global (lines 94-168):
    1. Add `confirm, _ := cmd.Flags().GetBool("confirm")` at the start of RunE (after the store nil check)
    2. Keep all existing computation logic (read dir, sort files, compute pruneCount)
    3. Before the delete loop (line 156), add: `if !confirm { outputOK(map[string]interface{}{"pruned": 0, "kept": len(files), "would_prune": pruneCount, "dry_run": true}); return nil }`
    4. When confirm is true, run existing delete logic and output `{"pruned": pruneCount, "kept": cap}` (no dry_run field or dry_run: false)
    5. In the init() function, add: `backupPruneGlobalCmd.Flags().Bool("confirm", false, "Confirm pruning (default: dry-run)")`

    For temp-clean (lines 170-213):
    1. Add `confirm, _ := cmd.Flags().GetBool("confirm")` at the start of RunE
    2. Collect removable files into a list first (don't delete in the same loop)
    3. If !confirm, output `{"cleaned": 0, "would_clean": len(removable), "dry_run": true}` and return nil
    4. If confirm, iterate and delete, then output `{"cleaned": len(removable)}`
    5. In the init() function, add: `tempCleanCmd.Flags().Bool("confirm", false, "Confirm cleanup (default: dry-run)")`

    **Part C: Write tests in cmd/maintenance_test.go**

    Create test file cmd/maintenance_test.go with:

    For isTestArtifact tests:
    - Test function: `TestIsTestArtifact`
    - Table-driven test cases covering: source="user" with test content, source="cli" with test_ prefix, source="auto" with test content (should flag), source="user" with demo content, source="promotion" with test prefix (should flag), missing source field (should use existing content logic)

    For confirmation gate tests:
    - Test function: `TestBackupPruneGlobalConfirmGate`
    - Test function: `TestTempCleanConfirmGate`
    - Use `saveGlobals(t)`, `resetRootCmd(t)`, `newTestStoreWithRoot(t)` or `createTestStore(dataDir)` for isolation
    - Create actual backup files in temp dir to test pruning
    - Create actual temp files older than 7 days (set mod time manually)
    - Verify JSON output contains dry_run:true when --confirm is absent
    - Verify files are actually removed when --confirm is present
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/ -run "TestIsTestArtifact|TestBackupPruneGlobalConfirmGate|TestTempCleanConfirmGate" -count=1 -v</automated>
  </verify>
  <done>
    - isTestArtifact returns false for source="user" and source="cli" regardless of content
    - isTestArtifact still returns true for source="auto" with test artifact patterns
    - backup-prune-global without --confirm outputs dry_run:true and deletes nothing
    - backup-prune-global with --confirm prunes files beyond cap
    - temp-clean without --confirm outputs dry_run:true and deletes nothing
    - temp-clean with --confirm removes files older than 7 days
    - All new tests pass
    - Existing tests still pass: go test ./cmd/ -count=1
  </done>
</task>

<task type="auto">
  <name>Task 2: Standardize error formatting in modified files</name>
  <files>cmd/helpers.go, cmd/maintenance.go</files>
  <read_first>
    - cmd/helpers.go (outputError function, outputErrorMessage, all helper functions)
    - cmd/maintenance.go (after Task 1 modifications -- verify current state of error messages)
  </read_first>
  <acceptance_criteria>
    - cmd/helpers.go contains a comment block documenting the error message convention: "prefix: description. remediation hint"
    - cmd/maintenance.go error calls in backup-prune-global and temp-clean follow the prefix:description format
    - Example: outputError(1, "backup: failed to read directory. Check path exists.", nil)
    - go test ./cmd/ -count=1 passes with no regressions
  </acceptance_criteria>
  <action>
    **Per INTG-03:** Standardize error messages in the files modified by this phase.

    The research determined that mass-updating all 200+ outputError call sites is high-risk for INTG-06 (no regressions). Instead, apply the convention to new/changed code only.

    In cmd/helpers.go:
    1. Add a documentation comment above the outputError function explaining the standard format:
       ```
       // Error message convention (INTG-03):
       // Format: "prefix: description. remediation hint"
       // Example: "store: no store initialized. Run 'aether init' first."
       // This convention applies to new and modified commands.
       // Existing commands will be updated incrementally in later phases.
       ```
    2. Do NOT change the outputError function signature or behavior -- it already works correctly.

    In cmd/maintenance.go:
    1. Update error messages in backup-prune-global RunE:
       - `outputError(1, fmt.Sprintf("failed to read backup directory: %v", err), nil)` becomes `outputError(1, fmt.Sprintf("backup: failed to read backup directory: %v. Verify the backup path exists.", err), nil)`
    2. Update error messages in temp-clean RunE:
       - `outputError(1, fmt.Sprintf("failed to read temp directory: %v", err), nil)` becomes `outputError(1, fmt.Sprintf("temp: failed to read temp directory: %v. Verify the .aether/temp path.", err), nil)`
    3. Update error messages in data-clean RunE:
       - `outputError(1, fmt.Sprintf("failed to parse pheromones.json: %v", err), nil)` becomes `outputError(1, fmt.Sprintf("data-clean: failed to parse pheromones.json: %v. Check file is valid JSON.", err), nil)`
       - `outputError(2, fmt.Sprintf("failed to save pheromones.json: %v", err), nil)` becomes `outputError(2, fmt.Sprintf("data-clean: failed to save pheromones.json: %v. Check disk space and permissions.", err), nil)`

    Do NOT modify error messages in other files -- they will be updated in later phases.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/ -count=1 -timeout 60s && grep -c "remediation hint" cmd/helpers.go</automated>
  </verify>
  <done>
    - cmd/helpers.go documents the error message convention with examples
    - Error messages in maintenance.go follow the prefix:description.hint format
    - No changes to outputError function signature or behavior
    - All cmd/ tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> isTestArtifact | Signal content comes from user-created pheromones (untrusted input). Source field check prevents data loss from misclassification. |
| CLI flags -> destructive operations | --confirm flag controls whether files are deleted. Without it, commands are read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | backup-prune-global | mitigate | --confirm flag required for actual deletion; dry-run by default |
| T-02-02 | Tampering | temp-clean | mitigate | --confirm flag required for actual deletion; dry-run by default |
| T-02-03 | Tampering | isTestArtifact | mitigate | Source field check prevents false-positive classification of user signals |
| T-02-04 | Information Disclosure | error messages | accept | Error messages may reveal file paths but this is standard CLI behavior, no secrets in paths |
</threat_model>

<verification>
1. `go test ./cmd/ -run "TestIsTestArtifact" -count=1` -- isTestArtifact source-field fix verified
2. `go test ./cmd/ -run "TestBackupPruneGlobalConfirmGate|TestTempCleanConfirmGate" -count=1` -- confirmation gates verified
3. `go test ./cmd/ -count=1` -- no regressions in cmd package
4. `grep "remediation hint" cmd/helpers.go` -- error format convention documented
</verification>

<success_criteria>
1. A user pheromone with source="user" and content containing "test signal" is NOT flagged by isTestArtifact
2. Running `aether backup-prune-global` (no flags) outputs dry_run:true and deletes nothing
3. Running `aether temp-clean` (no flags) outputs dry_run:true and deletes nothing
4. Error messages in maintenance.go follow the prefix:description.hint format
5. All existing cmd/ tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-system-integrity/02-01-SUMMARY.md`
</output>
