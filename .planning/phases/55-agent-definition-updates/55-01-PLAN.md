---
phase: 55-agent-definition-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/ant/aether-gatekeeper.md
  - .claude/agents/ant/aether-auditor.md
  - .claude/agents/ant/aether-chaos.md
  - .claude/agents/ant/aether-watcher.md
  - .claude/agents/ant/aether-archaeologist.md
  - .claude/agents/ant/aether-measurer.md
  - .claude/agents/ant/aether-tracker.md
  - .aether/agents-claude/aether-gatekeeper.md
  - .aether/agents-claude/aether-auditor.md
  - .aether/agents-claude/aether-chaos.md
  - .aether/agents-claude/aether-watcher.md
  - .aether/agents-claude/aether-archaeologist.md
  - .aether/agents-claude/aether-measurer.md
  - .aether/agents-claude/aether-tracker.md
  - .opencode/agents/aether-gatekeeper.md
  - .opencode/agents/aether-auditor.md
  - .opencode/agents/aether-chaos.md
  - .opencode/agents/aether-watcher.md
  - .opencode/agents/aether-archaeologist.md
  - .opencode/agents/aether-measurer.md
  - .opencode/agents/aether-tracker.md
  - .codex/agents/aether-gatekeeper.toml
  - .codex/agents/aether-auditor.toml
  - .codex/agents/aether-chaos.toml
  - .codex/agents/aether-watcher.toml
  - .codex/agents/aether-archaeologist.toml
  - .codex/agents/aether-measurer.toml
  - .codex/agents/aether-tracker.toml
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06
  - AGENT-07
  - AGENT-08
  - AGENT-09

must_haves:
  truths:
    - "Each of 7 review agents has Write in its tools frontmatter across all 4 surfaces"
    - "Each agent body contains write-scope guardrails restricting Write to .aether/data/reviews/ only"
    - "Each agent body lists what it MUST NOT write to (source code, tests, colony state, dreams, .env)"
    - "All 4 surfaces (claude, agents-claude, opencode, codex) are in sync for all 7 agents"
    - "Tracker has carve-out language replacing 'no Write' with scoped Write for bugs domain"
  artifacts:
    - path: ".claude/agents/ant/aether-gatekeeper.md"
      provides: "Gatekeeper agent with Write tool and security domain guardrails"
      contains: "Write"
    - path: ".claude/agents/ant/aether-tracker.md"
      provides: "Tracker agent with scoped Write for bugs domain"
      contains: "review-ledger-write"
    - path: ".opencode/agents/aether-chaos.md"
      provides: "OpenCode Chaos mirror with write: true"
      contains: "write: true"
  key_links:
    - from: ".claude/agents/ant/aether-gatekeeper.md"
      to: ".aether/agents-claude/aether-gatekeeper.md"
      via: "byte-identical copy"
      pattern: "diff.*no differences"
    - from: ".opencode/agents/aether-watcher.md"
      to: ".claude/agents/ant/aether-watcher.md"
      via: "matching body content, updated frontmatter"
      pattern: "write: true"
---

<objective>
Update 7 review agent definitions across 4 surfaces (28 files) to add scoped Write tool access and write-scope guardrails.

Purpose: Review agents currently cannot persist their findings to domain ledgers. Per D-01/D-03, each agent gains Write tool with explicit guardrails restricting writes to their designated review ledger files only. Per D-07/D-08/D-09, Tracker gets a specific carve-out preserving its diagnose-only identity.

Output: 28 agent files updated with Write tool in frontmatter, write-scope guardrails in boundaries section, and consistent "no Write" language replaced across role, critical_rules, and boundaries sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-agent-definition-updates/55-CONTEXT.md
@.planning/phases/55-agent-definition-updates/55-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Edit 7 canonical agent definitions in .claude/agents/ant/ to add Write tool and write-scope guardrails</name>
  <files>
    .claude/agents/ant/aether-gatekeeper.md
    .claude/agents/ant/aether-auditor.md
    .claude/agents/ant/aether-chaos.md
    .claude/agents/ant/aether-watcher.md
    .claude/agents/ant/aether-archaeologist.md
    .claude/agents/ant/aether-measurer.md
    .claude/agents/ant/aether-tracker.md
  </files>
  <action>
For each of the 7 agents, make the following changes to the canonical `.claude/agents/ant/aether-{agent}.md` file. Process the 6 standard agents first (Gatekeeper, Auditor, Chaos, Watcher, Archaeologist, Measurer), then handle Tracker separately with its special carve-out.

**STANDARD AGENTS (Gatekeeper, Auditor, Chaos, Watcher, Archaeologist, Measurer):**

**1. Frontmatter `tools:` line** -- Add `Write` to the comma-separated list:
- Gatekeeper: `tools: Read, Grep, Glob` becomes `tools: Read, Grep, Glob, Write`
- Auditor: `tools: Read, Grep, Glob` becomes `tools: Read, Grep, Glob, Write`
- Chaos: `tools: Read, Bash, Grep, Glob` becomes `tools: Read, Bash, Grep, Glob, Write`
- Watcher: `tools: Read, Bash, Grep, Glob` becomes `tools: Read, Bash, Grep, Glob, Write`
- Archaeologist: `tools: Read, Bash, Grep, Glob` becomes `tools: Read, Bash, Grep, Glob, Write`
- Measurer: `tools: Read, Bash, Grep, Glob` becomes `tools: Read, Bash, Grep, Glob, Write`

**2. `<role>` section** -- Update "no Write" / "read-only" language to reflect scoped Write. For each agent, find and update text claiming the agent has no Write capability:

- Gatekeeper (line ~12): "Your constraint is absolute and by design: you have no Bash." -- Keep this. The `<boundaries>` section handles the Write scope change. In `<boundaries>` (line ~316): Change "Gatekeeper has no Write, Edit, or Bash tools." to "Gatekeeper has Write tool for persisting findings to the security domain review ledger only, and no Edit or Bash tools."
- Auditor (line ~13): Change "No Write. No Edit. No Bash." to "Write access is restricted to persisting findings to your domain review ledger only. No Edit. No Bash."
- Chaos (line ~14): Change "No file modifications. No side effects." to "No source file modifications. No side effects except persisting findings to your domain review ledger."
- Watcher (line ~14): Change "You have no Write or Edit tools." to "You have scoped Write access for persisting findings to your domain review ledger only. You do not modify source code or test files."
- Archaeologist (line ~14): Change "You are strictly read-only. You excavate and report." to "You are read-only except for persisting findings to your domain review ledger. You excavate and report."
- Measurer (line ~14): Change "you do not optimize" is fine -- keep it. But update boundary: "you measure, profile, and analyze -- you do not optimize" stays. Add in boundaries: "Measurer has Write tool restricted to persisting findings only, and no Edit tools."

**3. `<critical_rules>` section** -- Update "no Write or Edit" rules:

For each agent, find rules stating "You have no Write or Edit tools" or equivalent and update to reflect scoped Write exception. Replace with: "You have Write tool access restricted to persisting findings to your domain review ledger via `aether review-ledger-write`. You do not write source code, tests, or any files outside `.aether/data/reviews/`."

Specific locations to update:
- Gatekeeper (line ~142): "Gatekeeper has no Bash tool" -- keep. No Write reference here (Write is in `<boundaries>`).
- Auditor (line ~113): "Auditor is read-only including during Security Lens Mode" -- update to "Auditor is read-only except for persisting findings to domain review ledgers, including during Security Lens Mode"
- Chaos (line ~95): "You have no Write or Edit tools by design." -- update to "You have Write tool restricted to persisting findings to your domain review ledger via `aether review-ledger-write`. You have no Edit tools. No source code modifications."
- Watcher (no explicit "no Write" in critical_rules -- the boundary is in `<boundaries>` section only)
- Archaeologist (line ~147): "You are strictly read-only." -- update to "You are read-only except for persisting findings to your domain review ledger."
- Measurer (line ~141): "You have no Write or Edit tools." -- update to "You have Write tool restricted to persisting findings to your domain review ledger via `aether review-ledger-write`. You have no Edit tools."

**4. `<boundaries>` section** -- Add write-scope guardrail subsection and update first boundary declaration:

Update the first boundary declaration in each agent to acknowledge scoped Write:
- Gatekeeper: "Gatekeeper has Write tool for persisting security findings only, and no Edit or Bash tools."
- Auditor: "Auditor has Write tool restricted to persisting findings only, and no Edit or Bash tools."
- Chaos: "Chaos has Write tool restricted to persisting resilience findings only, and no Edit tools."
- Watcher: "Watcher has scoped Write for persisting testing and quality findings only. No Edit tools."
- Archaeologist: "Archaeologist has Write tool restricted to persisting history findings only, and no Edit tools."
- Measurer: "Measurer has Write tool restricted to persisting performance findings only, and no Edit tools."

Then add a new "### Write-Scope Restriction" subsection at the end of each agent's `<boundaries>` section (before the closing `</boundaries>` tag):

```markdown
### Write-Scope Restriction
You have Write tool access for ONE purpose only: persisting findings to your domain review ledger. You MUST use `aether review-ledger-write` to write findings.

**You MAY write to:**
- `.aether/data/reviews/{domains}/ledger.json` (via `review-ledger-write`)

**You MUST NOT write to:**
- Source code files (any `*.go`, `*.js`, `*.ts`, `*.py`, etc.)
- Test files
- Colony state (`.aether/data/COLONY_STATE.json`, `.aether/data/pheromones.json`, etc.)
- User notes (`.aether/dreams/`)
- Environment files (`.env*`)
- CI configuration (`.github/workflows/`)
- Any file not in `.aether/data/reviews/`

If you need a file modified to address a finding, report it in your return and route to Builder.
```

Replace `{domains}` per agent:
- Gatekeeper: `security`
- Auditor: `quality`, `security`, or `performance`
- Chaos: `resilience`
- Watcher: `testing` or `quality`
- Archaeologist: `history`
- Measurer: `performance`

**5. `<return_format>` section** -- Add findings persistence note after the JSON example block:

```markdown
### Findings Persistence
After completing your analysis, persist findings to your domain review ledger:
```bash
aether review-ledger-write --domain {domain} --phase {N} --findings '<json>' --agent {agent} --agent-name "{your name}"
```
The findings JSON should be an array of objects with: severity, file, line, category, description, suggestion.
```

Replace placeholders per agent:
- Gatekeeper: domain=security, agent=gatekeeper
- Auditor: domain=quality (or security or performance as appropriate), agent=auditor
- Chaos: domain=resilience, agent=chaos
- Watcher: domain=testing (or quality), agent=watcher
- Archaeologist: domain=history, agent=archaeologist
- Measurer: domain=performance, agent=measurer

**TRACKER SPECIAL CASE (per D-07/D-08/D-09):**

Tracker has 6 specific locations referencing "no Write" or "do not write to .aether/data/" that need carve-out language. Tracker's diagnose-only identity must be preserved per D-09 -- Write is for persisting findings ONLY, never for applying fixes.

1. **Frontmatter** (line 4): `tools: Read, Bash, Grep, Glob` becomes `tools: Read, Bash, Grep, Glob, Write`

2. **`<role>` section** (line ~14): Change "You return structured analysis. No activity logs. No side effects." to "You return structured analysis. No activity logs. No side effects except persisting findings to your domain review ledger."

3. **`<critical_rules>` -- "Diagnose Only"** (line ~99): Change "You have no Write or Edit tools. This is intentional and permanent." to "You have Write tool access restricted to persisting findings to the bugs domain review ledger via `aether review-ledger-write`. You still do not fix, modify, or apply changes to source code. This boundary is intentional and permanent."

4. **`<critical_rules>` -- "Never Modify Files"** (line ~117): After "do not edit it", add: "Persisting findings to the review ledger is the sole exception -- use `aether review-ledger-write` for this purpose only."

5. **`<boundaries>` -- "Tracker Is Diagnose-Only"** (line ~256): Change "Tracker has no Write or Edit tools by design. This is a platform-enforced constraint, not a convention." to "Tracker has Write tool restricted to persisting findings to `.aether/data/reviews/bugs/` only. Write is for findings persistence -- never for applying fixes or modifying source code. This boundary is by design."

6. **`<boundaries>` -- "Do not write to `.aether/data/`"** (line ~268): Change "Do not write to `.aether/data/`" to "Do not write to `.aether/data/` except `reviews/bugs/` for persisting bug investigation findings via `aether review-ledger-write`."

7. **`<boundaries>`** -- Add Write-Scope Restriction subsection (same pattern as other agents, but bugs-specific):

```markdown
### Write-Scope Restriction
You have Write tool access for ONE purpose only: persisting findings to the bugs domain review ledger. You MUST use `aether review-ledger-write` to write findings.

**You MAY write to:**
- `.aether/data/reviews/bugs/ledger.json` (via `review-ledger-write`)

**You MUST NOT write to:**
- Source code files (any `*.go`, `*.js`, `*.ts`, `*.py`, etc.)
- Test files
- Colony state (`.aether/data/COLONY_STATE.json`, `.aether/data/pheromones.json`, etc.)
- User notes (`.aether/dreams/`)
- Environment files (`.env*`)
- CI configuration (`.github/workflows/`)
- Any file not in `.aether/data/reviews/bugs/`

Write is available ONLY for persisting findings to the bugs domain ledger. You still do not fix, modify, or apply changes to source code. If you find a bug, describe the fix in `suggested_fix` and return -- Builder applies it.
```

8. **`<return_format>`** -- Add findings persistence note after the JSON example:

```markdown
### Findings Persistence
After completing your investigation, persist findings to your domain review ledger:
```bash
aether review-ledger-write --domain bugs --phase {N} --findings '<json>' --agent tracker --agent-name "{your name}"
```
The findings JSON should be an array of objects with: severity, file, line, category, description, suggestion.
```

**THOROUGHNESS CHECK:** For each agent file, after making all edits, search for remaining occurrences of these patterns and verify none remain without the scoped Write exception:
- "no Write"
- "no Write or Edit"
- "Write or Edit" (in the context of "you have no...")
- "no file modifications"
- "read-only" (only where it claims the agent cannot write at all)

Do NOT change references to "no Edit" or "no Bash" where those tools genuinely remain unavailable (Gatekeeper still has no Bash, Auditor still has no Bash, Gatekeeper/Auditor still have no Edit).
  </action>
  <verify>
    <automated>echo "=== Canonical frontmatter ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f:"; grep "tools:" .claude/agents/ant/aether-$f.md; done && echo "=== review-ledger-write count ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f: $(grep -c 'review-ledger-write' .claude/agents/ant/aether-$f.md)"; done && echo "=== MUST NOT write to ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f: $(grep -c 'MUST NOT write to' .claude/agents/ant/aether-$f.md)"; done && echo "=== Write-Scope Restriction ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f: $(grep -c 'Write-Scope Restriction' .claude/agents/ant/aether-$f.md)"; done && echo "=== Stale no-Write check ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do count=$(grep -c 'no Write or Edit tools' .claude/agents/ant/aether-$f.md || true); if [ "$count" -gt 0 ]; then echo "WARN $f: still has 'no Write or Edit tools' ($count occurrences)"; else echo "$f: OK"; fi; done</automated>
  </verify>
  <done>All 7 canonical agent definitions in .claude/agents/ant/ have Write tool in frontmatter, updated role/critical_rules/boundaries sections reflecting scoped Write, write-scope guardrails with MUST NOT list, and findings persistence CLI example in return format. Tracker has bugs domain carve-out preserving diagnose-only identity per D-07/D-08/D-09.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sync all 7 canonical agents to 3 mirror surfaces (21 files)</name>
  <files>
    .aether/agents-claude/aether-gatekeeper.md
    .aether/agents-claude/aether-auditor.md
    .aether/agents-claude/aether-chaos.md
    .aether/agents-claude/aether-watcher.md
    .aether/agents-claude/aether-archaeologist.md
    .aether/agents-claude/aether-measurer.md
    .aether/agents-claude/aether-tracker.md
    .opencode/agents/aether-gatekeeper.md
    .opencode/agents/aether-auditor.md
    .opencode/agents/aether-chaos.md
    .opencode/agents/aether-watcher.md
    .opencode/agents/aether-archaeologist.md
    .opencode/agents/aether-measurer.md
    .opencode/agents/aether-tracker.md
    .codex/agents/aether-gatekeeper.toml
    .codex/agents/aether-auditor.toml
    .codex/agents/aether-chaos.toml
    .codex/agents/aether-watcher.toml
    .codex/agents/aether-archaeologist.toml
    .codex/agents/aether-measurer.toml
    .codex/agents/aether-tracker.toml
  </files>
  <action>
**Part A: Sync canonical to `.aether/agents-claude/` mirror (7 files)**

This mirror must be byte-identical to the canonical files. For each of the 7 agents:

```bash
cp .claude/agents/ant/aether-{agent}.md .aether/agents-claude/aether-{agent}.md
```

After copying, verify byte-identical with diff for each agent:
```bash
diff .claude/agents/ant/aether-{agent}.md .aether/agents-claude/aether-{agent}.md
```

Expected: no output (files identical) for all 7 agents.

**Part B: Sync to `.opencode/agents/` mirror (7 files)**

OpenCode uses a different frontmatter format: YAML map for tools instead of comma-separated string. The body content (everything after the YAML `---` closer) must match the canonical body exactly.

For each of the 7 agents, update the OpenCode mirror file:

1. Change `write: false` to `write: true` in the YAML frontmatter `tools:` map
2. Replace the body content (everything after the second `---`) with the canonical body from `.claude/agents/ant/aether-{agent}.md`
3. Preserve the OpenCode-specific frontmatter fields: `mode: subagent` and the `tools:` map format

The OpenCode frontmatter should look like this for each agent:
```yaml
---
name: aether-{agent}
description: "{same description as canonical}"
mode: subagent
tools:
  write: true
  edit: false
  bash: {true|false as appropriate}
  grep: true
  glob: true
  task: false
color: "{existing color}"
---
```

Important notes on bash setting per agent:
- Gatekeeper: `bash: false` (no Bash in canonical)
- Auditor: `bash: false` (no Bash in canonical)
- Chaos, Watcher, Archaeologist, Measurer, Tracker: `bash: true` (Bash in canonical)

Preserve the existing color values for each OpenCode agent (they may differ from canonical).

**Part C: Sync to `.codex/agents/` TOML mirror (7 files)**

Codex uses TOML format with a `developer_instructions` block containing the agent body text. There is no tools configuration field in TOML (confirmed by RESEARCH.md assumption A1). The `developer_instructions` body must be updated to reflect the scoped Write capability.

For each of the 7 TOML files:

1. Read the current `developer_instructions` content
2. Find text referencing "no Write", "read-only", "NEVER modify", "No Writes Permitted", or equivalent boundary language
3. Update to include scoped Write exception language consistent with the canonical agent
4. Add a paragraph: "You MAY persist findings to your domain review ledger using `aether review-ledger-write`. This is your ONLY write operation. You MUST NOT write to source code, test files, colony state, or any file outside `.aether/data/reviews/`."
5. Keep the TOML `name`, `description`, `nickname_candidates` fields unchanged

Agent-specific TOML updates:
- Gatekeeper TOML: Currently says "No Writes Permitted" -- update to "Writes restricted to persisting findings to the security domain review ledger via `aether review-ledger-write`."
- Auditor TOML: Currently says "read-only" -- update to include scoped Write exception for quality/security/performance domains.
- Chaos TOML: Currently says "no file modifications" -- update to include resilience findings persistence exception.
- Watcher TOML: Currently says "read-only" -- update to include testing/quality findings persistence exception.
- Archaeologist TOML: Update to include history findings persistence exception.
- Measurer TOML: Update to include performance findings persistence exception.
- Tracker TOML: Currently has "no Write" language -- update with bugs domain carve-out per D-07/D-08.
  </action>
  <verify>
    <automated>echo "=== agents-claude mirror diff ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f:"; diff -q .claude/agents/ant/aether-$f.md .aether/agents-claude/aether-$f.md && echo "IDENTICAL" || echo "MISMATCH"; done && echo "=== opencode write: true check ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f: $(grep -c 'write: true' .opencode/agents/aether-$f.md)"; done && echo "=== opencode review-ledger-write check ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f: $(grep -c 'review-ledger-write' .opencode/agents/aether-$f.md)"; done && echo "=== codex review-ledger-write check ===" && for f in gatekeeper auditor chaos watcher archaeologist measurer tracker; do echo "$f: $(grep -c 'review-ledger-write' .codex/agents/aether-$f.toml)"; done</automated>
  </verify>
  <done>All 7 agent definitions synced across all 4 surfaces: 28 files verified. agents-claude mirrors are byte-identical to canonical. OpenCode mirrors have write: true and matching body content with review-ledger-write references. Codex TOML files reference review-ledger-write in developer_instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent instructions -> LLM behavior | Guardrails are instructional only per D-02 -- relies on LLM following instructions |
| Agent -> review-ledger-write CLI | Agent writes to ledger files via CLI which validates domain mapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | Agent body guardrails | mitigate | Write-scope guardrails with explicit MUST NOT list in boundaries section per D-03 |
| T-55-02 | Elevation | Cross-domain agent writes | mitigate | `agentAllowedDomains` map in Go CLI enforces domain membership at runtime |
| T-55-03 | Tampering | Malicious findings JSON | mitigate | `review-ledger-write` validates JSON structure and caps at 50 entries |
| T-55-04 | Tampering | Mirror sync drift | accept | Mechanical copy + diff verification; no runtime impact from mirror drift |
</threat_model>

<verification>
1. All 7 canonical agent files have `Write` in tools frontmatter
2. No canonical agent file contains unqualified "no Write" or "no Write or Edit tools" without the scoped Write exception
3. All 7 agents-claude mirrors are byte-identical to canonical (diff returns no output)
4. All 7 opencode mirrors have `write: true`
5. All 7 codex TOML files reference `review-ledger-write`
6. Tracker specifically contains bugs domain carve-out and "except reviews/bugs/"
7. Each canonical agent contains "Write-Scope Restriction" heading in boundaries section
8. Each canonical agent contains "MUST NOT write to" in boundaries section
</verification>

<success_criteria>
- 28 agent files updated across 4 surfaces
- Each agent has Write in tools, write-scope guardrails, findings persistence CLI reference
- Tracker has bugs domain carve-out preserving diagnose-only identity
- agents-claude mirrors are byte-identical to canonical (verified by diff)
- opencode mirrors have `write: true` and matching body content
- codex TOML files reference `review-ledger-write` in developer_instructions
</success_criteria>

<output>
After completion, create `.planning/phases/55-agent-definition-updates/55-01-SUMMARY.md`
</output>
