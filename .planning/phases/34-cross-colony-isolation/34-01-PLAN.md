---
phase: 34-cross-colony-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/aether-utils.sh
  - .aether/utils/learning.sh
  - .aether/docs/command-playbooks/build-verify.md
  - .aether/docs/command-playbooks/build-wave.md
  - .aether/docs/command-playbooks/build-full.md
  - .aether/docs/command-playbooks/continue-advance.md
  - .opencode/commands/ant/continue.md
autonomous: true
requirements: [SAFE-02]
must_haves:
  truths:
    - "No code anywhere in the codebase extracts colony name by splitting session_id -- all 13 locations use colony-name subcommand or _colony_name()"
    - "Colony name extraction uses _colony_name() via the colony-name subcommand, returning a human-readable name instead of a timestamp. If the subcommand is unavailable, falls back to 'unknown' tag"
    - "Shell scripts use `bash \"$0\" colony-name` and playbooks use `bash .aether/aether-utils.sh colony-name` as the single source of truth"
  artifacts:
    - path: ".aether/utils/learning.sh"
      provides: "_learning_promote_auto and _learning_promote_review use colony-name subcommand instead of session_id splitting"
    - path: ".aether/aether-utils.sh"
      provides: "memory-capture subcommand uses colony-name subcommand instead of session_id splitting"
    - path: ".aether/docs/command-playbooks/build-verify.md"
      provides: "All bash blocks use colony-name subcommand"
    - path: ".aether/docs/command-playbooks/build-wave.md"
      provides: "All bash blocks use colony-name subcommand"
    - path: ".aether/docs/command-playbooks/build-full.md"
      provides: "All bash blocks use colony-name subcommand"
    - path: ".aether/docs/command-playbooks/continue-advance.md"
      provides: "Bash block uses colony-name subcommand"
    - path: ".opencode/commands/ant/continue.md"
      provides: "Bash block uses colony-name subcommand"
  key_links:
    - from: ".aether/utils/learning.sh"
      to: ".aether/utils/queen.sh"
      via: "colony-name subcommand dispatches to _colony_name()"
      pattern: "bash.*colony-name"
    - from: ".aether/aether-utils.sh"
      to: ".aether/utils/queen.sh"
      via: "colony-name subcommand dispatches to _colony_name()"
      pattern: "bash.*colony-name"
---

<objective>
Replace all 13 fragile session_id splitting locations with proper colony-name subcommand calls.

Purpose: The current pattern `jq -r '.session_id | split("_")[1]'` extracts a unix timestamp, not a colony name. It "works" only because the value is used as a tag, never as a lookup key. Replacing with the `colony-name` subcommand (which reads `_colony_name()` from queen.sh) returns the real human-readable colony name via the proper fallback chain (COLONY_STATE.json -> package.json -> directory basename).

Output: All 13 locations (3 shell scripts + 9 playbooks + 1 OpenCode) use the colony-name subcommand. Zero session_id splitting remains.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cross-colony-isolation/34-RESEARCH.md
@.aether/utils/learning.sh
@.aether/aether-utils.sh
@.aether/utils/queen.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace session_id splitting in shell scripts (3 locations)</name>
  <files>
    .aether/utils/learning.sh
    .aether/aether-utils.sh
  </files>
  <action>
Replace the fragile session_id splitting pattern at all 3 shell script locations with the colony-name subcommand.

**Pattern to find (all 3 use identical code):**
```bash
colony_name=$(jq -r '.session_id | split("_")[1] // "unknown"' "$DATA_DIR/COLONY_STATE.json" 2>/dev/null || echo "unknown")
```

**Replace with:**
```bash
colony_name=$(bash "$0" colony-name 2>/dev/null | jq -r '.result.name // ""') || colony_name="unknown"
[[ -z "$colony_name" ]] && colony_name="unknown"
```

**Locations:**

1. **learning.sh:378** (inside `_learning_promote_auto`): Find the `colony_name=` line within the function around line 375-380. It has a preceding comment `# MIGRATE: direct COLONY_STATE.json access`. Remove the MIGRATE comment (the migration is happening now) and replace the line.

2. **learning.sh:930** (inside `_learning_promote_review`): Find the `colony_name=` line within the function around line 927-931. Replace similarly.

3. **aether-utils.sh:3667** (inside `memory-capture` subcommand handler): Find the `colony_name=` line around line 3664-3668. It has a preceding MIGRATE comment -- remove that comment and replace the line.

After replacement, verify each location by searching for `session_id.*split` in both files -- there should be zero matches.

Also do a broader audit: `grep -rn 'session_id.*split' .aether/` to confirm no other shell scripts use this pattern. If any additional locations are found, fix them with the same pattern.
  </action>
  <verify>
Run `grep -rn 'session_id.*split' .aether/utils/learning.sh .aether/aether-utils.sh` -- should return zero matches.

Run `grep -rn 'colony-name' .aether/utils/learning.sh .aether/aether-utils.sh` -- should show the new pattern at the 3 locations.

Run `npm test` to confirm no regressions.
  </verify>
  <done>All 3 shell script locations use `bash "$0" colony-name` instead of session_id splitting. Zero `session_id | split` patterns remain in shell scripts. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace session_id splitting in playbooks and OpenCode (10 locations)</name>
  <files>
    .aether/docs/command-playbooks/build-verify.md
    .aether/docs/command-playbooks/build-wave.md
    .aether/docs/command-playbooks/build-full.md
    .aether/docs/command-playbooks/continue-advance.md
    .opencode/commands/ant/continue.md
  </files>
  <action>
Replace the session_id splitting pattern in all playbook bash blocks and the OpenCode continue command. These are markdown files containing bash code snippets that AI agents copy and execute, so the replacement must use the absolute script path (not `$0`).

**Pattern to find (identical across all 10 locations):**
```bash
colony_name=$(jq -r '.session_id | split("_")[1] // "unknown"' .aether/data/COLONY_STATE.json 2>/dev/null || echo "unknown")
```

**Replace with:**
```bash
colony_name=$(bash .aether/aether-utils.sh colony-name 2>/dev/null | jq -r '.result.name // ""')
[[ -z "$colony_name" ]] && colony_name="unknown"
```

**Locations (9 playbook + 1 OpenCode):**

1. **build-verify.md:315** -- inside bash block for "Logging resilience finding..."
2. **build-verify.md:371** -- inside bash block for "Logging verification failure..."
3. **build-wave.md:534** -- inside bash block for logging approach changes
4. **build-wave.md:597** -- inside bash block for "Logging failure to midden..."
5. **build-full.md:762** -- inside bash block for logging approach changes
6. **build-full.md:825** -- inside bash block for "Logging failure to midden..."
7. **build-full.md:1272** -- inside bash block for "Logging resilience finding..."
8. **build-full.md:1313** -- inside bash block for "Logging verification failure..."
9. **continue-advance.md:82** -- inside bash block for "Recording learning observations..."
10. **continue.md:977** (OpenCode) -- inside bash block for "Recording learning observations..."

For each location, replace only the `colony_name=` line and add the `[[ -z ... ]]` fallback line immediately after. Leave the surrounding code (phase_num extraction, etc.) unchanged.

After replacement, verify with `grep -rn 'session_id.*split' .aether/docs/command-playbooks/ .opencode/commands/ant/continue.md` -- should return zero matches.
  </action>
  <verify>
Run `grep -rn 'session_id.*split' .aether/docs/command-playbooks/ .opencode/commands/ant/` -- should return zero matches.

Run `grep -rn 'colony-name' .aether/docs/command-playbooks/ .opencode/commands/ant/continue.md` -- should show the new pattern at all 10 locations.

Run full project-wide check: `grep -rn 'session_id.*split' .aether/ .opencode/ .claude/` -- should return zero matches anywhere.
  </verify>
  <done>All 10 playbook/OpenCode locations use `bash .aether/aether-utils.sh colony-name`. Zero `session_id | split` patterns remain in the entire codebase.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'session_id.*split' .aether/ .opencode/ .claude/` -- zero matches in entire codebase
2. All 13 locations now use the colony-name subcommand
3. `npm test` -- all tests pass
</verification>

<success_criteria>
- Zero session_id splitting patterns remain anywhere in the codebase
- Colony name extraction uses _colony_name() via the colony-name subcommand at all 13 locations
- All existing tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/34-cross-colony-isolation/34-01-SUMMARY.md`
</output>
