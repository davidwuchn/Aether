---
phase: 20-visual-ux-restoration-emoji-consistency
plan: 01
status: complete
started: 2026-04-21
completed: 2026-04-21
---

# Plan 20-01: Emoji Consistency

## What Was Built

- **commandEmojiMap** in `cmd/codex_visuals.go` — centralized map of 50+ commands to their banner emoji, replacing hardcoded emoji in `renderBanner()` calls
- **commandEmoji()** helper — lookups with fallback to ant emoji
- Refactored all `renderBanner()` calls in `cmd/` to use `commandEmojiMap` instead of hardcoded emoji
- Standardized all 49 Claude and 49 OpenCode wrapper descriptions to single-emoji prefix matching runtime banner emoji
- Removed old emoji-ant-emoji sandwich pattern from wrapper descriptions
- **Tests**: `TestCommandEmojiMapCompleteness`, `TestCasteEmojiMapCompleteness`, `TestWrapperDescriptionEmojiConsistency`, `TestCommandEmojiMapNoDuplicates`

## Files Modified

- `cmd/codex_visuals.go` — commandEmojiMap, commandEmoji(), refactored renderBanner calls
- `cmd/codex_visuals_test.go` — emoji consistency tests
- `cmd/status.go` — renderBanner uses commandEmoji
- `cmd/swarm_cmd.go` — renderBanner uses commandEmoji
- `cmd/compatibility_cmds.go` — renderBanner uses commandEmoji
- `.claude/commands/ant/*.md` — 49 wrapper descriptions standardized
- `.opencode/commands/ant/*.md` — 49 wrapper descriptions standardized

## Verification

- All 2900+ tests pass
- `go vet` clean
- `grep -r "🐜" .claude/commands/ant/ | grep "description:"` — empty (no sandwich)
- Binary builds successfully
