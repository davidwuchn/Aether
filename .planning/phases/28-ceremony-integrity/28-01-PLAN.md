---
phase: 28-ceremony-integrity
plan: 01
type: execute
wave: 1
files_modified:
  - cmd/medic_ceremony.go
  - cmd/medic_ceremony_test.go
  - cmd/medic_cmd.go
  - cmd/medic_scanner.go
autonomous: true
requirements:
  - R042

must_haves:
  truths:
    - "scanCeremonyIntegrity validates emoji consistency across commandEmojiMap/casteEmojiMap and wrapper files"
    - "Verifies stage markers present in state-changing command wrappers"
    - "Verifies context-clear guidance is runtime-owned"
    - "Reports mismatches between Claude/OpenCode wrappers and runtime"
  artifacts:
    - path: "cmd/medic_ceremony.go"
      provides: "Ceremony integrity scanner"
      contains: "scanCeremonyIntegrity, extractEmojisFromMarkdown, checkStageMarkers"
    - path: "cmd/medic_ceremony_test.go"
      provides: "Ceremony integrity tests"
      contains: "TestExtractEmojis, TestCheckStageMarkers, TestCeremonyEmojiConsistency"
---

<objective>
Build the ceremony integrity scanner that verifies wrapper files match runtime expectations. This checks emoji consistency, stage marker presence, and context-clear guidance across Claude/OpenCode wrappers and the Go runtime renderer.

Purpose: Catch wrapper/runtime drift automatically — the #1 issue from v1.3.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create ceremony scanner with emoji extraction and validation</name>
  <files>cmd/medic_ceremony.go</files>
  <read_first>
    - cmd/codex_visuals.go (commandEmojiMap, casteEmojiMap, casteColorMap — the ground truth)
    - cmd/medic_wrapper.go (countFilesInDir, scanWrapperParity — file counting pattern)
    - cmd/medic_scanner.go (fileChecker, issue helpers)
    - .claude/commands/ant/build.md (example wrapper — see what emojis are in wrapper markdown)
    - .claude/commands/ant/init.md (example wrapper)
    - .opencode/commands/ant/build.md (OpenCode wrapper example)
  </read_first>
  <action>
    1. Create `cmd/medic_ceremony.go`:
       - Implement `scanCeremonyIntegrity(fc *fileChecker) []HealthIssue`:
         - Call `checkEmojiConsistency(fc)` — verify emojis in wrappers match commandEmojiMap
         - Call `checkStageMarkers(fc)` — verify state-changing commands have stage markers
         - Call `checkContextClearGuidance(fc)` — verify context-clear is runtime-owned
         - Aggregate all issues
       - Implement `checkEmojiConsistency(fc *fileChecker) []HealthIssue`:
         - For each command in commandEmojiMap, read the corresponding wrapper file
         - Extract emoji characters from the wrapper markdown using regex `[\x{1F300}-\x{1F9FF}]` or Unicode ranges
         - If the wrapper contains an emoji that doesn't match commandEmojiMap: warning "Wrapper for '{command}' uses emoji '{found}' but runtime expects '{expected}'"
         - If the wrapper has NO emoji where one is expected: info "Wrapper for '{command}' has no emoji (runtime uses '{expected}')"
         - Check both Claude and OpenCode wrappers
       - Implement `extractEmojisFromMarkdown(content string) []string`:
         - Use regex to find emoji Unicode characters in markdown content
         - Return list of unique emojis found
       - Implement `getCommandEmoji(command string) string`:
         - Look up command in commandEmojiMap, return emoji or ""
  </action>
  <acceptance_criteria>
    - `scanCeremonyIntegrity` function exists and returns `[]HealthIssue`
    - `checkEmojiConsistency` reads wrapper files and validates emojis against commandEmojiMap
    - `extractEmojisFromMarkdown` extracts emoji characters from markdown content
    - File compiles without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement stage marker and context-clear checks</name>
  <files>cmd/medic_ceremony.go</files>
  <read_first>
    - cmd/medic_ceremony.go (ceremony scanner from Task 1)
    - .aether/docs/command-playbooks/build-prep.md (stage marker examples: "── Stage Name ──")
    - .aether/docs/command-playbooks/continue-verify.md (stage markers in continue)
    - .claude/commands/ant/build.md (wrapper — look for stage marker references)
    - .claude/commands/ant/continue.md (wrapper — look for stage marker references)
    - cmd/codex_visuals.go (renderStageMarker function — what the runtime emits)
  </read_first>
  <action>
    1. Implement `checkStageMarkers(fc *fileChecker) []HealthIssue`:
       - Define state-changing commands that should have stage markers:
         ```
         stateChangingCommands = []string{"build", "continue", "init", "seal", "plan"}
         ```
       - For each state-changing command, read the Claude wrapper
       - Check if the wrapper references stage markers (pattern: `──.*──` or `renderStageMarker`)
       - If a state-changing command's wrapper has NO stage marker reference: warning "Wrapper for '{command}' has no stage markers (state-changing command should include ceremony)"
       - Also verify the YAML source exists for each command in `.aether/commands/`
    2. Implement `checkContextClearGuidance(fc *fileChecker) []HealthIssue`:
       - Read `.claude/commands/ant/continue.md` (the main context-clear guidance file)
       - Verify context-clear guidance exists and is not empty
       - Check that context-clear instructions reference the runtime (not hard-coded values)
       - If context-clear guidance references hardcoded values that should be runtime-owned: warning "Context-clear guidance in continue.md contains hardcoded values (should be runtime-owned)"
  </action>
  <acceptance_criteria>
    - `checkStageMarkers` verifies stage markers in state-changing command wrappers
    - `checkContextClearGuidance` verifies context-clear is runtime-owned
    - Both return `[]HealthIssue` with appropriate severity levels
    - File compiles, existing tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire ceremony scan into medic deep mode</name>
  <files>cmd/medic_scanner.go</files>
  <read_first>
    - cmd/medic_scanner.go (performHealthScan, existing deep scan integration)
    - cmd/medic_wrapper.go (scanWrapperParity — called when opts.Deep)
    - cmd/medic_ceremony.go (scanCeremonyIntegrity)
  </read_first>
  <action>
    1. Update `performHealthScan` in `cmd/medic_scanner.go`:
       - When `opts.Deep` is true, also call `scanCeremonyIntegrity(fc)` after `scanWrapperParity`
       - Append ceremony issues to the result
    2. This ensures `aether medic --deep` now includes ceremony integrity checks alongside wrapper parity
  </action>
  <acceptance_criteria>
    - `aether medic --deep` includes ceremony integrity checks
    - Ceremony issues appear in the deep scan output
    - All existing tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Write ceremony integrity tests</name>
  <files>cmd/medic_ceremony_test.go</files>
  <read_first>
    - cmd/medic_ceremony.go (ceremony scanner implementation)
    - cmd/medic_wrapper_test.go (test patterns for wrapper checking)
  </read_first>
  <action>
    1. Create `cmd/medic_ceremony_test.go`:
       - `TestExtractEmojisFromMarkdown` — verify emoji extraction from markdown content
       - `TestEmojiConsistencyMatching` — wrapper with correct emoji produces no issues
       - `TestEmojiConsistencyMismatch` — wrapper with wrong emoji produces warning
       - `TestEmojiConsistencyMissing` — wrapper with no emoji produces info
       - `TestCheckStageMarkersPresent` — state-changing commands with markers pass
       - `TestCheckStageMarkersMissing` — state-changing commands without markers produce warning
       - `TestCheckContextClearGuidance` — verifies context-clear check runs
       - `TestScanCeremonyIntegrityIntegration` — full ceremony scan with all checks
    2. Each test creates temp directory with fixture wrapper files
    3. Verify correct severity levels for each issue type
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run "TestExtract|TestEmoji|TestStageMarker|TestContextClear|TestScanCeremony" -v` passes
    - All existing tests pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `go test ./cmd -run "TestExtract|TestEmoji|TestStageMarker|TestContextClear|TestScanCeremony" -v` — all pass
- `go test ./cmd/...` — all pass
- `go vet ./...` — clean
- `go build ./cmd/aether` — compiles
- `aether medic --deep` — includes ceremony integrity section in output
</verification>

<success_criteria>
- Ceremony integrity scanner validates emojis, stage markers, and context-clear guidance
- Integrated into `aether medic --deep` output
- Reports mismatches between wrappers and runtime
- Tests cover all check types
</success_criteria>
