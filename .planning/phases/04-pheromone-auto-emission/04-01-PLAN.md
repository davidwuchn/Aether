---
phase: 04-pheromone-auto-emission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/docs/command-playbooks/continue-advance.md
  - .aether/docs/command-playbooks/continue-full.md
autonomous: true
requirements: [PHER-01, PHER-02, PHER-03]

must_haves:
  truths:
    - "Running /ant:continue after recording decisions emits FEEDBACK pheromones with source auto:decision and [decision] content label"
    - "Running /ant:continue when midden has 3+ failures in the same category emits REDIRECT pheromones with source auto:error and [error-pattern] content label"
    - "Running /ant:continue when success criteria text recurs across 2+ completed phases emits FEEDBACK pheromones with source auto:success and [success-pattern] content label"
    - "Auto-emitted pheromones use strength 0.6-0.7 (lower than user-emitted) and 30d TTL"
    - "Deduplication prevents repeated continue runs from emitting the same pheromone twice"
    - "Emission caps limit each type per continue run: 3 decisions, 3 error patterns, 2 success criteria"
    - "All emission blocks are SILENT -- failures never block phase advancement"
  artifacts:
    - path: ".aether/docs/command-playbooks/continue-advance.md"
      provides: "Three new auto-emission sub-steps in Step 2.1"
      contains: "auto:decision"
    - path: ".aether/docs/command-playbooks/continue-full.md"
      provides: "Mirrored auto-emission sub-steps in Step 2.1"
      contains: "auto:decision"
  key_links:
    - from: ".aether/docs/command-playbooks/continue-advance.md"
      to: ".aether/aether-utils.sh pheromone-write"
      via: "bash pheromone-write calls in Step 2.1b/c/d"
      pattern: "pheromone-write.*auto:(decision|error|success)"
    - from: ".aether/docs/command-playbooks/continue-advance.md"
      to: ".aether/aether-utils.sh midden-recent-failures"
      via: "bash midden-recent-failures call in Step 2.1c"
      pattern: "midden-recent-failures"
    - from: ".aether/docs/command-playbooks/continue-advance.md"
      to: ".aether/data/COLONY_STATE.json"
      via: "jq query for success criteria in Step 2.1d"
      pattern: "plan\\.phases.*success_criteria"
---

<objective>
Wire three auto-emission sources into continue-advance.md Step 2.1: decision-to-FEEDBACK (PHER-01), midden-error-to-REDIRECT (PHER-02), and success-criteria-to-FEEDBACK (PHER-03). Mirror changes to continue-full.md.

Purpose: The colony automatically emits pheromone signals from decisions, recurring errors, and success patterns during /ant:continue -- no manual /ant:focus or /ant:feedback needed for routine signals.
Output: Updated continue-advance.md and continue-full.md with three new emission sub-steps in Step 2.1.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pheromone-auto-emission/04-RESEARCH.md
@.aether/docs/command-playbooks/continue-advance.md
@.aether/docs/command-playbooks/continue-full.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three auto-emission blocks to continue-advance.md Step 2.1</name>
  <files>.aether/docs/command-playbooks/continue-advance.md</files>
  <action>
Modify Step 2.1 "Auto-Emit Phase Pheromones (SILENT)" to add three new sub-steps after the existing 2.1a. The existing structure has 2.1a (phase outcome FEEDBACK), 2.1b (flagged_patterns REDIRECT), and 2.1c (expire phase_end signals). The new structure should be:

- 2.1a: Keep as-is (auto-emit FEEDBACK for phase outcome)
- 2.1b: NEW -- Auto-emit FEEDBACK for phase decisions (PHER-01)
- 2.1c: REPLACE existing 2.1b -- Auto-emit REDIRECT for midden error patterns (PHER-02)
- 2.1d: NEW -- Auto-emit FEEDBACK for recurring success criteria (PHER-03)
- 2.1e: Renumber existing 2.1c -- Expire phase_end signals + archive

**Step 2.1b (PHER-01): Decision-to-FEEDBACK emission:**

Extract recent decisions from CONTEXT.md "Recent Decisions" table using awk (same pattern colony-prime uses). For each decision not already covered by an existing auto:decision or system:decision pheromone, emit a FEEDBACK with:
- Content: `[decision] {decision text}`
- Source: `"auto:decision"`
- Strength: 0.6
- TTL: 30d
- Reason: "Auto-emitted from phase decision during continue"

Deduplication: Before emitting, check pheromones.json for active signals with source "auto:decision" or "system:decision" containing the same decision text. Skip if duplicate found. Cap: max 3 decision pheromones per continue run.

Use the code pattern from the research PHER-01 example. Key details:
- Extract decisions with awk matching `/^\| [0-9]{4}-[0-9]{2}/` rows from CONTEXT.md "Recent Decisions" table
- Parse the 3rd pipe-delimited field as the decision text
- All operations use `2>/dev/null || true` for SILENT contract

**Step 2.1c (PHER-02): Midden-error-to-REDIRECT emission:**

REPLACE the existing Step 2.1b that reads `errors.flagged_patterns[]` from COLONY_STATE.json. Instead, query the actual failure store using `midden-recent-failures 50`. Group failures by `category` field using jq. For categories with 3+ occurrences, emit a REDIRECT with:
- Content: `[error-pattern] Category "{category}" recurring ({count} occurrences)`
- Source: `"auto:error"`
- Strength: 0.7
- TTL: 30d
- Reason: "Auto-emitted: midden error pattern recurred 3+ times"

Deduplication: Check pheromones.json for active signals with source "auto:error" containing the category. Cap: max 3 error pattern pheromones per continue run.

Use the code pattern from the research PHER-02 example. Key details:
- Use `midden-recent-failures 50` subcommand (NOT errors.flagged_patterns)
- Group by `.category` using jq `group_by(.)`
- Threshold is 3+ (not 2+ as in the old code)
- Use `@base64` encoding for safe iteration over jq output
- Keep the memory-capture resolution call from the old 2.1b for patterns that fire

**Step 2.1d (PHER-03): Success-criteria-to-FEEDBACK emission:**

Extract success criteria from all completed phases in COLONY_STATE.json. Compare text (normalized: lowercase, trimmed) across phases. For criteria appearing in 2+ completed phases, emit a FEEDBACK with:
- Content: `[success-pattern] "{criteria text}" recurs across phases {phase list}`
- Source: `"auto:success"`
- Strength: 0.6
- TTL: 30d
- Reason: "Auto-emitted: success criteria pattern recurred across {count} phases"

Deduplication: Check pheromones.json for active signals with source "auto:success" containing the criteria text. Cap: max 2 success criteria pheromones per continue run (the jq query should use `.[:2]` to limit output).

Use the code pattern from the research PHER-03 example. Key details:
- Extract from `.plan.phases[] | select(.status == "completed")`
- Include both phase-level `.success_criteria` and task-level `.tasks[].success_criteria`
- Normalize with `ascii_downcase` and whitespace trimming
- Use `group_by(.text)` and `select(length >= 2)` for recurrence detection
- Use `@base64` encoding for safe iteration

**Step 2.1e: Renumber existing 2.1c to 2.1e** (expire phase_end signals + archive). Keep content identical, just change the heading number.

IMPORTANT: All three new blocks follow the SILENT contract -- every pheromone operation uses `2>/dev/null || true`. Failures must never block phase advancement.
  </action>
  <verify>
Read the modified continue-advance.md and confirm:
1. Step 2.1 has sub-steps 2.1a through 2.1e
2. Step 2.1b contains `auto:decision` source and `[decision]` content label
3. Step 2.1c contains `midden-recent-failures` call and `auto:error` source with 3+ threshold
4. Step 2.1d contains success_criteria jq query and `auto:success` source
5. Step 2.1e is the renamed expire step (unchanged content from old 2.1c)
6. All emission blocks use `2>/dev/null || true`
7. Each block has deduplication checks and emission caps
  </verify>
  <done>
continue-advance.md Step 2.1 has five sub-steps: phase outcome (2.1a), decision FEEDBACK (2.1b), midden error REDIRECT (2.1c), success criteria FEEDBACK (2.1d), and expire signals (2.1e). All auto-emitted pheromones use the auto: source prefix and [type] content labels. Deduplication and caps are enforced. SILENT contract maintained.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror Step 2.1 changes to continue-full.md</name>
  <files>.aether/docs/command-playbooks/continue-full.md</files>
  <action>
Mirror the exact same Step 2.1 changes from continue-advance.md into continue-full.md. The continue-full.md has an identical Step 2.1 section structure (2.1a, 2.1b, 2.1c) that needs the same transformation.

Find the "Step 2.1: Auto-Emit Phase Pheromones (SILENT)" section in continue-full.md and apply the identical changes:
- Keep 2.1a as-is
- Add new 2.1b (decision FEEDBACK, PHER-01)
- Replace old 2.1b with new 2.1c (midden error REDIRECT, PHER-02)
- Add new 2.1d (success criteria FEEDBACK, PHER-03)
- Renumber old 2.1c to 2.1e (expire phase_end signals)

The content should be byte-identical to the continue-advance.md changes. Copy the exact blocks from Task 1.

Also update the Step 2.5 cross-reference note in continue-full.md if it mentions "Step 2.1a" or "Step 2.1b" to reflect the new numbering. Check for any internal references to the old sub-step numbers (2.1b, 2.1c) and update them to the new numbering (2.1c and 2.1e respectively).
  </action>
  <verify>
Read the modified continue-full.md and confirm:
1. Step 2.1 has sub-steps 2.1a through 2.1e (matching continue-advance.md)
2. The three new emission blocks (2.1b, 2.1c, 2.1d) match continue-advance.md exactly
3. Internal references to old step numbers are updated
4. No content before or after Step 2.1 was accidentally modified
  </verify>
  <done>
continue-full.md Step 2.1 mirrors continue-advance.md exactly: five sub-steps with decision FEEDBACK, midden error REDIRECT, and success criteria FEEDBACK emission blocks. All internal references updated to new step numbering.
  </done>
</task>

</tasks>

<verification>
1. Both continue-advance.md and continue-full.md have matching Step 2.1 sections with 5 sub-steps
2. All three auto: source prefixes appear: auto:decision, auto:error, auto:success
3. All three content labels appear: [decision], [error-pattern], [success-pattern]
4. PHER-02 uses midden-recent-failures (not errors.flagged_patterns) with 3+ threshold
5. Deduplication checks exist in all three new blocks
6. Emission caps: 3 decisions, 3 errors, 2 success criteria
7. All operations use 2>/dev/null || true (SILENT contract)
</verification>

<success_criteria>
- continue-advance.md Step 2.1 has three new auto-emission blocks (2.1b, 2.1c, 2.1d) implementing PHER-01, PHER-02, PHER-03
- continue-full.md mirrors the changes exactly
- All auto-emitted pheromones use auto: source prefix and [type] content labels for distinguishability
- Deduplication prevents repeated emissions; caps limit flooding
- Existing steps (2.1a phase outcome, 2.1e expire) are preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-pheromone-auto-emission/04-01-SUMMARY.md`
</output>
