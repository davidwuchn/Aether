---
phase: 31-init-md-smart-init-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/init.md
  - .opencode/commands/ant/init.md
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03]

must_haves:
  truths:
    - "Running /ant:init scans the repo and displays a structured approval prompt with Charter, Context, and Pheromones sections"
    - "No colony files are created or modified before the user approves the prompt"
    - "User can edit any section of the prompt and approve in a single interaction"
    - "After approval, charter content is written to QUEEN.md via charter-write (no new ## headers)"
    - "Running /ant:init on an existing colony enters re-init mode, pre-populates existing charter, and only updates charter content"
    - "Re-init preserves all existing colony state, wisdom, instincts, learnings, pheromones, and phase progress"
  artifacts:
    - path: ".claude/commands/ant/init.md"
      provides: "Smart init flow with scan-assemble-approve-create steps"
      min_lines: 200
    - path: ".opencode/commands/ant/init.md"
      provides: "OpenCode mirror with identical flow structure"
      min_lines: 200
  key_links:
    - from: ".claude/commands/ant/init.md"
      to: "aether-utils.sh init-research"
      via: "Bash tool call in scan step"
      pattern: "init-research"
    - from: ".claude/commands/ant/init.md"
      to: "aether-utils.sh charter-write"
      via: "Bash tool call in post-approval step"
      pattern: "charter-write"
    - from: ".claude/commands/ant/init.md"
      to: ".aether/data/COLONY_STATE.json"
      via: "Read tool check for re-init detection"
      pattern: "COLONY_STATE.json"
---

<objective>
Rewrite `/ant:init` from direct colony-state writer to a two-phase flow: scan the repo, generate a structured approval prompt, display it for user review, then create colony files only after approval. Update the OpenCode mirror to maintain structural parity.

Purpose: Users should see and approve the colony charter before any files are created. Re-init should update charter content without resetting state.

Output: Rewritten `.claude/commands/ant/init.md` and `.opencode/commands/ant/init.md` with scan-assemble-approve-create flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Phase 29 provides the scan module
@.aether/utils/scan.sh

# Phase 30 provides charter-write
@.aether/utils/queen.sh

# Current init.md (being rewritten)
@.claude/commands/ant/init.md
@.opencode/commands/ant/init.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite .claude/commands/ant/init.md with smart init flow</name>
  <files>.claude/commands/ant/init.md</files>
  <action>
Rewrite `.claude/commands/ant/init.md` with the following structure. The file currently has 388 lines with Steps 1-8 that write colony state immediately. Restructure into a scan-assemble-approve-create flow where ALL file writes happen ONLY after user approval.

**Keep unchanged from current init.md:**
- Frontmatter (name, description)
- Step 1: Validate Input (empty args check, usage text)
- Step 1.5: Verify Aether Setup (aether-utils.sh existence check)

**Remove entirely:**
- Step 2.6: Load Prior Colony Knowledge (deferred to Phase 32 INTEL-01)
- The old Step 2 re-init warning that says "state will be reset" (new flow preserves state)

**New flow (replace Steps 2-8):**

**Step 2: Initialize QUEEN.md**
Run `bash .aether/aether-utils.sh queen-init`. Display result (created/already_exists). Non-blocking, proceed regardless.

**Step 3: Scan Repository**
Run the scan via Bash tool:
```bash
scan_result=$(bash .aether/aether-utils.sh init-research 2>/dev/null)
scan_data=$(echo "$scan_result" | jq '.result')
```
Extract fields with jq defaults for missing data:
- `tech_langs`: `.tech_stack.languages | if length > 0 then join(", ") else "not detected" end`
- `tech_fwks`: `.tech_stack.frameworks | if length > 0 then join(", ") else "none" end`
- `tech_pkg`: `.tech_stack.package_managers | join(", ")`
- `complexity`: `.complexity.size`
- `file_count`: `.complexity.metrics.file_count`
- `top_dirs`: `.directory_structure.top_level_dirs | if . and length > 0 then join(", ") else "flat" end`
- `commit_count`: `.git_history.commit_count // "unknown"`
- `is_git`: `.git_history.is_git_repo // false`
- `survey_suggestion`: `.survey_status.suggestion.reason // empty`
- `has_active`: `.prior_colonies.has_active_colony // false`
- `active_goal`: `.prior_colonies.active_goal // empty`

If `scan_result` is empty or `jq` fails, set all fields to fallback values and proceed (graceful degradation -- never stop init because scan fails).

**Step 4: Detect Re-Init Mode**
Use Read tool to check `.aether/data/COLONY_STATE.json`.
- If file exists AND has a non-null `goal` field: set `reinit_mode = true`, store `existing_goal`
- Otherwise: set `reinit_mode = false`

If re-init mode, read existing charter entries from `.aether/QUEEN.md`:
```bash
existing_intent=$(grep '\[charter\] \*\*Intent\*\*:' .aether/QUEEN.md 2>/dev/null | sed 's/.*\*\*Intent\*\*: //' | sed 's/ (Colony:.*//' || true)
existing_vision=$(grep '\[charter\] \*\*Vision\*\*:' .aether/QUEEN.md 2>/dev/null | sed 's/.*\*\*Vision\*\*: //' | sed 's/ (Colony:.*//' || true)
existing_governance=$(grep '\[charter\] \*\*Governance\*\*:' .aether/QUEEN.md 2>/dev/null | sed 's/.*\*\*Governance\*\*: //' | sed 's/ (Colony:.*//' || true)
existing_goals=$(grep '\[charter\] \*\*Goal\*\*:' .aether/QUEEN.md 2>/dev/null | sed 's/.*\*\*Goal\*\*: //' | sed 's/ (Colony:.*//' || true)
```
Strip `(Colony: ...)` suffixes using sed. If grep finds nothing, variables remain empty.

**Step 5: Assemble and Display Approval Prompt**

Display a brief header:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   A E T H E R   C O L O N Y   I N I T
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

If re-init mode, display:
```
Re-init mode detected (existing goal: "{existing_goal}")
Charter will be updated. All colony state, wisdom, instincts, and progress will be preserved.
```

Then display the approval prompt as formatted Markdown with three sections:

**Section 1: Charter**
```markdown
## Charter

**Intent:** {user's goal from $ARGUMENTS, or existing_intent if re-init}
**Vision:** {derived from user's goal by Claude, or existing_vision if re-init}
**Governance:** {blank for fresh init, or existing_governance if re-init}
**Goals:** {blank for fresh init, or existing_goals if re-init}
```

For fresh init, Claude should derive a brief Vision from the user's goal (1-2 sentences). Governance and Goals start blank. The user fills them in if desired.

For re-init, pre-populate all fields from existing QUEEN.md charter entries.

**Section 2: Context**
```markdown
## Context

**Tech Stack:** {tech_langs} | {tech_fwks} | {tech_pkg}
**Project Size:** {complexity} ({file_count} files)
**Structure:** {top_dirs}
**Git:** {commit_count} commits
{if survey_suggestion: **Note:** {survey_suggestion}}
```

**Section 3: Pheromones**
```markdown
## Pheromones

No pheromone suggestions yet — use /ant:focus and /ant:redirect to guide the colony.
```

End with clear instructions:
```
──────────────────────────────────────────────────
Review the prompt above. You can:
  - Edit any section (just describe your changes)
  - Say "approve" or "looks good" to proceed
  - Say "cancel" to abort

If you don't respond, the colony will not be initialized.
──────────────────────────────────────────────────
```

**STOP HERE.** Wait for the user's response. Do NOT proceed to Step 6 until the user responds.

**Step 6: Handle User Response**

Parse the user's response:
- If the user approves (says "approve", "looks good", "yes", "ok", or similar): proceed to Step 7
- If the user provides edits: apply the edits to the relevant section(s), re-display the full prompt, increment a revision counter, and wait again
- If the user cancels: display "Colony initialization cancelled." and stop
- Max 2 revision rounds. After 2 rejections/edits, display: "Maximum revisions reached. Approve current version, or cancel init?" and wait for final decision

When applying edits, Claude updates the section content in memory (not files) and re-displays the full prompt. Each re-display includes a revision counter: "(Revision {N}/2)"

**Step 7: Create Colony (Post-Approval)**

Only reached after user approval. ALL file writes happen here.

**If re-init mode:**
1. Write charter content via:
```bash
bash .aether/aether-utils.sh charter-write --intent "{approved_intent}" --vision "{approved_vision}" --governance "{approved_governance}" --goals "{approved_goals}"
```
2. Optionally update the goal field in COLONY_STATE.json in-place:
```bash
jq --arg new_goal "{approved_intent}" '.goal = $new_goal' .aether/data/COLONY_STATE.json > .aether/data/COLONY_STATE.json.tmp && mv .aether/data/COLONY_STATE.json.tmp .aether/data/COLONY_STATE.json
```
3. Run `bash .aether/aether-utils.sh session-init "$(jq -r '.session_id' .aether/data/COLONY_STATE.json)" "{approved_intent}"`
4. Skip to Step 8 (display result). Do NOT write COLONY_STATE.json from template, do NOT write constraints.json, do NOT write pheromones.json.

**If fresh init:**
1. Initialize QUEEN.md (already done in Step 2)
2. Write charter content via charter-write (same command as above)
3. Write COLONY_STATE.json from template (same logic as current Step 3 -- resolve template, replace placeholders, write)
4. Write constraints.json from template (same logic as current Step 4)
5. Initialize runtime files from templates (same loop as current Step 4.5 -- pheromones, midden, learning-observations)
6. Run `bash .aether/aether-utils.sh context-update init "{approved_intent}"`
7. Run `bash .aether/aether-utils.sh validate-state colony`
8. Register repo (same as current Step 6.6 -- registry-add, version.json copy, both silent on failure)
9. Seed QUEEN.md from hive (same as current Step 6.7 -- queen-seed-from-hive)
10. Run `bash .aether/aether-utils.sh session-init "{session_id}" "{approved_intent}"`

**Step 8: Display Result**

Display the success header and result block (same visual format as current Step 7), adapted for the new flow:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   A E T H E R   C O L O N Y
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Queen has set the colony's intention

   "{approved_intent}"

   Colony Status: READY

{If re-init: "   Mode: Re-init (charter updated, state preserved)"}
{If fresh: include hive seed count if > 0, nestmate count if > 0}

State persisted — safe to /clear, then run /ant:plan

──────────────────────────────────────────────────
   Next Up
──────────────────────────────────────────────────
   /ant:plan                 Generate execution plan
   /ant:status               Check colony state
   /ant:focus                Set initial focus
```

**Preserve from current init.md (keep as-is):**
- `<failure_modes>` section (adapt Colony State Overwrite to reference re-init mode)
- `<success_criteria>` section (adapt for new flow)
- `<read_only>` section (unchanged)
</action>
  <verify>
1. Read the rewritten `.claude/commands/ant/init.md` and verify:
   - Steps 1 and 1.5 are unchanged (input validation, setup check)
   - Step 2 is queen-init (non-blocking)
   - Step 3 runs init-research and extracts scan data with jq
   - Step 4 detects re-init by reading COLONY_STATE.json
   - Step 5 displays three-section approval prompt (Charter, Context, Pheromones)
   - Step 5 ends with "STOP HERE" instruction (no file writes before approval)
   - Step 6 handles approve/edit/cancel with max 2 revision rounds
   - Step 7 has conditional logic: re-init (charter-write only) vs fresh (full template initialization)
   - Step 8 displays result
   - NO Write tool calls appear before Step 7
2. Run `npm run lint:sync` and verify no sync errors
3. Run `npm test` to verify no regressions from any test changes
  </verify>
  <done>
The `.claude/commands/ant/init.md` file follows a scan-assemble-approve-create flow where no colony files are written before user approval, re-init preserves all state, and charter content is written via charter-write after approval.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update OpenCode init.md mirror and verify sync</name>
  <files>.opencode/commands/ant/init.md</files>
  <action>
Update `.opencode/commands/ant/init.md` to mirror the new smart init flow from Task 1.

**Key differences from Claude version:**
- Include Step -1: Normalize Arguments (existing in current OpenCode version):
  ```
  Run: normalized_args=$(bash .aether/aether-utils.sh normalize-args "$@")
  The user's goal is: $normalized_args
  ```
- Use `$normalized_args` instead of `$ARGUMENTS` throughout
- Keep the OpenCode visual mode structure (Step 0 references)
- All other steps (scan, assembly, approval, create) must be structurally identical to Claude version

**The OpenCode version should:**
- Have the same step numbering and flow structure as Claude version
- Have identical Step 3 (scan), Step 4 (re-init detection), Step 5 (approval prompt format)
- Have identical Step 6 (approval handling with max 2 revisions)
- Have identical Step 7 (create colony, conditional re-init vs fresh)
- Have identical Step 8 (display result)

After updating, run `npm run lint:sync` to verify both init.md files pass sync checks.
</action>
  <verify>
1. Run `npm run lint:sync` and confirm init.md passes (no differences beyond expected arg handling)
2. Read both files and verify step structure matches (same step numbers, same flow)
3. Verify OpenCode version uses `$normalized_args` consistently
  </verify>
  <done>
`.opencode/commands/ant/init.md` mirrors the Claude version with structural parity. `npm run lint:sync` passes.
  </done>
</task>

</tasks>

<verification>
1. Read both init.md files end-to-end and verify no Write tool calls appear before the approval step
2. Verify re-init mode in Step 7 skips COLONY_STATE.json template write, constraints write, and runtime file init
3. Verify fresh init mode in Step 7 includes all initialization (template writes, constraints, runtime files, registration, session)
4. Run `npm run lint:sync` -- must pass
5. Run `npm test` -- must pass (no regressions)
</verification>

<success_criteria>
- `/ant:init "goal"` scans the repo, displays a 3-section approval prompt, and waits for user response
- No colony files are created before approval
- After approval, charter is written via `charter-write` to QUEEN.md existing sections
- Re-init detects existing colony, pre-populates charter, updates charter only
- Both Claude and OpenCode init.md files pass `npm run lint:sync`
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/31-init-md-smart-init-rewrite/31-01-SUMMARY.md`
</output>
