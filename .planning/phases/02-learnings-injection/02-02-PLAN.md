---
phase: 02-learnings-injection
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/integration/learnings-injection.test.js
autonomous: true
requirements:
  - LEARN-01
  - LEARN-04

must_haves:
  truths:
    - "Integration tests prove validated learnings reach builder prompts"
    - "Integration tests prove hypothesis and disproven learnings are excluded"
    - "Integration tests prove inherited learnings are included"
    - "Integration tests prove empty phases produce no section"
    - "Integration tests prove compact mode caps at 5 claims"
    - "Integration tests prove log_line includes learning count"
  artifacts:
    - path: "tests/integration/learnings-injection.test.js"
      provides: "End-to-end learnings injection regression tests"
      min_lines: 200
  key_links:
    - from: "tests/integration/learnings-injection.test.js"
      to: ".aether/aether-utils.sh (colony-prime)"
      via: "runAetherUtil(tmpDir, 'colony-prime')"
      pattern: "colony-prime"
    - from: "tests/integration/learnings-injection.test.js"
      to: "COLONY_STATE.json"
      via: "setupTestColony with phaseLearnings option"
      pattern: "phase_learnings"
---

<objective>
Add integration tests that verify the learnings injection pipeline end-to-end: validated claims from previous phases appear in colony-prime output, non-validated claims are excluded, edge cases are handled.

Purpose: Regression protection for the Phase 2 wiring. Proves LEARN-01 and LEARN-04 work together correctly.
Output: tests/integration/learnings-injection.test.js with 6-8 integration tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-learnings-injection/02-RESEARCH.md
@.planning/phases/02-learnings-injection/02-01-SUMMARY.md
@tests/integration/instinct-pipeline.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create learnings injection integration tests</name>
  <files>tests/integration/learnings-injection.test.js</files>
  <action>
Create tests/integration/learnings-injection.test.js following the exact same pattern as tests/integration/instinct-pipeline.test.js (same helpers: createTempDir, cleanupTempDir, runAetherUtil, setupTestColony).

Extend the setupTestColony helper to accept a phaseLearnings option and a currentPhase option:
- phaseLearnings: array of phase_learning entries (with id, phase, phase_name, learnings array, timestamp)
- currentPhase: number (default 1) -- sets current_phase in COLONY_STATE.json

Write these test cases (all test.serial):

1. **colony-prime includes validated learnings from previous phases** -- Set up colony at phase 3 with validated learnings from phases 1 and 2 plus a hypothesis claim. Verify: validated claims appear in prompt_section, hypothesis claim does NOT appear, "PHASE LEARNINGS" header present.

2. **colony-prime excludes learnings from current and future phases** -- Set up colony at phase 2 with learnings from phases 1, 2, and 3. Verify: only phase 1 claims appear (phase 2 is current, phase 3 is future).

3. **colony-prime includes inherited learnings** -- Set up colony at phase 2 with a validated inherited learning (phase="inherited") and a validated phase 1 learning. Verify: both appear in prompt_section, "Inherited" group label present.

4. **colony-prime omits section when no validated learnings exist** -- Set up colony at phase 2 with only hypothesis learnings from phase 1. Verify: "PHASE LEARNINGS" does NOT appear in prompt_section.

5. **colony-prime omits section when no previous phases have learnings** -- Set up colony at phase 1 (no previous phases). Verify: "PHASE LEARNINGS" does NOT appear.

6. **colony-prime respects compact mode cap** -- Set up colony at phase 3 with 10 validated claims spread across phases 1 and 2. Run colony-prime --compact. Verify: prompt_section contains at most 5 claims (count occurrences of "  - " within the PHASE LEARNINGS section).

7. **colony-prime log_line includes learning count** -- Set up colony at phase 2 with 2 validated learnings from phase 1. Verify: log_line contains "learnings".

8. **colony-prime formats learnings as actionable text grouped by phase** -- Set up colony at phase 3 with validated learnings from phases 1 and 2. Verify: output contains "Phase 1" group header and "Phase 2" group header, claims are indented with "  - " prefix.

Each test follows the pattern: create temp dir -> setupTestColony with options -> runAetherUtil -> parse JSON -> assert on prompt_section and/or log_line -> cleanup.

Use IEEE 754-safe comparisons where needed (lesson from Phase 1). Follow exact ava serial pattern from instinct-pipeline.test.js.
  </action>
  <verify>Run: npx ava tests/integration/learnings-injection.test.js --timeout=60s -- all 8 tests pass.</verify>
  <done>
8 integration tests pass covering: validated inclusion, hypothesis exclusion, inherited inclusion, empty handling (no validated, no previous phases), compact mode cap, log_line count, and phase-grouped formatting. All tests in tests/integration/learnings-injection.test.js.
  </done>
</task>

</tasks>

<verification>
1. npx ava tests/integration/learnings-injection.test.js --timeout=60s -- all tests pass
2. npx ava tests/integration/instinct-pipeline.test.js --timeout=60s -- existing tests still pass (no regressions)
3. npm test -- full test suite passes
</verification>

<success_criteria>
- tests/integration/learnings-injection.test.js exists with 8 test cases
- All 8 tests pass
- Existing instinct-pipeline tests still pass
- Tests cover: validated inclusion, hypothesis exclusion, inherited handling, empty phases, compact cap, log_line count, phase grouping
</success_criteria>

<output>
After completion, create `.planning/phases/02-learnings-injection/02-02-SUMMARY.md`
</output>
