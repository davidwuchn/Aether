---
phase: 07-repository-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .npmignore
autonomous: true
requirements:
  - HYGN-01
  - HYGN-05

must_haves:
  truths:
    - "Running git ls-files | grep '\\.sh$' returns zero results"
    - "Running ls on the repo root shows no clutter files (1, echo, git, AetherAntColony, aether.exe)"
    - "No duplicate image files remain in the repo root"
    - "No GSD artifact directories or stale structural files remain tracked"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion rules for GSD artifacts and stale directories"
      contains: "2026-*"
    - path: ".npmignore"
      provides: "Exclusion rules preventing dead files from being published"
  key_links:
    - from: ".gitignore"
      to: "git tracking"
      via: "pattern exclusion"
      pattern: "2026-"
---

<objective>
Remove all dead shell scripts, root-level clutter files, and structural artifacts from git tracking.

Purpose: The v4-to-v5 migration left 58 tracked shell scripts (all deprecated, all replaced by Go code), root-level clutter (empty files, test artifacts, stale binaries, duplicate images), and structural artifacts (GSD research directories, stale planning files, empty runtime/ directory). These confuse new contributors and bloat the npm package.

Output: A clean repo root with no legacy clutter and zero tracked shell scripts in active code paths.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-repository-hygiene/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all 58 tracked shell scripts from git</name>
  <files>.aether/utils/*, .aether/exchange/*.sh, .aether/scripts/*.sh, .aether/aether-utils.sh</files>
  <read_first>
    .aether/aether-utils.sh (verify it has DEPRECATED header)
    cmd/install_cmd.go (lines 198-199 -- chmod on .sh files, confirm this is harmless no-op after removal)
    cmd/setup_cmd.go (lines 206-207 -- same pattern)
    cmd/update_cmd.go (line 244 -- same pattern)
  </read_first>
  <acceptance_criteria>
    - `git ls-files | grep '\.sh$' | wc -l` returns 0
    - `go test ./cmd/... -count=1` exits 0 (install tests still pass because chmod code gracefully handles no .sh files)
    - `go test ./... -count=1` exits 0
  </acceptance_criteria>
  <action>
Remove all 58 tracked shell scripts using `git rm`:

1. Remove the 52 files in `.aether/utils/` (all are shell scripts with DEPRECATED headers):
   `git rm .aether/utils/*.sh` and `git rm .aether/utils/curation-ants/*.sh`

2. Remove the 3 shell scripts in `.aether/exchange/`:
   `git rm .aether/exchange/pheromone-xml.sh .aether/exchange/registry-xml.sh .aether/exchange/wisdom-xml.sh`

3. Remove the 2 shell scripts in `.aether/scripts/`:
   `git rm .aether/scripts/incident-test-add.sh .aether/scripts/weekly-audit.sh`

4. Remove the monolith:
   `git rm .aether/aether-utils.sh`

5. Verify: `git ls-files | grep '\.sh$'` returns nothing.

6. Run `go test ./cmd/... -count=1` to confirm install/update/setup tests still pass. The chmod code in install_cmd.go:198-199 and setup_cmd.go:206-207 iterates over copied files and makes .sh files executable -- when no .sh files exist, the loop body simply never triggers for that extension, so it is harmless.

7. Run `go test ./... -count=1` for full suite verification.

Do NOT remove `cmd/completion.go` lines 23-25 (legitimate bash completion references, not shell scripts).
  </action>
  <verify>
    <automated>git ls-files | grep '\.sh$' | wc -l && go test ./... -count=1</automated>
  </verify>
  <done>All 58 shell scripts removed from git tracking. Zero .sh files in git ls-files output. All Go tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove root clutter, structural artifacts, and stale directories</name>
  <files>.gitignore, .npmignore, repo-structure.md, TO-DOS.md, runtime/, 2026-04-05-210945/, docs/plans/</files>
  <read_first>
    README.md (check which root images are actually referenced -- look for AetherBanner.png, AetherLogo.png references)
    .gitignore (current exclusion patterns)
    .npmignore (current exclusion patterns)
    repo-structure.md (confirm stale references to bin/ and tests/)
    TO-DOS.md (confirm stale content from Feb 2026)
  </read_first>
  <acceptance_criteria>
    - `git ls-files | grep -E '^(1|echo|git|AetherAntColony|aether\.exe)$'` returns nothing
    - `git ls-files | grep 'AetherArtork'` returns nothing (typo-named file removed)
    - `git ls-files | grep -c 'AetherAnts\|AtherAnts'` returns exactly 1 (one canonical copy kept)
    - `git ls-files | grep '2026-04-05-210945/'` returns nothing
    - `git ls-files | grep 'runtime/'` returns nothing
    - `git ls-files | grep 'docs/plans/'` returns nothing
    - `git ls-files | grep -E '^(repo-structure\.md|TO-DOS\.md)$'` returns nothing
    - `go test ./... -count=1` exits 0
    - `.gitignore` contains pattern `2026-*/` for GSD artifact exclusion
  </acceptance_criteria>
  <action>
Remove root-level clutter, structural artifacts, and stale files from git tracking.

**Root clutter removal (git rm):**
- `git rm 1` (empty file, 0 bytes)
- `git rm echo` (test artifact, 64 bytes)
- `git rm git` (test artifact, 2476 bytes)
- `git rm AetherAntColony` (old binary, 2.6MB)
- `git rm aether.exe` (Windows build artifact, 8MB)
- `git rm AetherArtork.png` (typo filename, 1.2MB)
- For duplicate images: keep `AetherAnts.png` (correct spelling), remove `AtherAnts.png` (typo):
  `git rm AtherAnts.png`

**Keep these root images (used by README.md):**
- `AetherBanner.png` -- referenced by README.md banner
- `AetherLogo.png` -- brand asset
- `AetherAnts.png` -- canonical copy (correct spelling)

**Stale structural files (git rm):**
- `git rm repo-structure.md` (references non-existent bin/ and tests/ directories)
- `git rm TO-DOS.md` (stale tasks from Feb 2026)
- `git rm runtime/.gitkeep` then `git rm -r runtime/` if empty (directory with only .gitkeep)

**GSD artifact directory (git rm):**
- `git rm -r 2026-04-05-210945/` (5 files, GSD research artifact)

**Old planning documents (git rm):**
- `git rm -r docs/plans/` (7 files from Feb-Mar 2026, all historical, preserved in git history)

**Update .gitignore:**
Add these patterns to prevent future GSD artifacts from being tracked:
```
# GSD research artifacts
2026-*/
20??-??-??-??????/
```

**Verify:**
Run `go test ./... -count=1` to confirm nothing broke.
  </action>
  <verify>
    <automated>git ls-files | grep -E '^(1|echo|git|AetherAntColony|aether\.exe|AetherArtork|AtherAnts|2026-04-05-210945/|runtime/|docs/plans/|repo-structure|TO-DOS)' | wc -l && go test ./... -count=1</automated>
  </verify>
  <done>Root directory is clean -- no clutter files, no test artifacts, no stale binaries, no duplicate images. Stale structural files and GSD artifacts removed. .gitignore updated to prevent GSD artifact tracking. All Go tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo -> npm package | Files removed from git must also not appear in npm publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | Removed files may contain secrets | mitigate | Before removing, spot-check removed files for embedded tokens/keys. These are all shell scripts and test artifacts -- low risk. |
| T-07-01-02 | Denial of Service | npm consumers get broken package after removal | accept | Shell scripts are already deprecated and non-functional. npm consumers already cannot use them. Removal is cleanup, not a breaking change. |
</threat_model>

<verification>
1. `git ls-files | grep '\.sh$' | wc -l` returns 0
2. `git ls-files | grep -E '^(1|echo|git|AetherAntColony|aether\.exe|AetherArtork|AtherAnts|2026-04-05-210945/|runtime/|docs/plans/)' | wc -l` returns 0
3. `go test ./... -count=1` exits 0
4. `.gitignore` contains `2026-*/` pattern
</verification>

<success_criteria>
- Zero tracked shell scripts in the repository
- Root directory contains only expected project files (Go source, images used by README, config files)
- No GSD artifacts or stale structural files tracked
- All 524+ Go tests continue passing
- .gitignore updated to prevent future GSD artifact tracking
</success_criteria>

<output>
After completion, create `.planning/phases/07-repository-hygiene/07-01-SUMMARY.md`
</output>
