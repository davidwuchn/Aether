---
phase: 07-repository-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/agents-claude/*.md
  - .npmignore
autonomous: true
requirements:
  - HYGN-01
  - HYGN-03

must_haves:
  truths:
    - "Agent mirror .aether/agents-claude/ is byte-identical to canonical .claude/agents/ant/"
    - "Legacy .aether/agents/ directory no longer exists in git tracking"
    - "npm pack --dry-run does not include old agent definitions from .aether/agents/"
  artifacts:
    - path: ".aether/agents-claude/"
      provides: "Byte-identical mirror of canonical .claude/agents/ant/"
      min_lines: 24
    - path: ".npmignore"
      provides: "Exclusion of .aether/agents/ from npm package"
      contains: ".aether/agents/"
  key_links:
    - from: ".claude/agents/ant/"
      to: ".aether/agents-claude/"
      via: "file copy (mirror sync)"
      pattern: "diff -q .claude/agents/ant/ .aether/agents-claude/"
---

<objective>
Fix agent mirror parity and remove the legacy .aether/agents/ directory.

Purpose: The canonical agent definitions live in `.claude/agents/ant/` (24 files). The mirror `.aether/agents-claude/` has 7 of 25 files that differ from canonical, plus an extra `seal.md` file. The legacy `.aether/agents/` directory (24 files) contains completely outdated definitions and is published via npm, creating distribution inconsistency. The CLAUDE.md documents that `.aether/agents-claude/` must be byte-identical to the canonical source.

Output: Agent mirrors in sync, legacy agent directory removed, npm package no longer ships old definitions.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-repository-hygiene/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize agent mirror and remove legacy agent directory</name>
  <files>.aether/agents-claude/*.md, .aether/agents/*, .npmignore</files>
  <read_first>
    .claude/agents/ant/aether-ambassador.md (canonical -- verify it is the source of truth)
    .aether/agents-claude/aether-ambassador.md (mirror -- one of 7 that differs)
    .aether/agents-claude/seal.md (extra file not in canonical -- check if needed by any code)
    cmd/install_cmd.go (line 94 -- copies .claude/agents/ant/ to ~/.claude/agents/ant/)
    cmd/setup_cmd.go (line 92 -- copies agents-claude from ../.claude/agents/ant)
    cmd/update_cmd.go (line 167 -- copies agents-claude from ../.claude/agents/ant)
  </read_first>
  <acceptance_criteria>
    - `diff -q .claude/agents/ant/ .aether/agents-claude/ 2>&1 | grep -v seal.md` returns nothing (byte-identical ignoring seal.md)
    - `git ls-files | grep '\.aether/agents/[^-]' | wc -l` returns 0 (legacy directory removed from tracking)
    - `.npmignore` contains `.aether/agents/` line
    - `npm pack --dry-run 2>&1 | grep '.aether/agents/'` returns nothing or only shows `.aether/agents-claude/`
    - `go test ./... -count=1` exits 0
  </acceptance_criteria>
  <action>
Synchronize the agent mirror and remove the legacy agent directory.

**Step 1: Sync mirror from canonical source**
Copy all 24 canonical agent files from `.claude/agents/ant/` to `.aether/agents-claude/`, overwriting the 7 that differ:
```bash
cp .claude/agents/ant/*.md .aether/agents-claude/
```
This overwrites the 7 differing files (aether-ambassador.md, aether-builder.md, aether-keeper.md, aether-oracle.md, aether-queen.md, aether-route-setter.md, aether-tracker.md) with canonical versions.

The extra `seal.md` file in `.aether/agents-claude/` (not present in canonical) should be investigated:
- If `grep -rn 'seal.md' cmd/*.go pkg/**/*.go` returns results, it is referenced and must be kept
- If no references found, remove it: `git rm .aether/agents-claude/seal.md`
- Based on research, no Go code references seal.md -- remove it.

**Step 2: Stage the mirror updates**
```bash
git add .aether/agents-claude/*.md
```

**Step 3: Remove legacy .aether/agents/ directory**
```bash
git rm -r .aether/agents/
```
Verified safe: `grep -rn '.aether/agents/' cmd/*.go pkg/**/*.go` returns zero results. No Go code references this path. The install/update commands only reference `.claude/agents/ant/` and `agents-claude`.

**Step 4: Update .npmignore to exclude .aether/agents/**
Add to `.npmignore` in the "# Aether private directories" section:
```
.aether/agents/
```
This ensures that even if the directory is recreated locally, it won't be published.

**Step 5: Verify mirror parity**
```bash
diff -r .claude/agents/ant/ .aether/agents-claude/
```
Should return no differences (both directories now have identical content).

**Step 6: Verify npm package**
```bash
npm pack --dry-run 2>&1 | grep -E '\.aether/agents'
```
Should only show `.aether/agents-claude/` files, not `.aether/agents/`.

**Step 7: Run tests**
```bash
go test ./... -count=1
```
  </action>
  <verify>
    <automated>diff -r .claude/agents/ant/ .aether/agents-claude/ && git ls-files | grep '\.aether/agents/[^-]' | wc -l && go test ./... -count=1</automated>
  </verify>
  <done>Agent mirror .aether/agents-claude/ is byte-identical to canonical .claude/agents/ant/. Legacy .aether/agents/ directory removed from git. .npmignore updated. All Go tests pass. npm pack does not include old agent definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical agents -> npm consumers | Mirror drift means npm consumers get wrong agent definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | Agent mirror drift recurs after fix | accept | No automated sync mechanism in v1. CLAUDE.md documents the requirement. Consider CI check in v2. |
| T-07-02-02 | Information Disclosure | seal.md contains sensitive info | accept | seal.md is an agent definition (public markdown), not secrets. |
</threat_model>

<verification>
1. `diff -r .claude/agents/ant/ .aether/agents-claude/` returns no output (byte-identical)
2. `git ls-files | grep '\.aether/agents/[^-]' | wc -l` returns 0
3. `.npmignore` contains `.aether/agents/`
4. `go test ./... -count=1` exits 0
</verification>

<success_criteria>
- Agent mirror is byte-identical to canonical source
- Legacy .aether/agents/ directory completely removed from git tracking
- npm package no longer publishes old agent definitions
- All 524+ Go tests continue passing
</success_criteria>

<output>
After completion, create `.planning/phases/07-repository-hygiene/07-02-SUMMARY.md`
</output>
