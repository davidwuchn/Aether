---
phase: 07-repository-hygiene
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - .aether/docs/*.md
  - .aether/workers.md
  - .aether/QUEEN.md
  - .claude/commands/ant/init.md
  - .claude/commands/ant/lay-eggs.md
  - .claude/agents/ant/aether-oracle.md
  - .claude/agents/ant/aether-auditor.md
  - .opencode/agents/*.md
  - .aether/commands/
  - src/commands/
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - HYGN-02
  - HYGN-05

must_haves:
  truths:
    - "Running grep -rn 'aether-utils\\.sh' across .aether/ .claude/ .opencode/ returns zero results (excluding CHANGELOG and chambers)"
    - "Running grep -rn '\\.sh\\b' across .aether/docs/ returns zero results"
    - "README.md contains no shell script references"
    - "A developer can navigate from CLAUDE.md to any source file within 5 minutes"
    - "The .aether/commands/ and src/commands/ directories no longer create confusion"
  artifacts:
    - path: ".aether/docs/README.md"
      provides: "Documentation index without aether-utils.sh references"
      not_contains: "aether-utils.sh"
    - path: "README.md"
      provides: "Project README reflecting Go architecture"
      not_contains: ".sh"
  key_links:
    - from: "CLAUDE.md"
      to: "source files"
      via: "directory table and Quick Reference"
      pattern: "Architecture Overview"
---

<objective>
Remove all shell script references from documentation, clean up stale command definition directories, and ensure all docs reflect the current Go architecture.

Purpose: After removing the actual shell scripts in Plan 01, 100+ documentation files still reference `aether-utils.sh`, `.sh` file paths, and old shell-era tooling. Additionally, `.aether/commands/` (135 tracked files) and `src/commands/` (3 files) are dead weight that duplicate the actual command definitions in `.claude/commands/ant/` and `.opencode/commands/ant/`. These create confusion for new contributors trying to understand the repo structure.

Output: All documentation references current Go commands exclusively. No shell-era references remain in active files. Stale command definition directories are removed.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-repository-hygiene/07-RESEARCH.md
@.planning/phases/07-repository-hygiene/07-01-SUMMARY.md
@.planning/phases/07-repository-hygiene/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale command definition directories and audit documentation for shell references</name>
  <files>.aether/commands/*, src/commands/*, .aether/docs/*.md, .aether/workers.md, .aether/QUEEN.md, .claude/commands/ant/init.md, .claude/commands/ant/lay-eggs.md, .claude/agents/ant/aether-oracle.md, .claude/agents/ant/aether-auditor.md, .opencode/agents/*.md, cmd/emoji_audit.go</files>
  <read_first>
    .aether/docs/README.md (check for aether-utils.sh authority reference)
    .aether/workers.md (check for aether-utils.sh call examples -- 13 refs)
    .claude/commands/ant/init.md (check for clash-detect.sh reference)
    .claude/commands/ant/lay-eggs.md (check for shell count check)
    .claude/agents/ant/aether-oracle.md (check for shell references)
    .claude/agents/ant/aether-auditor.md (check for shell references)
    cmd/emoji_audit.go (line 51 -- references .aether/commands/*.yaml, will need update)
    .aether/commands/ (spot-check a few files to confirm they duplicate .claude/commands/ant/)
    src/commands/README.md (check if any generation tool or reference exists)
  </read_first>
  <acceptance_criteria>
    - `git ls-files | grep '\.aether/commands/' | wc -l` returns 0 (entire directory removed)
    - `git ls-files | grep '^src/commands/' | wc -l` returns 0 (entire directory removed)
    - `grep -rn 'aether-utils\.sh' --include='*.md' .aether/ .claude/ .opencode/ 2>/dev/null | grep -v chambers | grep -v CHANGELOG | wc -l` returns 0
    - `grep -rn '\.sh\b' --include='*.md' .aether/docs/ 2>/dev/null | wc -l` returns 0
    - `grep -c '\.sh\b\|aether-utils' README.md` returns 0
    - cmd/emoji_audit.go line 51 no longer references `.aether/commands/*.yaml`
    - `go test ./... -count=1` exits 0
  </acceptance_criteria>
  <action>
Remove stale command definition directories and clean all shell references from active documentation.

**Part A: Remove stale command directories**

1. Remove `.aether/commands/` (135 tracked YAML+MD files that duplicate actual commands):
```bash
git rm -r .aether/commands/
```
These YAML source definitions were intended for a generation script (`bin/generate-commands.sh`) that was never created. No Go code references them for install/update (verified: only `cmd/emoji_audit.go:51` has a glob reference, which will be updated below).

2. Remove `src/commands/` (3 files: README.md, _meta/template.yaml, _meta/tool-mapping.yaml):
```bash
git rm -r src/commands/
```
This directory references non-existent `bin/generate-commands.sh` and is dead weight.

3. Update `cmd/emoji_audit.go` line 51:
Change `".aether/commands/*.yaml"` to just `".claude/commands/ant/*.md"` since the YAML files are gone. The glob will match only the canonical command files.

4. Check if `src/` directory is now empty. If only `src/commands/` was tracked and it is now removed, remove `src/` from tracking entirely.

**Part B: Documentation shell reference cleanup**

Run this baseline search to identify all files needing updates:
```bash
grep -rln 'aether-utils\.sh\|\.sh\b' --include='*.md' \
  .aether/ .claude/ .opencode/ \
  | grep -v chambers | grep -v CHANGELOG | grep -v archive | grep -v planning-archive
```

For each file found, replace shell references with Go equivalents using these replacement rules:

- `aether-utils.sh` -> `aether` CLI (the Go binary)
- `clash-detect.sh` -> `aether clash-detect`
- `atomic-write.sh` -> `aether` uses Go atomic writes internally
- References to specific `.sh` scripts -> reference the equivalent Go subcommand
- Any "shell script" or "bash script" phrasing -> "Go command" or "aether CLI"

**Key files to update (from research):**
- `.aether/docs/README.md` -- remove `aether-utils.sh` as "execution authority"
- `.aether/workers.md` -- remove `aether-utils.sh` call examples (13 refs)
- `.aether/QUEEN.md` -- remove `aether-utils.sh` reference
- `.claude/commands/ant/init.md` -- remove `clash-detect.sh` reference
- `.claude/commands/ant/lay-eggs.md` -- update shell count check
- `.claude/agents/ant/aether-oracle.md` -- remove shell refs
- `.claude/agents/ant/aether-auditor.md` -- remove shell refs
- `.opencode/agents/` -- update all 23 agent definitions with shell refs
- `.aether/docs/*.md` -- update 20 docs files with shell refs
- `pkg/agent/curation/orchestrator.go` lines 4, 73, 89 -- update comments referencing "shell orchestrator.sh"

**Files NOT to modify:**
- `.aether/chambers/` (sealed colony archives, historical)
- `CHANGELOG.md` (historical record)
- `.planning-archive/` (historical)
- `.planning/` (GSD working files)
- `cmd/completion.go` (legitimate bash completion references)

**Verification after cleanup:**
```bash
grep -rn 'aether-utils\.sh' --include='*.md' .aether/ .claude/ .opencode/ | grep -v chambers | grep -v CHANGELOG
```
Must return zero results.

```bash
go test ./... -count=1
```
Must pass.
  </action>
  <verify>
    <automated>git ls-files | grep -E '(\.aether/commands/|src/commands/)' | wc -l && grep -rn 'aether-utils\.sh' --include='*.md' .aether/ .claude/ .opencode/ 2>/dev/null | grep -v chambers | grep -v CHANGELOG | wc -l && grep -rn '\.sh\b' --include='*.md' .aether/docs/ 2>/dev/null | wc -l && go test ./... -count=1</automated>
  </verify>
  <done>All stale command definition directories removed. Zero shell script references in active documentation files. All docs reference current Go architecture. All Go tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> developer understanding | Stale references mislead new contributors about how the system works |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | .aether/commands/ contains sensitive info | accept | Command definitions are public markdown/YAML. No secrets. |
| T-07-03-02 | Denial of Service | Removing .aether/commands/ breaks npm consumers | accept | Verified: no Go code references .aether/commands/ for install/update flow except emoji-audit glob which is updated. npm consumers install commands from .claude/commands/ant/ and .opencode/commands/ant/. |
</threat_model>

<verification>
1. `git ls-files | grep -E '(\.aether/commands/|src/commands/)' | wc -l` returns 0
2. `grep -rn 'aether-utils\.sh' --include='*.md' .aether/ .claude/ .opencode/ | grep -v chambers | grep -v CHANGELOG | wc -l` returns 0
3. `grep -rn '\.sh\b' --include='*.md' .aether/docs/ | wc -l` returns 0
4. `go test ./... -count=1` exits 0
</verification>

<success_criteria>
- .aether/commands/ and src/commands/ directories fully removed from git tracking
- Zero active documentation files reference aether-utils.sh or .sh scripts
- All documentation reflects current Go CLI architecture
- cmd/emoji_audit.go updated to reference only canonical command files
- All 524+ Go tests continue passing
</success_criteria>

<output>
After completion, create `.planning/phases/07-repository-hygiene/07-03-SUMMARY.md`
</output>
