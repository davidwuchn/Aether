---
phase: 07-repository-hygiene
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - cmd/deprecated_cmds.go
  - cmd/deprecated_cmds_test.go
  - cmd/suggest.go
  - cmd/suggest_cmds_test.go
  - cmd/worktree_merge.go
  - cmd/worktree_merge_test.go
  - cmd/maintenance.go
  - cmd/internal_cmds.go
  - pkg/exchange/export_test.go
  - pkg/graph/graph.go
autonomous: true
requirements:
  - HYGN-04

must_haves:
  truths:
    - "Running grep -c 'DEPRECATED' cmd/deprecated_cmds.go returns nothing (file removed)"
    - "Running grep -c 'isTestArtifact' cmd/suggest.go returns nothing (file removed, function moved)"
    - "Running grep -c 'worktree-merge' cmd/worktree_merge.go returns nothing (file removed)"
    - "Running go test ./pkg/exchange/... exits 0 (compilation error fixed)"
    - "Running go test ./... -count=1 exits 0 with no regressions"
  artifacts:
    - path: "cmd/maintenance.go"
      provides: "data-clean command with isTestArtifact function extracted from suggest.go"
      contains: "func isTestArtifact"
    - path: "pkg/exchange/export_test.go"
      provides: "Fixed test file with valid Go syntax"
      not_contains: "expected declaration"
  key_links:
    - from: "cmd/maintenance.go"
      to: "isTestArtifact function"
      via: "function call in data-clean handler"
      pattern: "isTestArtifact\\(signal\\)"
---

<objective>
Remove all deprecated Go commands, fix test compilation errors, and clean up dead code.

Purpose: Three Go source files contain deprecated commands that add clutter without value: `deprecated_cmds.go` (8 deprecated commands), `suggest.go` (5 deprecated suggest commands + one used function), and `worktree_merge.go` (deprecated merge command). Additionally, `pkg/exchange/export_test.go` has a compilation error, `cmd/internal_cmds.go` has a deprecated alias, and `pkg/graph/graph.go` has an unreachable code path. These create noise in the codebase and prevent `go test ./pkg/exchange/...` from running.

Output: Zero deprecated commands in Go source. All test files compile. No unreachable code paths. All tests pass.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-repository-hygiene/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract isTestArtifact, remove deprecated files, fix compilation errors</name>
  <files>cmd/maintenance.go, cmd/suggest.go, cmd/suggest_cmds_test.go, cmd/deprecated_cmds.go, cmd/deprecated_cmds_test.go, cmd/worktree_merge.go, cmd/worktree_merge_test.go, cmd/internal_cmds.go, pkg/exchange/export_test.go, pkg/graph/graph.go</files>
  <read_first>
    cmd/suggest.go (full file -- contains isTestArtifact function to extract, lines 62-84)
    cmd/maintenance.go (full file -- need to understand where to add isTestArtifact)
    cmd/deprecated_cmds.go (full file -- verify all commands are truly deprecated)
    cmd/worktree_merge.go (full file -- verify it is truly deprecated)
    cmd/internal_cmds.go (lines 355-365 -- deprecated error-patterns-check alias)
    pkg/exchange/export_test.go (lines 300-320 -- find the compilation error)
    pkg/graph/graph.go (line 490-500 -- find the unreachable return)
    cmd/root.go or cmd/main.go (check how deprecated commands are registered in init())
  </read_first>
  <acceptance_criteria>
    - `go build ./cmd/...` exits 0 (compiles successfully after isTestArtifact extraction)
    - `go test ./cmd/... -count=1` exits 0 (all command tests pass)
    - `go test ./pkg/exchange/... -count=1` exits 0 (compilation error fixed)
    - `go test ./... -count=1` exits 0 (full suite green)
    - `git ls-files | grep -E '(deprecated_cmds|suggest\.go|suggest_cmds_test|worktree_merge)' | wc -l` returns 0
    - `grep -n 'func isTestArtifact' cmd/maintenance.go` returns a match (function successfully moved)
    - `grep -c 'error-patterns-check' cmd/internal_cmds.go` returns 0 (deprecated alias removed)
  </acceptance_criteria>
  <action>
Remove all deprecated Go commands, extract the one used function, and fix test compilation.

**Step 1: Extract isTestArtifact from suggest.go to maintenance.go**

Read `cmd/suggest.go` lines 62-84 to get the exact `isTestArtifact` function. Then:

1. Open `cmd/maintenance.go` and add the `isTestArtifact` function (copy from suggest.go lines 62-84).
2. Add `"strings"` to the import block in `cmd/maintenance.go` (isTestArtifact uses `strings.HasPrefix`, `strings.ToLower`, `strings.Contains`).
3. Verify `go build ./cmd/...` compiles with isTestArtifact in maintenance.go.

**Step 2: Remove deprecated files**

```bash
git rm cmd/deprecated_cmds.go cmd/deprecated_cmds_test.go
git rm cmd/suggest.go cmd/suggest_cmds_test.go
git rm cmd/worktree_merge.go cmd/worktree_merge_test.go
```

**Step 3: Remove deprecated alias in internal_cmds.go**

In `cmd/internal_cmds.go`, remove lines 357-365 (the deprecated `error-patterns-check` alias block and its comment):
```go
// --- error-patterns-check (deprecated alias for error-pattern-check) ---
var errorPatternsCheckCmd = &cobra.Command{
    Use:        "error-patterns-check",
    ...
}
```

Also remove the `errorPatternsCheckCmd` registration in the `init()` function if it exists (search for `rootCmd.AddCommand(errorPatternsCheckCmd)`).

**Step 4: Fix pkg/exchange/export_test.go compilation error**

Read `pkg/exchange/export_test.go` around line 307. The error is `expected declaration, found 'for'` which means a function body or block was not properly closed before line 307. The `for` loop starting at line 307 appears to be at incorrect indentation level or outside a function.

Look at lines 295-320 to understand the context:
- Line 303 ends a test function with `}`
- Line 305-312 starts a new `for` loop block that is NOT inside a function definition
- Fix: This orphaned code block needs to either be inside a test function or removed entirely if it duplicates a test that already exists.

Examine the full file structure to determine the correct fix. The code at lines 305-320 looks like a continuation of the previous test function (lines around 280-303) that was accidentally split. Merge the orphaned block back into the preceding test function.

**Step 5: Review pkg/graph/graph.go unreachable return**

Read `pkg/graph/graph.go` around line 495. There is an explicit `// unreachable` return statement. Examine the context:
- If the code path is truly unreachable (e.g., after a return statement in the same branch), remove it
- If it is a defensive return at the end of a function, it can stay (Go requires return statements)

**Step 6: Verify everything compiles and tests pass**

```bash
go build ./cmd/...
go vet ./...
go test ./... -count=1
```
  </action>
  <verify>
    <automated>go build ./cmd/... && go vet ./... && go test ./... -count=1</automated>
  </verify>
  <done>All deprecated Go command files removed. isTestArtifact function successfully extracted to maintenance.go. Deprecated alias removed from internal_cmds.go. pkg/exchange/export_test.go compilation error fixed. All Go code compiles. All tests pass with zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go source -> compiled binary | Removing functions that are still called breaks compilation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Denial of Service | Removing isTestArtifact breaks data-clean | mitigate | Extract function to maintenance.go FIRST, then verify compilation, then remove suggest.go. Three-step process with compilation verification at each step. |
| T-07-04-02 | Denial of Service | Removing deprecated commands that are still invoked | accept | All commands are marked DEPRECATED with Cobra's deprecation system. Users who invoke them already see deprecation warnings. Removal is safe. |
</threat_model>

<verification>
1. `go build ./cmd/...` exits 0
2. `go vet ./...` exits 0
3. `go test ./... -count=1` exits 0
4. `grep -n 'func isTestArtifact' cmd/maintenance.go` returns a match
5. `git ls-files | grep -E '(deprecated_cmds|suggest\.go|suggest_cmds_test|worktree_merge)' | wc -l` returns 0
6. `grep -c 'error-patterns-check' cmd/internal_cmds.go` returns 0
</verification>

<success_criteria>
- Zero deprecated command files remain in cmd/
- isTestArtifact function lives in maintenance.go and is called by data-clean
- pkg/exchange/export_test.go compiles and tests pass
- Deprecated alias removed from internal_cmds.go
- All Go tests pass with no regressions (go test ./... -count=1)
- go vet ./... reports no issues
</success_criteria>

<output>
After completion, create `.planning/phases/07-repository-hygiene/07-04-SUMMARY.md`
</output>
