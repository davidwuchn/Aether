---
phase: 52-continue-review-worker-outcome-reports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/continue.md
  - .opencode/commands/ant/continue.md
autonomous: true
requirements:
  - CONT-05

must_haves:
  truths:
    - "Wrapper completion packet documentation in both Claude and OpenCode continue.md includes the report field"
    - "Wrappers know to include full structured findings as markdown in the report field"
  artifacts:
    - path: ".claude/commands/ant/continue.md"
      provides: "Updated completion packet JSON example with report field"
      contains: "report"
    - path: ".opencode/commands/ant/continue.md"
      provides: "Updated completion packet JSON example with report field"
      contains: "report"
  key_links:
    - from: ".claude/commands/ant/continue.md"
      to: ".opencode/commands/ant/continue.md"
      via: "Must stay structurally aligned per platform parity policy"
      pattern: "report"
---

<objective>
Update the completion packet documentation in both Claude and OpenCode continue command wrappers to include the `report` field.

Purpose: LLM agents running review workers need to know they should return their full markdown findings in the `report` field of the completion packet. Without documentation, wrappers will omit this field and reports will be empty.

Output: Both continue.md files updated with report field in the JSON example and guidance text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-continue-review-worker-outcome-reports/52-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add report field to completion packet documentation in both wrappers</name>
  <files>.claude/commands/ant/continue.md, .opencode/commands/ant/continue.md</files>

  <read_first>
    .claude/commands/ant/continue.md (lines 55-98 for dispatch instructions and completion packet section)
    .opencode/commands/ant/continue.md (lines 55-98 for the same sections)
  </read_first>

  <action>
  **Both files get identical changes (platform parity policy).**

  **Change 1: Update worker return requirement (line 59)**

  Current text:
  ```
  5. Require every worker to return a terminal structured result with: `name`, `caste`, `stage`, `wave`, `task_id`, `status`, `summary`, `blockers`, and `duration`.
  ```

  Change to:
  ```
  5. Require every worker to return a terminal structured result with: `name`, `caste`, `stage`, `wave`, `task_id`, `status`, `summary`, `blockers`, `duration`, and `report`.
  ```

  **Change 2: Update completion packet JSON example (lines 74-86)**

  Add `"report": ""` field after the `"duration": 0` line in the dispatch object. The updated JSON block should be:
  ```json
  {
    "continue_manifest": {
      "...": "the exact result.continue_manifest object"
    },
    "dispatches": [
      {
        "name": "Vigil-17",
        "caste": "watcher",
        "stage": "verification",
        "wave": 1,
        "task_id": "continue-verification-1",
        "status": "completed",
        "summary": "Verified the phase can advance.",
        "blockers": [],
        "duration": 0,
        "report": "## Findings\n\nAll tests pass. No issues found in phase 1 implementation."
      }
    ]
  }
  ```

  **Change 3: Add prose guidance after the JSON block (after line 88)**

  Insert a new paragraph between the closing ``` of the JSON block and the "Then finalize" instruction:
  ```
  The `report` field is optional but strongly recommended for review workers (Watcher, Gatekeeper, Auditor, Probe). Include the worker's full structured findings as markdown -- this content is persisted as a per-worker `.md` report on disk. When omitted, the report file still renders with assignment metadata but shows "No detailed report provided."
  ```
  </action>

  <verify>
    <automated>grep -c '"report"' /Users/callumcowie/repos/Aether/.claude/commands/ant/continue.md /Users/callumcowie/repos/Aether/.opencode/commands/ant/continue.md</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/commands/ant/continue.md` contains `"report":` in the completion packet JSON example
    - `.opencode/commands/ant/continue.md` contains `"report":` in the completion packet JSON example
    - `.claude/commands/ant/continue.md` line 59 area contains `report` in the worker return requirement list
    - `.opencode/commands/ant/continue.md` line 59 area contains `report` in the worker return requirement list
    - `.claude/commands/ant/continue.md` contains `The \`report\` field is optional but strongly recommended`
    - `.opencode/commands/ant/continue.md` contains `The \`report\` field is optional but strongly recommended`
    - Both files have the same report-related changes (diff output is identical for changed regions)
  </acceptance_criteria>

  <done>Both Claude and OpenCode continue command wrappers document the report field in the JSON example, list it in the worker return requirements, and include guidance prose explaining its purpose and optional nature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This task only modifies documentation/markdown files. No code changes, no security surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03 | N/A | Wrapper documentation | accept | Documentation-only change. No security implications. |
</threat_model>

<verification>
1. `grep '"report"' .claude/commands/ant/continue.md .opencode/commands/ant/continue.md` -- report field appears in both files
2. `grep 'report' .claude/commands/ant/continue.md` -- appears in JSON example, requirement list, and guidance prose
3. Diff both files' completion packet sections to confirm structural parity
</verification>

<success_criteria>
1. Both continue.md files contain the `report` field in the completion packet JSON example
2. Both files list `report` in the worker return requirement (line ~59)
3. Both files include the guidance paragraph about the report field being optional but recommended
4. Both files are structurally aligned (same changes in both)
</success_criteria>

<output>
After completion, create `.planning/phases/52-continue-review-worker-outcome-reports/52-02-SUMMARY.md`
</output>
