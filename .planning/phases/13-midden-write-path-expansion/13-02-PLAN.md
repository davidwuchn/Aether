---
phase: 13-midden-write-path-expansion
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - .aether/docs/command-playbooks/build-wave.md
  - .aether/docs/command-playbooks/build-full.md
autonomous: true
requirements: [MID-03]

must_haves:
  truths:
    - "After a wave completes in build-wave.md, a midden threshold check runs and detects categories with 3+ occurrences"
    - "When a category reaches 3+ occurrences, a REDIRECT pheromone is emitted mid-build with source auto:error"
    - "Duplicate REDIRECT pheromones for the same category are not emitted (dedup via existing pheromone check)"
    - "Maximum 3 REDIRECT emissions per build (capped)"
    - "A visible warning is displayed when a REDIRECT is emitted mid-build"
    - "Existing builder failure midden-write calls from Plan 13-01 still fire unchanged (no regression)"
  artifacts:
    - path: ".aether/docs/command-playbooks/build-wave.md"
      provides: "Intra-phase midden threshold check block after Step 5.2 wave completion"
      contains: "midden-recent-failures"
    - path: ".aether/docs/command-playbooks/build-full.md"
      provides: "Mirror of threshold check block"
      contains: "midden-recent-failures"
  key_links:
    - from: "build-wave.md threshold check"
      to: "midden-recent-failures subcommand"
      via: "bash call querying last 50 failures"
      pattern: "midden-recent-failures 50"
    - from: "build-wave.md threshold check"
      to: "pheromone-write REDIRECT"
      via: "conditional bash call when category count >= 3"
      pattern: 'pheromone-write REDIRECT'
    - from: "build-wave.md threshold check"
      to: "pheromones.json dedup check"
      via: "jq query for existing auto:error signals"
      pattern: 'source == "auto:error"'
---

<objective>
Add an intra-phase midden threshold check that runs after each wave's results are processed, emitting REDIRECT pheromones mid-build when error categories recur 3+ times.

Purpose: Currently the midden threshold check only runs at phase-end (in continue-advance.md). This means recurring failure patterns during a multi-wave build are not detected until after the entire build completes. Adding a mid-build check allows the colony to course-correct while the build is still running.

Output: After each wave in build-wave.md, a threshold check queries midden-recent-failures, groups by category, and emits REDIRECT pheromones for categories with 3+ occurrences (capped at 3 emissions, with dedup).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-midden-write-path-expansion/13-RESEARCH.md
@.planning/phases/13-midden-write-path-expansion/13-01-SUMMARY.md
@.aether/docs/command-playbooks/build-wave.md
@.aether/docs/command-playbooks/build-full.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intra-phase midden threshold check after wave result processing</name>
  <files>
    .aether/docs/command-playbooks/build-wave.md
    .aether/docs/command-playbooks/build-full.md
  </files>
  <action>
Add a midden threshold check block in build-wave.md AFTER the "Check for total wave failure" section in Step 5.2 (after the partial wave failure escalation path and before Step 5.3). This block runs ONCE per wave, after ALL worker results for that wave are processed.

**Insertion point in build-wave.md:** After line 480 (`**Parse each worker's validated JSON output to collect:** status, files_created, files_modified, blockers`) and before Step 5.3 (`### Step 5.3: Spawn Wave 2+ Workers`).

Insert this new subsection:

```markdown
**Intra-phase midden threshold check (MID-03):**

After processing all wave results, check if any midden error category has reached 3+ occurrences. If so, emit a REDIRECT pheromone mid-build to alert the colony.

Run using the Bash tool with description "Checking midden thresholds...":
```bash
midden_result=$(bash .aether/aether-utils.sh midden-recent-failures 50 2>/dev/null || echo '{"count":0,"failures":[]}')
midden_count=$(echo "$midden_result" | jq '.count // 0')

if [[ "$midden_count" -gt 0 ]]; then
  recurring_categories=$(echo "$midden_result" | jq -r '
    [.failures[] | .category]
    | group_by(.)
    | map(select(length >= 3))
    | map({category: .[0], count: length})
    | .[]
    | @base64
  ' 2>/dev/null || echo "")

  redirect_emit_count=0
  for encoded in $recurring_categories; do
    [[ $redirect_emit_count -ge 3 ]] && break
    [[ -z "$encoded" ]] && continue
    category=$(echo "$encoded" | base64 -d | jq -r '.category')
    count=$(echo "$encoded" | base64 -d | jq -r '.count')

    existing=$(jq -r --arg cat "$category" '
      [.signals[] | select(.active == true and .source == "auto:error" and (.content.text | contains($cat)))] | length
    ' .aether/data/pheromones.json 2>/dev/null || echo "0")

    if [[ "$existing" == "0" ]]; then
      bash .aether/aether-utils.sh pheromone-write REDIRECT \
        "[error-pattern] Category \"$category\" recurring ($count occurrences)" \
        --strength 0.7 \
        --source "auto:error" \
        --reason "Auto-emitted: midden error pattern recurred 3+ times mid-build" \
        --ttl "30d" 2>/dev/null || true
      redirect_emit_count=$((redirect_emit_count + 1))
    fi
  done

  if [[ $redirect_emit_count -gt 0 ]]; then
    echo "Warning: Midden threshold triggered -- $redirect_emit_count REDIRECT pheromone(s) emitted mid-build"
  fi
fi
```

Display if any REDIRECT was emitted:
```
Warning: Midden threshold: "{category}" recurring ({count}x) -- REDIRECT emitted mid-build
```
```

This logic is copied directly from continue-advance.md Step 2.1c (the existing phase-end threshold check). The only adaptation is the display message says "mid-build" instead of referring to continue.

**Mirror in build-full.md:** Insert the identical block at the corresponding location -- after the "Parse each worker's validated JSON output" line (around line 898) and before Step 5.3.

**Important:** Do NOT place this check inside the per-worker processing loop. It runs ONCE per wave, after all worker results in that wave have been processed and their midden-write calls have fired.
  </action>
  <verify>
Run `grep -c 'midden-recent-failures' .aether/docs/command-playbooks/build-wave.md` -- should show the existing per-wave midden query (Step 5.1) PLUS 1 new threshold check call.
Run `grep 'pheromone-write REDIRECT' .aether/docs/command-playbooks/build-wave.md` -- should match 1 line in the threshold check block.
Run `grep -c 'midden-recent-failures' .aether/docs/command-playbooks/build-full.md` -- should show existing calls PLUS 1 new mirror.
Run `grep 'pheromone-write REDIRECT' .aether/docs/command-playbooks/build-full.md` -- should match 1 line (mirror).
Verify the threshold uses `length >= 3` (not > 3) and cap is 3 emissions (`redirect_emit_count -ge 3`).
Verify the dedup check queries `source == "auto:error"` in pheromones.json.
Verify existing Step 5.2 failure handling (midden-write calls from Plan 13-01) are still present by grepping for `midden-write "worker_failure"`.
Run `npm test` to confirm no regressions.
  </verify>
  <done>
Intra-phase midden threshold check block exists after Step 5.2 wave processing in build-wave.md. It queries midden-recent-failures, groups by category, emits REDIRECT for categories with 3+ occurrences (capped at 3, deduped). Build-full.md mirrors the block. Existing failure handling is unchanged. Tests pass.
  </done>
</task>

</tasks>

<verification>
1. Grep for `midden-recent-failures 50` in build-wave.md -- confirms threshold check queries the right number of recent failures
2. Grep for `pheromone-write REDIRECT` in build-wave.md and build-full.md -- confirms REDIRECT emission logic exists in both
3. Grep for `auto:error` in build-wave.md -- confirms dedup source check
4. Grep for `midden-write "worker_failure"` in build-wave.md -- confirms Plan 13-01 changes still present (no regression)
5. Verify threshold block is placed BETWEEN Step 5.2 and Step 5.3 (not inside per-worker loop)
6. npm test passes with no regressions
</verification>

<success_criteria>
- Threshold check block exists in build-wave.md between Step 5.2 and Step 5.3
- Block queries midden-recent-failures 50 and groups by category
- Categories with 3+ occurrences trigger REDIRECT pheromone with source "auto:error"
- Dedup prevents duplicate REDIRECT for same category
- Cap of 3 REDIRECT emissions per build
- Visible warning displayed when REDIRECT emitted
- Mirror in build-full.md matches exactly
- All existing midden-write calls from Plan 13-01 unchanged
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-midden-write-path-expansion/13-02-SUMMARY.md`
</output>
