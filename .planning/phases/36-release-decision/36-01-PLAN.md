---
phase: 36-release-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/version.json
  - npm/package.json
  - CHANGELOG.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All Go tests pass (2900+)"
    - "Binary builds cleanly"
    - "Agent parity tests pass (no drift)"
    - "Version files all say 1.0.20"
    - "CHANGELOG.md has v1.0.20 entry with headline and key items"
    - "CLAUDE.md version references updated to v1.0.20"
    - "Git tag v1.0.20 created on the release commit"
  artifacts:
    - path: ".aether/version.json"
      provides: "Source-checkout release version"
      contains: '"version":"1.0.20"'
    - path: "npm/package.json"
      provides: "npm bootstrap package version"
      contains: '"version": "1.0.20"'
    - path: "CHANGELOG.md"
      provides: "Release notes for v1.0.20"
      contains: "## [1.0.20]"
    - path: "CLAUDE.md"
      provides: "Updated version references"
      contains: "v1.0.20"
  key_links:
    - from: ".aether/version.json"
      to: "npm/package.json"
      via: "matching version string"
      pattern: '"version".*1\\.0\\.20'
    - from: "CHANGELOG.md"
      to: "CLAUDE.md"
      via: "version consistency"
      pattern: "1\\.0\\.20"
---

<objective>
Cut v1.0.20 release: validate readiness, update version files, write changelog, commit and tag.

Purpose: Ship the checkpoint release per D-08 (ship now). 69 commits and ~20K lines of changes since v1.0.19 include P0 runtime truth fixes, continue unblock, cleanup, and platform parity. The release is technically ready (tests green, binary builds, drift clean).

Output: Version files updated to 1.0.20, CHANGELOG.md entry written, CLAUDE.md version bumped, release commit and tag created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-release-decision/36-CONTEXT.md
@AETHER-OPERATIONS-GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate release readiness</name>
  <read_first>
    - .planning/phases/36-release-decision/36-CONTEXT.md (D-01 through D-06 validation checks)
  </read_first>
  <action>Run the four validation checks from CONTEXT.md decisions D-01 through D-04:

1. Run `go test ./...` and confirm all tests pass (per D-01). If any test fails, stop and report -- the release cannot proceed.
2. Run `go build ./cmd/aether` and confirm clean build with exit code 0 (per D-02).
3. Run `go test ./... -run "Drift|Parity|Completeness" -v` to verify agent parity tests pass with zero drift (per D-03).
4. Report validation results. All four checks must pass before proceeding to Task 2.

Do NOT modify any files in this task. This is read-only validation only.</action>
  <verify>
    <automated>go test ./... 2>&1 | grep -E "^(ok|FAIL)" | grep -c "FAIL" | xargs -I{} test {} -eq 0 && go build ./cmd/aether && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>All four validation checks pass: tests green, binary builds, drift clean. No files modified.</done>
  <acceptance_criteria>
    - `go test ./...` exits 0 with all packages reporting "ok"
    - `go build ./cmd/aether` exits 0
    - Drift/parity tests pass with zero failures
    - No files created or modified
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update version files and write changelog entry</name>
  <read_first>
    - .aether/version.json (current version to update)
    - npm/package.json (current version to update)
    - CHANGELOG.md (existing changelog format to follow, add entry after [Unreleased])
    - CLAUDE.md (three version references to update: line 3 header, line 12 table, line 841 footer)
  </read_first>
  <action>Update all version-tracking files and write the changelog entry per D-07, D-10, D-11.

**1. Update `.aether/version.json`:**
Change `"version":"1.0.19"` to `"version":"1.0.20"` and update `"updated_at"` to `"2026-04-23"`.

**2. Update `npm/package.json`:**
Change `"version": "1.0.19"` to `"version": "1.0.20"`.

**3. Write CHANGELOG.md entry for v1.0.20:**
Insert a new section between `## [Unreleased]` and `## [1.0.19]`. Follow the existing format exactly. Content per D-10 and D-11:

```markdown
## [1.0.20] - 2026-04-23

Truth recovery and platform parity checkpoint release.

### Fixed
- FakeInvoker blocked from production paths; real invoker requires honest platform dispatch (R045)
- DispatchBatch error propagation ensures dispatch errors surface to callers (R046)
- Four continue bypass paths closed for verified_partial, watcher timeout, reconcile, and git claims (R047, R048)
- In-repo build claims are now git-verified for ALL completed workers (R049)
- Environmental dismissal removed from verification -- all failures produce honest summaries (R050)
- Colony state advancement is atomic via UpdateJSONAtomically; state saved before side effects (R051)
- Continue detects abandoned builds (all dispatches stuck at "spawned" >10 min) and returns blocked with recovery commands
- Stale report files cleared before continue verification runs
- 523 stale worktrees and 259 orphaned branches cleaned up
- All 18 unresolved blocker flags archived (issues fixed by phases 31-33)
- All 25 OpenCode agents synced from Claude masters (zero drift)
- All 25 Codex agents updated with Phase 31-33 runtime concepts

### Known Limitations
- Medic does not detect "functionally stuck" deadlocks where colony state is structurally valid but all forward paths are blocked. A dedicated fix is planned for a future release.
```

**4. Update `CLAUDE.md` version references:**
Three specific locations need updating:
- Line 3: `> **Current Version:** v1.0.20`
- Line 12: `| Version | v1.0.20 |`
- Line 841: `*Updated for Aether v1.0.20 — 2026-04-23*`

Use grep to find the exact lines before editing -- line numbers may have shifted.</action>
  <verify>
    <automated>grep -c '"version":"1.0.20"' .aether/version.json && grep -c '"version": "1.0.20"' npm/package.json && grep -c '1.0.20' CHANGELOG.md && grep -c 'v1.0.20' CLAUDE.md</automated>
  </verify>
  <done>Version bumped to 1.0.20 in .aether/version.json, npm/package.json, CHANGELOG.md entry written with headline and key items per D-10/D-11, CLAUDE.md version references updated to v1.0.20.</done>
  <acceptance_criteria>
    - `.aether/version.json` contains exactly `"version":"1.0.20"` and `"updated_at":"2026-04-23"`
    - `npm/package.json` contains exactly `"version": "1.0.20"`
    - `CHANGELOG.md` has `## [1.0.20] - 2026-04-23` section between `[Unreleased]` and `[1.0.19]`
    - CHANGELOG entry includes "Truth recovery and platform parity" headline
    - CHANGELOG entry includes all R045-R051 references in Fixed section
    - CHANGELOG entry includes Known Limitations section about Medic deadlock (per D-12)
    - `CLAUDE.md` has exactly 3 occurrences of `v1.0.20` (header, table, footer)
    - `CLAUDE.md` has zero occurrences of `v1.0.19`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Commit, tag, and update planning state</name>
  <read_first>
    - .planning/ROADMAP.md (to update Phase 36 status)
    - .planning/STATE.md (to update progress and decisions)
  </read_first>
  <action>Finalize the release with git operations and planning state updates per D-08 and D-09.

**1. Stage and commit the release files:**
Stage only the version files and changelog (not planning state):
```bash
git add .aether/version.json npm/package.json CHANGELOG.md CLAUDE.md
git commit -m "release: v1.0.20 — truth recovery and platform parity"
```

**2. Create annotated git tag:**
```bash
git tag -a v1.0.20 -m "v1.0.20: Truth recovery and platform parity"
```

**3. Update ROADMAP.md:**
Change Phase 36 status from `Planned` to `Complete`. Update the phase detail section to show completion status.

**4. Update STATE.md:**
- Update `last_updated` to current timestamp
- Update `status` to reflect phase 36 complete
- Add decision: "v1.0.20 released 2026-04-23 as checkpoint release per D-08. GSD phases continue after release per D-09."

**5. Commit planning updates separately:**
```bash
git add .planning/ROADMAP.md .planning/STATE.md
git commit -m "docs(36): update planning state for v1.0.20 release"
```

Do NOT push. Per project rules, pushing requires explicit user request.</action>
  <verify>
    <automated>git describe --tags --exact-match HEAD 2>/dev/null | grep -q "v1.0.20" && echo "TAG VERIFIED"</automated>
  </verify>
  <done>Release commit and tag v1.0.20 created. ROADMAP.md updated to show Phase 36 complete. STATE.md updated with release decision. No push performed.</done>
  <acceptance_criteria>
    - `git log --oneline -1` shows "release: v1.0.20" commit
    - `git tag -l v1.0.20` lists the tag
    - `git tag -n1 v1.0.20` shows annotation "v1.0.20: Truth recovery and platform parity"
    - ROADMAP.md Phase 36 shows status "Complete"
    - STATE.md contains "v1.0.20 released" decision
    - `git log --oneline -1` (HEAD~1) shows "docs(36): update planning state" commit
    - No remote push has occurred
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source repo -> git tag | Tag creation is the trust anchor for downstream release builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | git tag v1.0.20 | accept | Tags are local-only; push requires explicit user request per project rules |
| T-36-02 | Spoofing | CHANGELOG.md content | mitigate | Changelog references specific requirement IDs (R045-R051) traceable to commit history |
| T-36-03 | Repudiation | Release commit | mitigate | Annotated tag with message provides non-repudiable release record |
</threat_model>

<verification>
1. `go test ./...` -- all packages green
2. `go build ./cmd/aether` -- clean build
3. `grep '"version":"1.0.20"' .aether/version.json` -- version updated
4. `grep '"version": "1.0.20"' npm/package.json` -- npm version updated
5. `grep '1.0.20' CHANGELOG.md` -- changelog entry present
6. `grep -c 'v1.0.20' CLAUDE.md` -- exactly 3 version references
7. `git tag -l v1.0.20` -- tag created
</verification>

<success_criteria>
- All validation checks pass (tests, build, drift)
- .aether/version.json and npm/package.json both contain "1.0.20"
- CHANGELOG.md has v1.0.20 entry with headline "Truth recovery and platform parity" and Known Limitations section
- CLAUDE.md updated to v1.0.20 in all three locations
- Release commit created with message "release: v1.0.20 — truth recovery and platform parity"
- Annotated tag v1.0.20 created
- ROADMAP.md Phase 36 marked Complete
- STATE.md updated with release decision
- No git push performed
</success_criteria>

<output>
After completion, create `.planning/phases/36-release-decision/36-01-SUMMARY.md`
</output>
