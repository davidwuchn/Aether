---
phase: 25-medic-ant-core
plan: 03
type: execute
wave: 2
files_modified:
  - cmd/medic_wrapper.go
  - cmd/medic_wrapper_test.go
  - .aether/commands/medic.yaml
  - .claude/commands/ant/medic.md
  - .opencode/commands/ant/medic.md
  - cmd/codex_visuals.go
depends_on:
  - 25-01-PLAN.md
  - 25-02-PLAN.md
autonomous: true
requirements:
  - R039
  - R044

must_haves:
  truths:
    - "scanWrapperParity checks command and agent counts across Claude/OpenCode/Codex surfaces"
    - "/ant:medic slash command wrapper exists in Claude and OpenCode"
    - "YAML source definition exists in .aether/commands/medic.yaml"
    - "Wrapper parity scanner reports count mismatches as warnings"
  artifacts:
    - path: "cmd/medic_wrapper.go"
      provides: "Wrapper parity scanner and deep scan integration"
      contains: "scanWrapperParity, countFilesInDir"
    - path: "cmd/medic_wrapper_test.go"
      provides: "Wrapper parity tests"
      contains: "TestScanWrapperParity"
    - path: ".aether/commands/medic.yaml"
      provides: "YAML source definition for medic command"
    - path: ".claude/commands/ant/medic.md"
      provides: "Claude Code slash command wrapper"
    - path: ".opencode/commands/ant/medic.md"
      provides: "OpenCode slash command wrapper"
  key_links:
    - from: "wrapper parity scanner"
      to: "deep scan mode"
      via: "scanWrapperParity called when --deep flag is set"
---

<objective>
Implement the wrapper parity scanner (deep scan mode) and create the slash command wrappers for `/ant:medic` on Claude Code and OpenCode. This connects the Medic to the colony's command system and adds cross-surface validation.

Purpose: Complete the Medic's scan coverage to include ceremony integrity checks, and make the command accessible via slash commands.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement wrapper parity scanner</name>
  <files>cmd/medic_wrapper.go</files>
  <read_first>
    - cmd/medic_scanner.go (fileChecker, issueWarning, issueInfo helpers)
    - .planning/phases/25-medic-ant-core/25-RESEARCH.md (wrapper file counts section)
    - CLAUDE.md (wrapper-runtime UX contract, YAML source chain)
  </read_first>
  <action>
    1. Create `cmd/medic_wrapper.go`:
       - Define expected count constants:
         ```go
         const (
             expectedYAMLCommands    = 50  // 49 existing + 1 medic
             expectedClaudeCommands  = 50
             expectedOpenCodeCommands = 50
             expectedClaudeAgents    = 25  // 24 existing + 1 medic
             expectedOpenCodeAgents  = 25
             expectedCodexAgents     = 25
             expectedClaudeMirror    = 25
             expectedCodexMirror     = 25
             expectedColonySkills    = 11  // 10 existing + 1 medic
             expectedDomainSkills    = 18
         )
         ```
       - Implement `scanWrapperParity(fc *fileChecker) []HealthIssue`:
         - Count files in each surface directory:
           - `.aether/commands/*.yaml`
           - `.claude/commands/ant/*.md`
           - `.opencode/commands/ant/*.md`
           - `.codex/agents/*.toml`
           - `.claude/agents/ant/*.md`
           - `.opencode/agents/*.md`
           - `.aether/agents-claude/*.md`
           - `.aether/agents-codex/*.toml`
           - `.aether/skills/colony/*/SKILL.md`
           - `.aether/skills/domain/*/SKILL.md`
         - Compare each count against expected constants
         - If mismatch: warning "{surface} has {actual} files, expected {expected}"
         - Compare command counts across Claude/OpenCode/YAML: all 3 must match
         - Compare agent counts across Claude/OpenCode/Codex/mirrors: all must match
         - If cross-surface mismatch: warning "Command count mismatch: YAML={yaml}, Claude={claude}, OpenCode={opencode}"
       - Implement helper `countFilesInDir(pattern string) int`:
         - Use `filepath.Glob` with repo-relative paths
         - Return count of matches
    2. Update `performHealthScan` in medic_scanner.go:
       - If `opts.Deep` is true, also call `scanWrapperParity`
  </action>
  <acceptance_criteria>
    - `scanWrapperParity` function exists and returns `[]HealthIssue`
    - `countFilesInDir` helper exists
    - Deep scan mode calls wrapper parity scanner
    - Detects count mismatches across surfaces
    - All existing tests still pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create medic YAML source definition</name>
  <files>.aether/commands/medic.yaml</files>
  <read_first>
    - .aether/commands/patrol.yaml (YAML command pattern)
    - .aether/commands/status.yaml (YAML command pattern)
    - .aether/commands/chaos.yaml (command with flags)
  </read_first>
  <action>
    1. Create `.aether/commands/medic.yaml`:
       ```yaml
       name: medic
       description: "Diagnose colony health — scan all colony data for issues"
       runtime_command: "aether medic"
       flags:
         - name: fix
           description: "Enable repair mode (default: read-only scan)"
           type: boolean
           default: false
         - name: force
           description: "Allow destructive repairs (requires --fix)"
           type: boolean
           default: false
         - name: json
           description: "Output structured JSON report"
           type: boolean
           default: false
         - name: deep
           description: "Include wrapper parity and ceremony checks"
           type: boolean
           default: false
       guardrails:
         - "Read-only by default; --fix required for any mutation"
         - "Backs up before repair; --force required for destructive ops"
         - "Exit codes: 0=healthy, 1=warnings, 2=critical"
       ```
  </action>
  <acceptance_criteria>
    - `.aether/commands/medic.yaml` exists with valid YAML
    - Contains name, description, runtime_command, flags, guardrails
    - Flags match medic_cmd.go CLI flags exactly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create Claude and OpenCode slash command wrappers</name>
  <files>.claude/commands/ant/medic.md, .opencode/commands/ant/medic.md</files>
  <read_first>
    - .claude/commands/ant/patrol.md (wrapper pattern for system commands)
    - .claude/commands/ant/status.md (wrapper pattern for dashboard commands)
    - .opencode/commands/ant/patrol.md (OpenCode wrapper pattern)
    - .aether/commands/medic.yaml (YAML source)
  </read_first>
  <action>
    1. Create `.claude/commands/ant/medic.md`:
       - Header with command name and description
       - Instructions to run `aether medic` with optional flags
       - Note that `--fix` is required for repairs
       - Reference to YAML source for guardrails
       - Output interpretation guidance (severity levels)
    2. Create `.opencode/commands/ant/medic.md`:
       - Same structure as Claude wrapper, adapted for OpenCode conventions
       - Reference to YAML source
  </action>
  <acceptance_criteria>
    - `.claude/commands/ant/medic.md` exists
    - `.opencode/commands/ant/medic.md` exists
    - Both reference the medic YAML source definition
    - Both explain the --fix flag requirement
    - Command count in repo increases to 50 per surface
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update command counts in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Quick Reference table — slash command counts)
  </read_first>
  <action>
    1. Update Quick Reference table in CLAUDE.md:
       - Slash commands: "49 (Claude)" → "50 (Claude)"
       - Slash commands: "49 (OpenCode)" → "50 (OpenCode)"
       - Keep Codex count at 24 TOML agents (medic agent already added in Plan 01)
    2. Update agent count from 24 to 25 if not already done
  </action>
  <acceptance_criteria>
    - CLAUDE.md Quick Reference shows 50 slash commands for Claude and OpenCode
    - Agent count shows 25
    - Counts are consistent
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Write wrapper parity tests</name>
  <files>cmd/medic_wrapper_test.go</files>
  <read_first>
    - cmd/medic_wrapper.go (wrapper parity implementation)
    - cmd/medic_scanner_test.go (test patterns)
  </read_first>
  <action>
    1. Create `cmd/medic_wrapper_test.go`:
       - `TestCountFilesInDir` — verify helper counts correctly
       - `TestScanWrapperParityHealthy` — all expected files present, no issues
       - `TestScanWrapperParityMismatch` — simulated count mismatch produces warning
       - `TestDeepScanIncludesWrapperParity` — performHealthScan with Deep=true includes wrapper checks
    2. Use temp directories with fixture files for isolation
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run "TestScanWrapper|TestCountFiles|TestDeepScan" -v` passes
    - All existing tests still pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scanner → wrapper files | Read-only glob operations only |
| YAML source → command wrappers | Source defines flags; wrappers present them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering | Expected counts hardcoded | accept | Counts verified by tests, updated when commands added |
| T-25-06 | Information Disclosure | Wrapper parity reveals repo structure | accept | Local-only, no remote exposure |

</threat_model>

<verification>
- `go test ./cmd -run "TestScanWrapper|TestCountFiles|TestDeepScan" -v` — all pass
- `go test ./cmd/...` — all pass (no regressions)
- `go vet ./...` — clean
- `go build ./cmd/aether` — compiles
- `aether medic --deep` — includes wrapper parity in output
- `.aether/commands/medic.yaml` — exists with valid YAML
- `.claude/commands/ant/medic.md` — exists
- `.opencode/commands/ant/medic.md` — exists
- CLAUDE.md — command counts updated
</verification>

<success_criteria>
- Wrapper parity scanner detects count mismatches across surfaces
- `/ant:medic` accessible via Claude and OpenCode slash commands
- YAML source definition exists
- Command counts updated in CLAUDE.md
- Tests cover parity checks and deep scan mode
</success_criteria>
