---
phase: 23-recovery-and-continuity
plan: 01
status: complete
started: 2026-04-21
completed: 2026-04-21
---

# Plan 23-01: Worktree Lifecycle Cleanup

## What Was Built

- **Error-propagating worktree removal** (`cmd/codex_build_worktree.go`):
  - `removeGitWorktree()` returns detailed error with all git command outputs
  - `finalizeBuildWorktree()` marks failed removals as `WorktreeOrphaned`

- **Pre-allocation path validation** (`cmd/codex_build_worktree.go`):
  - `allocateBuildWorktree()` cleans existing paths before creation
  - Branch name collision resolved with counter suffix
  - Git errors include output for debugging

- **Deferred cleanup** (`cmd/codex_build.go`, `cmd/codex_build_worktree.go`):
  - `cleanupBuildWorktrees()` removes unfinalized worktree entries
  - Deferred in `runCodexBuild()` for panic/error scenarios
  - Merged entries removed from state

- **Orphan branch reporting** — detects and reports orphaned worktree branches

## Files Modified

- `cmd/codex_build_worktree.go` — error propagation, path validation
- `cmd/codex_build_worktree_test.go` — lifecycle tests
- `cmd/codex_build.go` — deferred cleanup integration

## Verification

- All 2900+ tests pass
- `go vet` clean
- Binary builds successfully

---

# Plan 23-02: Model Slot System and Agent Resilience

## What Was Built

- **Model slot system**:
  - `model-slot` subcommand with get/list/validate verbs
  - `getModelSlotForCaste()` and `validateSlot()` functions
  - `cli_model_override` injection into worker prompts
  - Build playbooks validate `--model` against slot names

- **Agent resilience sections**:
  - Added resilience sections to 24 agents across all risk tiers
  - HIGH: queen, builder, watcher
  - MEDIUM: 4 agents
  - LOW: 9 read-only agents
  - Surveyor: 4 agents
  - Commands: init, build, seal, entomb, colonize, lay-eggs

- **Spawn gates and post-action validation** in all 6 worker specs

## Files Modified

- `cmd/model_slot.go` — new subcommand
- `.aether/agents-claude/*.md` — model slot frontmatter
- `.claude/agents/ant/*.md` — resilience sections
- `.aether/workers.md` — spawn gates
- Build/continue playbooks — model validation

## Verification

- All 2900+ tests pass
- `go vet` clean
- Binary builds successfully
