---
phase: 03-pheromone-signal-plumbing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/resume.md
  - .aether/agents-claude/aether-keeper.md
autonomous: true
requirements:
  - PHER-07

must_haves:
  truths:
    - "After /clear, running /ant:resume shows the user's actual pheromone signals from pheromones.json"
    - "Resumed signals display decay-aware state (effective strength, not raw stored values)"
    - "The resume command does not claim pheromones have no decay"
  artifacts:
    - path: ".claude/commands/ant/resume.md"
      provides: "Resume command reading pheromones.json via pheromone-display"
      contains: "pheromone-display"
    - path: ".aether/agents-claude/aether-keeper.md"
      provides: "Keeper agent mirror stays in sync with .claude/agents/ant/ policy"
  key_links:
    - from: ".claude/commands/ant/resume.md"
      to: ".aether/aether-utils.sh pheromone-display"
      via: "bash command call in Step 3"
      pattern: "pheromone-display"
    - from: ".claude/commands/ant/resume.md"
      to: ".aether/data/pheromones.json"
      via: "pheromone-display reads pheromones.json internally"
      pattern: "pheromones"
---

<objective>
Fix /ant:resume to read pheromone signals from pheromones.json instead of the legacy constraints.json, ensuring pheromones survive /clear and display correctly on resume.

Purpose: Currently when a user runs /ant:resume after /clear, Step 3 reads constraints.json (the legacy signal store) instead of pheromones.json (the actual source of truth). It also incorrectly states "Pheromones persist until explicitly cleared -- no decay" when signals DO decay. This means the user never sees their actual pheromone signals after resuming a session, which is a direct blocker for PHER-07 (session persistence).

Output: Updated resume.md that displays real pheromone signals with decay-aware state.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pheromone-signal-plumbing/03-RESEARCH.md
@.claude/commands/ant/resume.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update resume.md Step 3 to read pheromones.json via pheromone-display</name>
  <files>.claude/commands/ant/resume.md</files>
  <action>
Edit `.claude/commands/ant/resume.md` Step 3 ("Read Pheromone Signals"). Currently it reads:

```
### Step 3: Read Pheromone Signals

Use the Read tool to read `.aether/data/constraints.json`.

Extract the following top-level keys:
- `focus` array — active focus signals (if key missing, treat as empty array)
- `constraints` array — active redirect/constraint signals (if key missing, treat as empty array)

If the file is missing: skip silently (no pheromones active).

Pheromones persist until explicitly cleared — no decay.
```

Replace the entire Step 3 with:

```
### Step 3: Read Pheromone Signals

Run using the Bash tool with description "Loading active pheromone signals...":
```bash
bash .aether/aether-utils.sh pheromone-read all
```

Parse the JSON result. Extract `.result.signals` array.

- If `ok` is `true` and `.result.signals` is non-empty: store signals for dashboard rendering in Step 8
- If `ok` is `true` and `.result.signals` is empty: no active pheromones (skip in dashboard)
- If the command fails or returns an error: skip silently (no pheromones active)

Note: pheromone-read applies decay calculation automatically. The `effective_strength` field reflects current signal strength after time-based decay. Signals below 0.1 effective strength are already filtered out.
```

Also update Step 8 ("Render Dashboard") where signals are displayed. Currently it renders from focus/constraints arrays:

```
{if focus array or constraints array is not empty:}
Active Signals:
{for each focus signal:}
  FOCUS: {focus text}
{end}
{for each constraint signal:}
  REDIRECT: {constraint text}
{end}
```

Replace this section with:

```
{if signals array from Step 3 is not empty:}
Active Signals:
{for each signal in signals:}
  {signal.type}: "{signal.content}" [{signal.effective_strength * 100 | floor}%]
{end}
{end}
```

Also update the Error Handling Reference table:
- Change `constraints.json missing | Skip silently (no pheromones)` to `pheromone-read fails | Skip silently (no pheromones)`

Also update the Key Constraints section:
- Remove the line `Use Read tool for COLONY_STATE.json and constraints.json (not bash cat/jq)`
- Replace with `Use Read tool for COLONY_STATE.json (not bash cat/jq). Use Bash tool for pheromone-read (applies decay calculation).`

Do NOT change any other steps (1, 2, 4-10). Only modify Step 3, the relevant dashboard rendering in Step 8, the error handling reference, and the key constraints section.
  </action>
  <verify>
Verify the file no longer references constraints.json for pheromone reading:
```bash
grep -n "constraints.json" .claude/commands/ant/resume.md
```
This should return zero matches. Also verify pheromone-read is now referenced:
```bash
grep -n "pheromone-read" .claude/commands/ant/resume.md
```
This should return at least 1 match.

Also verify the false "no decay" claim is removed:
```bash
grep -n "no decay" .claude/commands/ant/resume.md
```
This should return zero matches.
  </verify>
  <done>resume.md Step 3 calls pheromone-read instead of reading constraints.json. Step 8 renders signals with type, content, and effective_strength percentage. The false claim "Pheromones persist until explicitly cleared -- no decay" is removed. Error handling and key constraints updated consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Sync resume.md changes to agent mirror and verify lint</name>
  <files>.aether/agents-claude/aether-keeper.md</files>
  <action>
Per the project's sync policy (CLAUDE.md: "Agent mirror (.aether/agents-claude/) must stay in sync with .claude/agents/ant/"), after modifying a command in .claude/commands/ant/, verify whether .aether/agents-claude/ has a corresponding file that needs updating.

In this case, resume.md is in `.claude/commands/ant/` (a command, not an agent), so the agent mirror may not contain it. Check:
```bash
ls .aether/agents-claude/ | grep resume
```

If no resume file exists in the agent mirror, no sync is needed.

Run the sync lint to verify no desync was introduced:
```bash
npm run lint:sync
```

If lint:sync fails, investigate and fix the desync. The most common cause would be if editing resume.md somehow affected an agent file referenced by the sync checker.
  </action>
  <verify>
```bash
npm run lint:sync
npm test
```
Both commands must pass without errors.
  </verify>
  <done>Sync lint passes confirming no agent mirror desync. All existing tests pass confirming no regressions from the resume.md edit.</done>
</task>

</tasks>

<verification>
1. `grep -c "constraints.json" .claude/commands/ant/resume.md` returns 0
2. `grep -c "pheromone-read" .claude/commands/ant/resume.md` returns >= 1
3. `grep -c "no decay" .claude/commands/ant/resume.md` returns 0
4. `npm run lint:sync` passes
5. `npm test` passes
</verification>

<success_criteria>
- /ant:resume Step 3 reads pheromones.json (via pheromone-read), not constraints.json
- Signal display in the resume dashboard shows type, content, and decay-aware effective_strength
- The incorrect "no decay" claim is removed
- Sync lint passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-pheromone-signal-plumbing/03-02-SUMMARY.md`
</output>
