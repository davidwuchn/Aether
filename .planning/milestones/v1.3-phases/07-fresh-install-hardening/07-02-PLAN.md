---
phase: 07-fresh-install-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/generate_cmds.go
  - cmd/build_flow_cmds.go
  - cmd/generate_cmds_test.go
  - cmd/build_flow_cmds_test.go
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-04
  - CMD-05
  - CMD-06
  - CMD-07
  - CMD-08
  - DIFF-01

must_haves:
  truths:
    - "aether generate-ant-name produces a random name like 'Hammer-42' for any supported caste"
    - "aether generate-commit-message produces a template-based commit message from git diff output"
    - "aether generate-progress-bar renders a terminal progress bar string"
    - "aether version-check-cached displays the current version with caching"
    - "aether milestone-detect returns the current milestone from colony state"
    - "aether update-progress updates phase progress in colony state"
    - "aether print-next-up displays next steps after phase completion"
    - "aether data-safety-stats returns data integrity statistics"
  artifacts:
    - path: "cmd/generate_cmds.go"
      provides: "generate-ant-name, generate-commit-message, generate-progress-bar, generate-threshold-bar commands"
      exports: ["generateAntNameCmd", "generateCommitMessageCmd", "generateProgressBarCmd"]
    - path: "cmd/build_flow_cmds.go"
      provides: "version-check-cached, milestone-detect, update-progress, print-next-up, data-safety-stats commands"
      exports: ["versionCheckCachedCmd", "milestoneDetectCmd", "updateProgressCmd", "printNextUpCmd", "dataSafetyStatsCmd"]
  key_links:
    - from: "cmd/generate_cmds.go"
      to: "random number generation"
      via: "math/rand for name/number selection"
      pattern: "rand\\."
    - from: "cmd/build_flow_cmds.go"
      to: "pkg/colony/colony.go"
      via: "ColonyState loading for milestone/progress"
      pattern: "colony\\.ColonyState"
---

<objective>
Port the core build-flow utility commands from shell to Go -- name generation, commit messages, progress bars, version caching, milestone detection, progress updates, next-step display, and data safety stats.

Purpose: These commands are invoked by every slash command and playbook. generate-ant-name is called 11 times across slash commands for worker spawning. midden-write and update-progress are called on every build cycle. Without these, the wiring phases cannot proceed.
Output: 2 new Go source files with 8 registered commands.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@cmd/helpers.go
@cmd/state_cmds.go
@cmd/version.go
@pkg/colony/colony.go
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From cmd/version.go:
```go
var Version = "0.0.0-dev"
// Used by rootCmd.Version
```

From cmd/helpers.go:
```go
func outputOK(result interface{})
func outputError(code int, message string, details interface{})
func mustGetString(cmd *cobra.Command, flag string) string
func mustGetInt(cmd *cobra.Command, flag string) int
```

From pkg/colony/colony.go:
```go
type ColonyState struct {
    CurrentPhase int     `json:"current_phase"`
    Milestone    string  `json:"milestone"`
    Plan         Plan    `json:"plan"`
    // ... other fields
}

type Plan struct {
    Phases []Phase `json:"phases"`
}

type Phase struct {
    ID     int    `json:"id"`
    Name   string `json:"name"`
    Status string `json:"status"`
}
```

From pkg/storage/:
```go
func (s *Store) LoadJSON(filename string, v interface{}) error
func (s *Store) SaveJSON(filename string, v interface{}) error
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port generate-ant-name, generate-commit-message, generate-progress-bar to Go</name>
  <files>
    cmd/generate_cmds.go
    cmd/generate_cmds_test.go
  </files>
  <read_first>
    cmd/generate_cmds.go (may not exist yet)
    cmd/helpers.go
    .aether/aether-utils.sh (lines 2209-2241 for generate-ant-name)
    .aether/aether-utils.sh (lines 2242-2280 for generate-commit-message approximately -- search for it)
    .aether/aether-utils.sh (lines 358-397 for generate-progress-bar)
  </read_first>
  <acceptance_criteria>
    - cmd/generate_cmds.go contains `var generateAntNameCmd`
    - cmd/generate_cmds.go contains `var generateCommitMessageCmd`
    - cmd/generate_cmds.go contains `var generateProgressBarCmd`
    - cmd/generate_cmds.go registers all commands via `rootCmd.AddCommand`
    - cmd/generate_cmds.go defines caste prefixes map with at least 20 caste entries (builder, watcher, scout, colonizer, architect, prime, chaos, archaeologist, oracle, ambassador, auditor, chronicler, gatekeeper, guardian, includer, keeper, measurer, probe, tracker, weaver)
    - `go build ./cmd/aether` exits 0
    - Test file exists at cmd/generate_cmds_test.go with TestGenerateAntName test function
  </acceptance_criteria>
  <action>
Create cmd/generate_cmds.go implementing 3 commands.

**generate-ant-name command:**
- Use: "generate-ant-name [caste]", Short: "Generate a random ant name for the given caste"
- Args: cobra.MaximumNArgs(1), default caste "builder"
- Implementation (reproducing shell logic from lines 2209-2241):
  1. Define a map[string][]string of caste prefixes exactly matching the shell version:
     - builder: Chip, Hammer, Forge, Mason, Brick, Anvil, Weld, Bolt
     - watcher: Vigil, Sentinel, Guard, Keen, Sharp, Hawk, Watch, Alert
     - scout: Swift, Dash, Ranger, Track, Seek, Path, Roam, Quest
     - colonizer: Pioneer, Map, Chart, Venture, Explore, Compass, Atlas, Trek
     - architect: Blueprint, Draft, Design, Plan, Schema, Frame, Sketch, Model
     - prime: Prime, Alpha, Lead, Chief, First, Core, Apex, Crown
     - chaos: Probe, Stress, Shake, Twist, Snap, Breach, Surge, Jolt
     - archaeologist: Relic, Fossil, Dig, Shard, Epoch, Strata, Lore, Glyph
     - oracle: Sage, Seer, Vision, Augur, Mystic, Sibyl, Delph, Pythia
     - ambassador: Bridge, Connect, Link, Diplomat, Protocol, Network, Port, Socket
     - auditor: Review, Inspect, Exam, Scrutin, Verify, Check, Audit, Assess
     - chronicler: Record, Write, Document, Chronicle, Scribe, Archive, Script, Ledger
     - gatekeeper: Guard, Protect, Secure, Shield, Defend, Bar, Gate, Checkpoint
     - guardian: Defend, Patrol, Watch, Vigil, Shield, Guard, Armor, Fort
     - includer: Access, Include, Open, Welcome, Reach, Universal, Equal, A11y
     - keeper: Archive, Store, Curate, Preserve, Guard, Keep, Hold, Save
     - measurer: Metric, Gauge, Scale, Measure, Benchmark, Track, Count, Meter
     - probe: Test, Probe, Excavat, Uncover, Edge, Mutant, Trial, Check
     - tracker: Track, Trace, Debug, Hunt, Follow, Trail, Find, Seek
     - weaver: Weave, Knit, Spin, Twine, Transform, Mend, Thread, Stitch
     - default: Ant, Worker, Drone, Toiler, Marcher, Runner, Carrier, Helper
  2. Pick random prefix from the caste's array using rand.Intn(len(prefixes))
  3. Generate random number 1-99 using rand.Intn(99) + 1
  4. Format as "{prefix}-{number}"
  5. outputOK with the name string
- Flags: --seed (optional int) -- if provided, seed the random generator for deterministic output (per DIFF-01)

**generate-commit-message command:**
- Use: "generate-commit-message", Short: "Generate a template-based commit message"
- Flags: --type (required, one of: feat, fix, docs, refactor, test, chore), --scope (optional), --subject (required), --body (optional)
- Implementation:
  1. Validate --type against allowed values
  2. Format as "{type}({scope}): {subject}" or "{type}: {subject}" if no scope
  3. If --body provided, append blank line then body
  4. outputOK with the formatted message string

**generate-progress-bar command:**
- Use: "generate-progress-bar", Short: "Render a terminal progress bar"
- Flags: --current (required int), --total (required int), --width (optional int, default 30)
- Implementation:
  1. Calculate percentage: current * 100 / total (clamp 0-100)
  2. Calculate filled: width * current / total
  3. Build bar string: "[" + "#" * filled + "-" * (width - filled) + "]"
  4. outputOK with {bar, percentage, current, total}

Register all 3 commands in init() via rootCmd.AddCommand.

Create cmd/generate_cmds_test.go with:
- TestGenerateAntNameBuilder: generates name matching "^[A-Z][a-z]+-\\d{1,2}$"
- TestGenerateAntNameAllCastes: each caste produces a valid name
- TestGenerateAntNameWithSeed: deterministic output for same seed (per DIFF-01)
- TestGenerateCommitMessage: produces correct format
- TestGenerateProgressBar: produces correct bar string
  </action>
  <verify>
    <automated>go test ./cmd/... -run "TestGenerate" -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    - cmd/generate_cmds.go exists with 3 registered commands
    - generate-ant-name produces caste-specific names matching shell format
    - All generate command tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Port version-check-cached, milestone-detect, update-progress, print-next-up, data-safety-stats to Go</name>
  <files>
    cmd/build_flow_cmds.go
    cmd/build_flow_cmds_test.go
  </files>
  <read_first>
    cmd/build_flow_cmds.go (may not exist yet)
    cmd/helpers.go
    cmd/state_cmds.go
    cmd/version.go
    pkg/colony/colony.go
    .aether/aether-utils.sh (search for "version-check-cached", "milestone-detect", "update-progress", "print-next-up", "data-safety-stats")
  </read_first>
  <acceptance_criteria>
    - cmd/build_flow_cmds.go contains `var versionCheckCachedCmd`
    - cmd/build_flow_cmds.go contains `var milestoneDetectCmd`
    - cmd/build_flow_cmds.go contains `var updateProgressCmd`
    - cmd/build_flow_cmds.go contains `var printNextUpCmd`
    - cmd/build_flow_cmds.go contains `var dataSafetyStatsCmd`
    - cmd/build_flow_cmds.go registers all 5 commands via `rootCmd.AddCommand`
    - `go build ./cmd/aether` exits 0
    - cmd/build_flow_cmds_test.go exists with tests for each command
  </acceptance_criteria>
  <action>
Create cmd/build_flow_cmds.go implementing 5 commands.

**version-check-cached command:**
- Use: "version-check-cached", Short: "Display version with caching"
- Flags: none
- Implementation:
  1. Check for cache file at .aether/data/.version-check-cache
  2. If cache exists and is less than 24 hours old, return cached version
  3. Otherwise, read current Version from the binary (use the Version variable from cmd/version.go)
  4. Write cache file with current version and timestamp
  5. outputOK with {version, cached, cached_at}

**milestone-detect command:**
- Use: "milestone-detect", Short: "Detect current milestone from colony state"
- Implementation:
  1. Load COLONY_STATE.json into colony.ColonyState
  2. Read state.Milestone field
  3. If milestone is empty, attempt to detect from plan phases (count completed phases)
  4. outputOK with {milestone, phase_count, completed_count}

**update-progress command:**
- Use: "update-progress", Short: "Update phase progress in colony state"
- Flags: --phase (required int), --status (required, one of: pending, ready, in_progress, completed), --task (optional string for task-level update)
- Implementation:
  1. Load COLONY_STATE.json
  2. Find the phase by --phase number (0-indexed: phase 1 = index 0)
  3. If --task provided, find the task and update its status
  4. Otherwise update the phase status
  5. Save COLONY_STATE.json
  6. outputOK with {updated:true, phase, status}

**print-next-up command:**
- Use: "print-next-up", Short: "Display next steps based on colony state"
- Implementation:
  1. Load COLONY_STATE.json
  2. Read current_phase and state
  3. Generate contextual suggestions based on state:
     - READY: "Run /ant:build {next_phase} to start the next phase"
     - EXECUTING: "Run /ant:continue to verify work and advance"
     - BUILT: "Run /ant:continue to verify and advance"
     - COMPLETED: "Colony complete! Run /ant:seal to finalize"
  4. outputOK with {suggestions, current_phase, state}

**data-safety-stats command:**
- Use: "data-safety-stats", Short: "Display data integrity statistics"
- Implementation:
  1. Check .aether/data/ directory exists
  2. Count files, check for locks, check COLONY_STATE.json size
  3. Check pheromones.json, session.json existence and sizes
  4. outputOK with {data_dir, file_count, state_exists, state_size_bytes, pheromones_count, session_active, lock_files_count}

Register all 5 commands in init() via rootCmd.AddCommand.

Create cmd/build_flow_cmds_test.go with basic tests for each command.
  </action>
  <verify>
    <automated>go test ./cmd/... -run "TestVersionCheck|TestMilestoneDetect|TestUpdateProgress|TestPrintNextUp|TestDataSafety" -v -count=1 2>&1 | tail -30</automated>
  </verify>
  <done>
    - cmd/build_flow_cmds.go exists with 5 registered commands
    - `go build ./cmd/aether` succeeds
    - All build flow tests pass
  </done>
</task>

</tasks>

<verification>
- `go build ./cmd/aether` compiles without errors
- `go test ./cmd/... -count=1` passes (no regressions)
- `go run ./cmd/aether generate-ant-name builder` returns a name like "Hammer-42"
- `go run ./cmd/aether generate-commit-message --type feat --subject "test"` returns formatted message
- `go run ./cmd/aether generate-progress-bar --current 5 --total 10` returns 50% bar
- `go run ./cmd/aether version-check-cached` returns version info
- Command count increased by 8 from this plan
</verification>

<success_criteria>
- 8 new commands registered in Go binary (3 generate + 5 build-flow)
- generate-ant-name produces names matching the shell format exactly
- All commands parse arguments correctly (no panics on --help or with valid args)
- All new tests pass, no regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/07-fresh-install-hardening/07-02-SUMMARY.md`
</output>
