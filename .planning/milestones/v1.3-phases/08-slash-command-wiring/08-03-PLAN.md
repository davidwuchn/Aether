---
phase: 08
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .claude/commands/ant/*.md
  - .opencode/commands/ant/*.md
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02
  - WIRE-03

must_haves:
  truths:
    - "All 45 generated Claude .md files contain aether calls, not shell dispatcher calls"
    - "All 45 generated OpenCode .md files contain aether calls, not shell dispatcher calls"
    - "Generated files match YAML sources (generator check passes)"
    - "The only shell fallback is for normalize-args in the OpenCode preamble"
  artifacts:
    - path: ".claude/commands/ant/status.md"
      provides: "Generated Claude status command"
      pattern: "Generated from .aether/commands/status.yaml"
    - path: ".opencode/commands/ant/status.md"
      provides: "Generated OpenCode status command"
      contains: "aether normalize-args"
  key_links:
    - from: ".aether/commands/*.yaml"
      to: ".claude/commands/ant/*.md"
      via: "node bin/generate-commands.js"
      pattern: "aether milestone-detect"
    - from: ".aether/commands/*.yaml"
      to: ".opencode/commands/ant/*.md"
      via: "node bin/generate-commands.js"
      pattern: "aether normalize-args"
---

<objective>
Regenerate all 90 command .md files from the updated YAML sources and verify complete wiring.

Purpose: The YAML updates from Plan 02 only take effect when the generator runs. This plan regenerates all command files and validates that zero shell invocations remain in the generated output.

Output: 45 Claude .md files and 45 OpenCode .md files, all calling the Go binary.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/08-slash-command-wiring/08-RESEARCH.md
@.planning/milestones/v1.3-phases/08-slash-command-wiring/08-01-SUMMARY.md
@.planning/milestones/v1.3-phases/08-slash-command-wiring/08-02-SUMMARY.md
@bin/generate-commands.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate all command files and verify wiring</name>
  <files>.claude/commands/ant/*.md, .opencode/commands/ant/*.md</files>
  <read_first>
    bin/generate-commands.js
  </read_first>
  <acceptance_criteria>
    - `node bin/generate-commands.js --check` exits with code 0
    - `ls .claude/commands/ant/*.md | wc -l` returns 45
    - `ls .opencode/commands/ant/*.md | wc -l` returns 45
    - `grep -r 'bash .aether/aether-utils.sh' .claude/commands/ant/*.md | grep -v normalize-args | wc -l` returns 0
    - `grep -r 'bash .aether/aether-utils.sh' .opencode/commands/ant/*.md | grep -v normalize-args | wc -l` returns 0
    - `grep -r 'bash .aether/aether-utils.sh normalize-args' .opencode/commands/ant/*.md | wc -l` returns 45 (one per OpenCode file, the fallback)
    - `grep -r 'aether pheromone-write' .claude/commands/ant/focus.md` returns a match
    - `grep -r 'aether milestone-detect' .claude/commands/ant/status.md` returns a match
  </action>
  <action>
    **Step 1: Regenerate all command files**

    Run the generator to produce all .md files from updated YAML sources:
    ```bash
    node bin/generate-commands.js
    ```

    This reads all 45 YAML files from `.aether/commands/` and writes 45 Claude .md files and 45 OpenCode .md files. Expect output: "Generated 90 command files from 45 YAML sources."

    **Step 2: Verify generator check passes**

    Run the check mode to confirm generated files match YAML sources:
    ```bash
    node bin/generate-commands.js --check
    ```

    Must exit with code 0 (no mismatches).

    **Step 3: Count generated files**

    Verify 45 Claude and 45 OpenCode files exist:
    ```bash
    ls .claude/commands/ant/*.md | wc -l
    ls .opencode/commands/ant/*.md | wc -l
    ```

    Both must return 45.

    **Step 4: Verify zero shell dispatcher calls in Claude files**

    Claude files should have NO shell dispatcher calls at all (they do not use the normalize-args preamble):
    ```bash
    grep -c 'bash .aether/aether-utils.sh' .claude/commands/ant/*.md
    ```

    Every file must show 0 occurrences.

    **Step 5: Verify only normalize-args fallback in OpenCode files**

    OpenCode files should have exactly ONE shell invocation per file -- the normalize-args fallback in the preamble:
    ```bash
    grep 'bash .aether/aether-utils.sh' .opencode/commands/ant/*.md
    ```

    All matches must be in the normalize-args fallback line only. Count should be exactly 45 (one per file).

    **Step 6: Spot-check key commands**

    Verify specific high-traffic commands contain Go binary calls:
    ```bash
    grep 'aether pheromone-write' .claude/commands/ant/focus.md
    grep 'aether milestone-detect' .claude/commands/ant/status.md
    grep 'aether flag-list' .claude/commands/ant/flags.md
    grep 'aether generate-progress-bar' .claude/commands/ant/status.md
    ```

    Each must show the `aether` command invocation.

    **Step 7: Verify --json flag usage**

    Confirm that table-output commands use --json when piped to jq:
    ```bash
    grep 'flag-list.*--json' .claude/commands/ant/*.md .opencode/commands/ant/*.md
    ```

    Must show --json in any file that pipes flag-list output through jq.
  </action>
  <verify>
    <automated>node bin/generate-commands.js --check && echo "Generator check passed" && grep -c 'bash .aether/aether-utils.sh' .claude/commands/ant/*.md | grep -v ':0$' && echo "FAIL: Claude files still have shell calls" || echo "Claude files clean"</automated>
  </verify>
  <done>90 generated .md files (45 Claude + 45 OpenCode) all call the Go binary. Generator check passes. Zero shell dispatcher calls in Claude files. Only normalize-args shell fallback in OpenCode preamble. --json flags present where needed for jq pipelines.</done>
</task>

</tasks>

<verification>
1. `node bin/generate-commands.js --check` exits 0
2. `grep -c 'bash .aether/aether-utils.sh' .claude/commands/ant/*.md | grep -v ':0$'` returns nothing (zero shell calls in Claude files)
3. `grep -c 'bash .aether/aether-utils.sh' .opencode/commands/ant/*.md | grep -v ':0$'` shows exactly 45 files with exactly 1 occurrence each (normalize-args fallback only)
4. `grep -r 'aether ' .claude/commands/ant/*.md | wc -l` shows Go binary invocations present
5. `go test ./cmd/ -count=1 -timeout 60s` still passes (no regressions)
</verification>

<success_criteria>
All 90 generated command files use `aether <cmd>` invocations. Generator --check passes. Zero non-fallback shell calls in Claude files. Only normalize-args shell fallback in OpenCode files. Phase 08 wiring is complete -- slash commands call Go binary.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/08-slash-command-wiring/08-03-SUMMARY.md`
</output>
