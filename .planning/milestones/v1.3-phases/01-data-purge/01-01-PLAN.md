---
phase: 01-data-purge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/QUEEN.md
  - .aether/data/pheromones.json
  - .aether/data/constraints.json
  - .aether/data/COLONY_STATE.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "QUEEN.md contains exactly 5 wisdom entries: colony-a philosophy, colony-b pattern, colony-c redirect, colony-d stack wisdom, colony-e decree"
    - "pheromones.json contains zero test signals -- no entries with text matching 'test signal', 'demo focus', 'sanity signal', 'test area', or 'test area for pheromone unification'"
    - "constraints.json focus array is empty -- all 5 test entries removed"
    - "COLONY_STATE.json has no stale Electron-to-Xcode goal, no TestAnt6 learning, no test error record"
  artifacts:
    - path: ".aether/QUEEN.md"
      provides: "Clean colony wisdom with only 5 canonical seed entries"
      contains: "colony-a"
    - path: ".aether/data/pheromones.json"
      provides: "Clean pheromone signals with only real entries"
      contains: "sig_feedback_001"
    - path: ".aether/data/constraints.json"
      provides: "Clean constraints with empty focus array and 3 real constraints"
      contains: "c_xml_001"
    - path: ".aether/data/COLONY_STATE.json"
      provides: "Clean colony state ready for new project"
      contains: "\"goal\": \"\""
  key_links:
    - from: ".aether/QUEEN.md"
      to: "QUEEN.md metadata stats"
      via: "METADATA HTML comment block"
      pattern: "total_patterns.*1"
---

<objective>
Purge all test artifacts from the four primary colony state files: QUEEN.md, pheromones.json, constraints.json, and COLONY_STATE.json.

Purpose: Establish a clean baseline so pheromone integration (Phase 3+) can be validated against real data, not polluted by test noise.
Output: Four clean state files containing only real, meaningful data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.aether/QUEEN.md
@.aether/data/pheromones.json
@.aether/data/constraints.json
@.aether/data/COLONY_STATE.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge test entries from QUEEN.md</name>
  <files>.aether/QUEEN.md</files>
  <action>
Rewrite QUEEN.md to contain ONLY the 5 canonical seed entries. Use the Write tool to replace the entire file.

KEEP these 5 entries (exact content):
1. Philosophies section: colony-a (2026-02-15T13:08:24Z): Test-driven development ensures quality
2. Patterns section: colony-b (2026-02-15T13:08:28Z): Always validate inputs
3. Redirects section: colony-c (2026-02-15T13:08:31Z): Never skip security checks
4. Stack Wisdom section: colony-d (2026-02-15T13:08:36Z): Use jq for JSON in bash
5. Decrees section: colony-e (2026-02-15T13:08:40Z): All code must have tests

REMOVE everything else:
- All "test-colony" entries from Patterns (25+ entries with content like "Test pattern", "Failure pattern to learn", "Wisdom to be primed", "Test pattern for promotion")
- All "colony" and "colony-1" entries from Patterns ("Test", "End-to-end test observation 1")
- All "test-colony" entries from Decrees (4 entries with "Immediate decree test")
- All test entries from Evolution Log (28+ entries from test-colony, colony, colony-1) -- keep only the 5 seed promotion entries (colony-a through colony-e) and the system initialized entry

Update the METADATA HTML comment block at the bottom:
- Set "colonies_contributed" to ["colony-a", "colony-b", "colony-c", "colony-d", "colony-e"]
- Set stats: total_philosophies: 1, total_patterns: 1, total_redirects: 1, total_stack_entries: 1, total_decrees: 1
- Set "last_evolved" to "2026-02-15T13:08:40Z" (the last seed entry timestamp)
- Remove the evolution_log JSON array from metadata (it duplicates the markdown table)

Preserve the overall markdown structure: header, each section with its emoji heading and description, the Evolution Log table, and the METADATA comment.
  </action>
  <verify>
Run these checks:
- grep -c "test-colony" .aether/QUEEN.md should return 0
- grep -c "colony-1" .aether/QUEEN.md should return 0
- grep -c "colony-a" .aether/QUEEN.md should return at least 2 (section + evolution log)
- grep -c "colony-b" .aether/QUEEN.md should return at least 2
- grep -c "colony-c" .aether/QUEEN.md should return at least 2
- grep -c "colony-d" .aether/QUEEN.md should return at least 2
- grep -c "colony-e" .aether/QUEEN.md should return at least 2
- grep "total_patterns" .aether/QUEEN.md should show 1
  </verify>
  <done>QUEEN.md contains exactly 5 wisdom entries (one per section) plus the 6 seed evolution log rows (5 promotions + 1 system initialized). Zero test entries remain.</done>
</task>

<task type="auto">
  <name>Task 2: Clean pheromones.json, constraints.json, and COLONY_STATE.json</name>
  <files>.aether/data/pheromones.json, .aether/data/constraints.json, .aether/data/COLONY_STATE.json</files>
  <action>
**pheromones.json** -- Remove 5 test signals, keep 3 real signals:

KEEP these signals (by id):
1. sig_feedback_001 -- real coverage feedback from worker_builder (active: true)
2. sig_redirect_001 -- real REDIRECT about runtime edits from system (active: true, though expired date -- keep as-is)
3. sig_redirect_1771366398000 -- real user REDIRECT "avoid direct runtime edits" (active: false)

REMOVE these signals (by id):
1. sig_focus_001 -- "XML migration" (expired, test-era)
2. sig_focus_1771366307000 -- "test area for pheromone unification" (reason: "testing pheromone-write")
3. sig_focus_1771366386000 -- "test area" (reason: "test")
4. sig_focus_1771379303000 -- "test signal"
5. sig_focus_1771762818000 -- "demo focus"
6. sig_focus_1771763253000 -- "sanity signal"
7. sig_feedback_1771366398000 -- "keep commits small" (test-era feedback, active: false)

Keep version and colony_id fields unchanged.

**constraints.json** -- Empty the focus array, keep everything else:

Set "focus" to an empty array [].
Keep the "constraints" array with its 3 entries (c_xml_001, c_council_1771173025, c_1771366398) unchanged.
Keep the "version" field unchanged.

**COLONY_STATE.json** -- Reset to clean state:

- Set "goal" to "" (empty string)
- Set "state" to "IDLE"
- Set "current_phase" to 0
- Keep "session_id" and "initialized_at" unchanged (they document when this state file was created)
- Set "memory.phase_learnings" to [] (removes TestAnt6 learning)
- Keep "memory.decisions" and "memory.instincts" as empty arrays
- Set "errors.records" to [] (removes test error)
- Keep "errors.flagged_patterns" as empty array
- Keep "signals", "graveyards", "events" as-is (events has real initialization record)
- Update "last_updated" to current ISO timestamp
  </action>
  <verify>
Run these checks:
- jq '.signals | length' .aether/data/pheromones.json should return 3
- jq '.signals[].content.text' .aether/data/pheromones.json should show only "Test coverage...", "Avoid editing runtime...", and "avoid direct runtime edits"
- jq '.focus | length' .aether/data/constraints.json should return 0
- jq '.constraints | length' .aether/data/constraints.json should return 3
- jq '.goal' .aether/data/COLONY_STATE.json should return ""
- jq '.memory.phase_learnings | length' .aether/data/COLONY_STATE.json should return 0
- jq '.errors.records | length' .aether/data/COLONY_STATE.json should return 0
  </verify>
  <done>pheromones.json has exactly 3 real signals (zero test signals). constraints.json has empty focus array and 3 real constraints. COLONY_STATE.json has empty goal, no stale learnings, no test errors.</done>
</task>

</tasks>

<verification>
After both tasks, run a comprehensive check:
1. grep -c "test-colony" .aether/QUEEN.md returns 0
2. grep -c "colony-1" .aether/QUEEN.md returns 0
3. jq '[.signals[].content.text] | map(select(test("test|demo|sanity"; "i"))) | length' .aether/data/pheromones.json returns 0
4. jq '.focus | length' .aether/data/constraints.json returns 0
5. jq '.goal' .aether/data/COLONY_STATE.json returns ""
6. npm test passes (changes to data files should not break tests since tests use their own fixtures)
</verification>

<success_criteria>
- QUEEN.md has exactly 5 entries total (1 philosophy, 1 pattern, 1 redirect, 1 stack wisdom, 1 decree)
- pheromones.json has exactly 3 signals (zero test signals)
- constraints.json focus array is empty, constraints array has 3 entries
- COLONY_STATE.json goal is empty string, no test learnings or errors
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-purge/01-01-SUMMARY.md`
</output>
