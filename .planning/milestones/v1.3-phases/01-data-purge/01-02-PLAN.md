---
phase: 01-data-purge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/data/learning-observations.json
  - .aether/data/spawn-tree.txt
  - .aether/data/midden/midden.json
autonomous: true
requirements:
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "learning-observations.json contains zero synthetic test data entries"
    - "spawn-tree.txt contains no test worker entries (no TestAnt6, Test-Worker, or test-worker lines)"
    - "midden.json contains no test entries and no archived test pheromone signals"
  artifacts:
    - path: ".aether/data/learning-observations.json"
      provides: "Empty observations array ready for real data"
      contains: "\"observations\": []"
    - path: ".aether/data/spawn-tree.txt"
      provides: "Clean spawn history with only real worker entries"
    - path: ".aether/data/midden/midden.json"
      provides: "Clean midden with only real failure records"
      contains: "midden_1771717833_19573"
  key_links:
    - from: ".aether/data/learning-observations.json"
      to: "learning pipeline"
      via: "learning-observe subcommand reads this file"
      pattern: "\"observations\""
---

<objective>
Purge test artifacts from the three secondary colony data files: learning-observations.json, spawn-tree.txt, and midden.json.

Purpose: Complete the data purge by cleaning the learning pipeline input (observations), worker history (spawn tree), and failure tracking (midden) so they contain only real data.
Output: Three clean data files ready for real colony usage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.aether/data/learning-observations.json
@.aether/data/spawn-tree.txt
@.aether/data/midden/midden.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge learning-observations.json and spawn-tree.txt</name>
  <files>.aether/data/learning-observations.json, .aether/data/spawn-tree.txt</files>
  <action>
**learning-observations.json** -- The ENTIRE observations array is synthetic test data (11 entries from colonies named "test-colony", "different-colony", "alpha-colony", "beta-colony", "gamma-colony", "c1", "c2", "test"). All content is fake ("Always validate inputs", "Test new learning", "test", "same content", "Philosophy test low count", "Stack test single", "Decree test immediate", "Test observation", etc.).

Replace the observations array with an empty array. Keep the JSON structure:
```json
{
  "observations": []
}
```

**spawn-tree.txt** -- Remove test worker entries while keeping real worker spawns.

REMOVE these lines (test workers):
- Any line containing "TestAnt6" (line 20: "TestAnt6|completed|Task done. Learned: verify working directory")
- Any line containing "Test-Worker" (line 19: "Test-Worker|completed|Task finished. Learned: always validate JSON before writing")
- Any line containing "test-worker" in the worker name field (lines 21-22: "QUEEN|Builder|test-worker|test task" and "test-worker|success|")
- The line with "Bolt-99" which was a test task spawn (line 16-17: "Queen|builder|Bolt-99|Test task" and "Bolt-99|completed|Test summary")

KEEP all other lines -- the real worker spawns from legitimate colony operations:
- 4 surveyor spawns (Toiler-47, Worker-72, Toiler-72, Ant-56)
- 4 scout research spawns (Roam-64, Quest-14, Path-75, Roam-88)
- Phase completion records (Bolt-48, Anvil-71, Chip-43, Weld-66)
- Verification records (Sharp-33, Snap-27, Mason-16)
- Sharp-33 watcher spawn
  </action>
  <verify>
Run these checks:
- jq '.observations | length' .aether/data/learning-observations.json should return 0
- grep -c "TestAnt6" .aether/data/spawn-tree.txt should return 0
- grep -c "Test-Worker" .aether/data/spawn-tree.txt should return 0
- grep -c "test-worker" .aether/data/spawn-tree.txt should return 0
- grep -c "Test task" .aether/data/spawn-tree.txt should return 0
- grep -c "Test summary" .aether/data/spawn-tree.txt should return 0
- The remaining lines in spawn-tree.txt should contain only real worker names (no test artifacts)
  </verify>
  <done>learning-observations.json has an empty observations array. spawn-tree.txt contains only real worker spawn records (zero test entries).</done>
</task>

<task type="auto">
  <name>Task 2: Clean midden.json of test entries and archived test signals</name>
  <files>.aether/data/midden/midden.json</files>
  <action>
midden.json has two sections to clean: "signals" (archived pheromones) and "entries" (failure records).

**Archived signals** -- Remove all test signals from the signals array:

REMOVE these archived signals (all are test artifacts):
1. sig_focus_1771366307000 -- "test area for pheromone unification" (reason: "testing pheromone-write")
2. sig_focus_1771366386000 -- "test area" (reason: "test")
3. sig_redirect_1771366398000 -- "avoid direct runtime edits" (real content but was archived as test)
4. sig_feedback_1771366398000 -- "keep commits small" (test-era)
5. sig_focus_001 -- "XML migration and pheromone system implementation" (expired, archived)

Set signals to an empty array and archived_at_count to 0.

**Failure entries** -- Remove the test entry, keep the real one:

KEEP: midden_1771717833_19573 (category: "security", source: "gatekeeper", message: "High CVEs found: 3") -- this is a real finding from the gatekeeper agent.

REMOVE: midden_1771718201_26518 (category: "test", source: "test-source", message: "Test message for verification") -- explicitly a test entry.

Update entry_count to 1.
  </action>
  <verify>
Run these checks:
- jq '.signals | length' .aether/data/midden/midden.json should return 0
- jq '.archived_at_count' .aether/data/midden/midden.json should return 0
- jq '.entries | length' .aether/data/midden/midden.json should return 1
- jq '.entries[0].id' .aether/data/midden/midden.json should return "midden_1771717833_19573"
- jq '.entry_count' .aether/data/midden/midden.json should return 1
  </verify>
  <done>midden.json has zero archived test signals, exactly 1 real failure entry (the gatekeeper security finding), and correct counts.</done>
</task>

</tasks>

<verification>
After both tasks, run a comprehensive check:
1. jq '.observations | length' .aether/data/learning-observations.json returns 0
2. grep -c "TestAnt6\|Test-Worker\|test-worker\|Test task\|Test summary" .aether/data/spawn-tree.txt returns 0 (no test worker entries remain)
3. jq '.signals | length' .aether/data/midden/midden.json returns 0
4. jq '.entries | length' .aether/data/midden/midden.json returns 1
5. npm test passes (changes to data files should not break tests since tests use their own fixtures)
</verification>

<success_criteria>
- learning-observations.json has empty observations array (zero synthetic entries)
- spawn-tree.txt has only real worker entries (no TestAnt6, Test-Worker, test-worker, or test task entries)
- midden.json has zero archived test signals and exactly 1 real failure entry
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-purge/01-02-SUMMARY.md`
</output>
