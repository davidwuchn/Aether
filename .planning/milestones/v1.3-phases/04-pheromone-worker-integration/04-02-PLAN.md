---
phase: 04-pheromone-worker-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/integration/pheromone-worker-integration.test.js
autonomous: true
requirements: [PHER-04, PHER-05]

must_haves:
  truths:
    - "An auto-emitted REDIRECT signal from a failure event appears in the colony-prime prompt_section of a subsequent build context"
    - "Agent definitions contain explicit instructions to respond to the signal types present in prompt_section"
    - "When 3+ midden failures share a category, a REDIRECT signal with source auto:error is created in pheromones.json"
    - "The midden-threshold auto-REDIRECT appears in colony-prime prompt_section with the error-pattern tag"
  artifacts:
    - path: "tests/integration/pheromone-worker-integration.test.js"
      provides: "Integration tests for cross-phase signal influence and midden threshold auto-REDIRECT"
      min_lines: 100
  key_links:
    - from: "memory-capture (auto-emit)"
      to: "colony-prime prompt_section"
      via: "pheromone-write -> pheromones.json -> pheromone-prime -> colony-prime"
      pattern: "prompt_section.*REDIRECT"
    - from: "midden-write (3+ failures)"
      to: "pheromones.json"
      via: "midden threshold check creates auto:error REDIRECT"
      pattern: "auto:error.*error-pattern"
    - from: "pheromones.json (auto:error signal)"
      to: "colony-prime prompt_section"
      via: "pheromone-prime includes active signals"
      pattern: "REDIRECT.*error-pattern"
---

<objective>
Verify that auto-emitted signals during builds actually influence subsequent build phases, and that midden failure threshold detection creates working auto-REDIRECT signals.

Purpose: Phase 3 proved signals flow through the system. Plan 04-01 added pheromone awareness to agent definitions. This plan verifies the behavioral loop is closed: (1) auto-emitted signals from failures appear in subsequent worker context (PHER-04), and (2) midden threshold detection creates REDIRECT signals that reach workers (PHER-05). These are the final two requirements for Phase 4.

Output: Integration test file with tests covering cross-phase signal influence and midden threshold auto-REDIRECT.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pheromone-worker-integration/04-RESEARCH.md
@.planning/phases/04-pheromone-worker-integration/04-01-SUMMARY.md
@tests/integration/pheromone-injection-chain.test.js
@tests/integration/pheromone-auto-emission.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write cross-phase signal influence integration tests (PHER-04)</name>
  <files>tests/integration/pheromone-worker-integration.test.js</files>
  <action>
Create a new integration test file `tests/integration/pheromone-worker-integration.test.js` using the AVA test runner and the same setupTestColony/runAetherUtil pattern established in pheromone-injection-chain.test.js and pheromone-auto-emission.test.js.

**Tests for PHER-04 (cross-phase signal influence):**

1. **Auto-emitted failure REDIRECT appears in subsequent prompt_section**: Use memory-capture to auto-emit a REDIRECT from a failure event (simulating what happens when a builder worker fails during a build). Then call colony-prime --compact and verify the auto-emitted REDIRECT signal appears in the prompt_section output with the failure description.

2. **Auto-emitted learning FEEDBACK appears in subsequent prompt_section**: Use memory-capture to auto-emit a FEEDBACK from a learning event. Then call colony-prime --compact and verify the FEEDBACK signal appears in prompt_section.

3. **Multiple auto-emitted signals from different events all appear in prompt_section**: Emit a REDIRECT (failure) and a FEEDBACK (learning) via memory-capture, then verify both appear in the colony-prime --compact output.

4. **Agent definition contains pheromone_protocol section** (structural verification): Read each of the three agent definition files (.claude/agents/ant/aether-builder.md, aether-watcher.md, aether-scout.md) and verify they contain the string "pheromone_protocol" and instructions for "REDIRECT", "FOCUS", and "FEEDBACK". This proves the agent definitions have been updated (plan 04-01) and therefore workers spawned with these definitions will receive both the signal text AND instructions to act on it.

**Key pattern:** The definition of "influence" for automated testing is: (a) auto-emitted signal appears in prompt_section of subsequent colony-prime output, AND (b) agent definition contains explicit instructions to respond to that signal type. Together these prove the behavioral loop is structurally closed.

Use the setupTestColony helper to create an isolated temp directory with COLONY_STATE.json, pheromones.json, and other required state files. Use runAetherUtil to call memory-capture, colony-prime, and other subcommands.

Reference pheromone-injection-chain.test.js and pheromone-auto-emission.test.js for the exact helper function signatures and test patterns.
  </action>
  <verify>
Run the new test file:
```bash
npx ava tests/integration/pheromone-worker-integration.test.js --timeout=60s
```
All PHER-04 tests pass.
  </verify>
  <done>4 integration tests for PHER-04 pass: auto-emitted REDIRECT in prompt_section, auto-emitted FEEDBACK in prompt_section, multiple signals in prompt_section, and agent definition structural verification.</done>
</task>

<task type="auto">
  <name>Task 2: Write midden threshold auto-REDIRECT integration tests (PHER-05)</name>
  <files>tests/integration/pheromone-worker-integration.test.js</files>
  <action>
Add tests to the same file (`tests/integration/pheromone-worker-integration.test.js`) for PHER-05 (midden threshold auto-REDIRECT).

**Tests for PHER-05 (midden threshold auto-REDIRECT):**

1. **3+ midden failures in same category triggers auto-REDIRECT**: Use midden-write to write 3 failure entries with the same category (e.g., "security"). Then execute the midden threshold check logic: call midden-recent-failures to get failures, group by category using jq, identify categories with 3+ occurrences, and check if a REDIRECT was created. The test should call pheromone-write with the auto:error source and [error-pattern] tag for each qualifying category (reproducing the build-wave.md Step 5.2 logic). Then verify pheromones.json contains a REDIRECT signal with source "auto:error" and content containing the category name.

2. **Auto-REDIRECT from midden threshold appears in colony-prime prompt_section**: After the midden threshold creates the REDIRECT (from test above or a fresh setup), call colony-prime --compact and verify the [error-pattern] REDIRECT appears in the REDIRECT section of prompt_section.

3. **Deduplication: no duplicate auto-REDIRECT for same category**: Write 3+ failures for category "parsing", create the auto-REDIRECT. Then attempt to create it again (simulating a second build wave detecting the same pattern). Verify pheromones.json contains exactly one REDIRECT for this category, not two.

4. **Below-threshold failures do NOT trigger auto-REDIRECT**: Write only 2 failures for a category. Run the threshold check. Verify NO auto-REDIRECT signal is created for that category.

**Important:** Use the midden-write subcommand (not direct JSON manipulation) to create real failure entries with correct schema. The test should reproduce the build-wave.md Step 5.2 threshold check logic as closely as possible -- read the midden threshold bash code from the research document for the exact jq pipeline.

Reference the existing test patterns for setupTestColony, runAetherUtil, and temp directory cleanup.
  </action>
  <verify>
Run the full test file:
```bash
npx ava tests/integration/pheromone-worker-integration.test.js --timeout=60s
```
All tests pass (both PHER-04 and PHER-05 tests).

Also run the full test suite to ensure no regressions:
```bash
npm test
```
  </verify>
  <done>4 integration tests for PHER-05 pass: threshold triggers auto-REDIRECT, auto-REDIRECT in prompt_section, deduplication prevents duplicates, below-threshold does not trigger. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
1. All 8 tests in pheromone-worker-integration.test.js pass
2. Full test suite (npm test) passes with no regressions
3. PHER-04 tests verify: auto-emitted signals appear in subsequent colony-prime prompt_section AND agent definitions contain pheromone_protocol
4. PHER-05 tests verify: midden threshold creates auto-REDIRECT, it appears in prompt_section, deduplication works, below-threshold is ignored
</verification>

<success_criteria>
- `npx ava tests/integration/pheromone-worker-integration.test.js` exits with code 0 and 8 tests pass
- `npm test` exits with code 0 (full suite, no regressions)
- Test file exists at tests/integration/pheromone-worker-integration.test.js with at least 100 lines
- Tests use real subcommands (memory-capture, midden-write, colony-prime) not mock data
</success_criteria>

<output>
After completion, create `.planning/phases/04-pheromone-worker-integration/04-02-SUMMARY.md`
</output>
