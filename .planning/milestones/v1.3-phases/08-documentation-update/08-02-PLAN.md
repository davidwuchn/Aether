---
phase: 08-documentation-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/docs/pheromones.md
  - README.md
  - .claude/rules/aether-colony.md
  - .aether/docs/source-of-truth-map.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-04

must_haves:
  truths:
    - "Pheromone documentation describes the injection model (colony-prime injects context into worker prompts)"
    - "No documentation says workers independently read or check signals"
    - "README.md numeric counts match verified codebase values"
    - "README.md documents all current commands including new ones from Phases 2, 6, 7"
    - "aether-colony.md command tables include new commands"
    - "source-of-truth-map.md inventory counts are current"
  artifacts:
    - path: ".aether/docs/pheromones.md"
      provides: "Accurate pheromone system documentation with injection model"
    - path: "README.md"
      provides: "User-facing documentation with verified behavior only"
      contains: "40.*slash commands"
    - path: ".claude/rules/aether-colony.md"
      provides: "Updated rules file with current command listings"
    - path: ".aether/docs/source-of-truth-map.md"
      provides: "Accurate inventory snapshot with current counts"
  key_links:
    - from: ".aether/docs/pheromones.md"
      to: "colony-prime injection model"
      via: "documentation text"
      pattern: "colony-prime.*inject|inject.*worker prompt"
    - from: "README.md"
      to: "codebase reality"
      via: "verified counts"
      pattern: "40.*[Ss]lash [Cc]ommand|22.*[Aa]gent"
---

<objective>
Fix pheromone documentation to describe the injection model and update README.md and related user-facing docs with verified counts and commands.

Purpose: pheromones.md incorrectly implies workers independently read signals -- the actual mechanism is colony-prime injecting signals into worker prompts. README.md has 5 stale numeric claims and is missing commands added in Phases 2, 6, and 7. aether-colony.md and source-of-truth-map.md also need count and command updates.

Output: Updated pheromone docs with injection model framing, README with accurate counts, aether-colony.md with new commands, source-of-truth-map.md with current inventory.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-update/08-RESEARCH.md
@.aether/docs/pheromones.md
@README.md
@.claude/rules/aether-colony.md
@.aether/docs/source-of-truth-map.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pheromone documentation to describe injection model</name>
  <files>.aether/docs/pheromones.md</files>
  <action>
Read `.aether/docs/pheromones.md` and update all text that implies workers independently read or check signals.

**Specific lines to fix (from research):**

1. Line ~24: "Workers read FOCUS signals and weight this area higher"
   Change to: "FOCUS signals are injected into worker prompts via colony-prime, weighting the indicated area higher in task execution"

2. Line ~150: "Pheromones combine. Workers check all active signals"
   Change to: "Pheromones combine. colony-prime injects all active signals into worker prompts, ordered by priority"

3. Line ~159: "Workers check high priority signals first"
   Change to: "High priority signals appear first in the injected context"

**General principle for any other instances:** Search the entire file for sentences where "workers" is the subject of an active verb related to signal consumption (read, check, scan, query, look up, access). Replace with the injection framing where colony-prime is the actor that assembles and injects signals.

**Add a brief "How Signals Reach Workers" section** (or integrate into existing flow description) that explains:
- colony-prime assembles all active signals via `pheromone-prime --compact`
- The assembled signals are injected into the `prompt_section` of worker spawn context
- Workers see signals as part of their prompt, not by reading files
- Builder, watcher, and scout have `pheromone_protocol` sections in their agent definitions that instruct them how to act on injected signals

**Do NOT change:**
- TTL/decay mechanics (these are accurate)
- Auto-emitted pheromone descriptions (accurate)
- Signal combination table values (just fix framing)
- `pheromone-prime --compact` references (accurate)
  </action>
  <verify>
Run these checks:
- `grep -in "workers read\|workers check\|workers scan\|workers query" .aether/docs/pheromones.md` should return 0 results
- `grep -in "colony-prime\|inject" .aether/docs/pheromones.md` should return results (injection model described)
- `grep -in "pheromone_protocol" .aether/docs/pheromones.md` should return results (agent protocols referenced)
  </verify>
  <done>pheromones.md describes the injection model throughout -- colony-prime injects signals into worker prompts, workers do not independently read signals. TTL/decay mechanics and auto-emit descriptions remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md, aether-colony.md, and source-of-truth-map.md</name>
  <files>README.md, .claude/rules/aether-colony.md, .aether/docs/source-of-truth-map.md</files>
  <action>
**README.md updates:**

Read README.md and fix these stale claims (from research):

1. Line ~54: "9 Active Agent Types" → "22 Agent Definitions" (or "10 Active Agent Castes" if referencing caste-system.md spawnable castes)
2. Line ~55: "35 Slash Commands" → "40 Slash Commands"
3. Line ~77: "installs 22 agents...plus 37 slash commands" → "installs 22 agents...plus 40 slash commands"
4. Line ~237: "Utility layer (80+ subcommands)" → "Utility layer (110 subcommands)"
5. Line ~234: Comment "# Repo-local runtime" → "# Repo-local colony files" (avoid "runtime" which references the eliminated runtime/ directory)

Add missing commands to the README command table(s):
- `/ant:data-clean` — Remove test artifacts from colony data
- `/ant:export-signals` — Export pheromone signals to XML
- `/ant:import-signals` — Import pheromone signals from XML

Verify the pheromone section (lines ~128-129) already correctly describes injection ("Auto-injected into worker prompts via colony-prime --compact") -- this should already be accurate per research and should NOT be changed.

**.claude/rules/aether-colony.md updates:**

Read `.claude/rules/aether-colony.md` and add missing commands to the command tables:
- Under "Advanced" or an appropriate section: `/ant:data-clean`, `/ant:export-signals`, `/ant:import-signals`
- Verify pheromone section is not actively misleading (research says it's "vague but not incorrect" -- enhance slightly to mention injection model if appropriate without major rewrite)

**source-of-truth-map.md updates:**

Read `.aether/docs/source-of-truth-map.md` and update the Verified Inventory Snapshot:
- "Slash commands (Claude) | 37" → "Slash commands (Claude) | 40"
- "Slash commands (OpenCode) | 37" → "Slash commands (OpenCode) | 39"
- Verify and update other counts (sourced shell utilities, tests, command playbooks) by running the relevant commands
- Update the date from "2026-02-22" to "2026-03-19"
  </action>
  <verify>
Run these checks:
- `grep -n "35 Slash\|37 slash\|36 slash\|80+ subcommands" README.md` should return 0 results
- `grep -n "Repo-local runtime" README.md` should return 0 results
- `grep -n "export-signals\|import-signals\|data-clean" README.md` should return results
- `grep -n "export-signals\|import-signals\|data-clean" .claude/rules/aether-colony.md` should return results
- `grep -n "| 37" .aether/docs/source-of-truth-map.md` should return 0 results (stale counts removed)
- `grep -n "| 40" .aether/docs/source-of-truth-map.md` should return results (updated Claude command count)
  </verify>
  <done>README.md has accurate counts (40 commands, 22 agents, 110 subcommands), documents new commands, and uses "colony files" not "runtime". aether-colony.md lists new commands. source-of-truth-map.md has current inventory counts and updated date.</done>
</task>

</tasks>

<verification>
1. `grep -rn "workers read\|workers check\|workers scan" .aether/docs/pheromones.md` returns 0 (no active-worker signal reading)
2. `grep -n "35 Slash\|37 slash\|36 slash\|80+ sub\|9 Active" README.md` returns 0 (no stale counts)
3. `grep -n "export-signals\|import-signals\|data-clean" README.md .claude/rules/aether-colony.md` returns results in both files
4. `grep -n "| 37" .aether/docs/source-of-truth-map.md` returns 0 (stale counts updated)
</verification>

<success_criteria>
- pheromones.md uses injection model framing throughout, no "workers read/check signals" language
- README.md has accurate counts and lists new commands
- aether-colony.md includes new commands in tables
- source-of-truth-map.md inventory snapshot matches current codebase
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-update/08-02-SUMMARY.md`
</output>
