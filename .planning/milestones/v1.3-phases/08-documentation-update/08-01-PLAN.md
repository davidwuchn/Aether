---
phase: 08-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .aether/docs/known-issues.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03

must_haves:
  truths:
    - "CLAUDE.md contains no references to eliminated features (like runtime/)"
    - "CLAUDE.md numeric counts match verified codebase values"
    - "CLAUDE.md documents all current commands including data-clean, export-signals, import-signals"
    - "CLAUDE.md 'Core Insight' section reflects the now-connected integration state, not unsolved gaps"
    - "known-issues.md contains zero FIXED entries"
    - "known-issues.md retains all genuinely open issues"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate project documentation matching verified v1.1.0+ behavior"
      contains: "40 slash commands"
    - path: ".aether/docs/known-issues.md"
      provides: "Clean issue tracker with only open issues"
  key_links:
    - from: "CLAUDE.md"
      to: "codebase reality"
      via: "verified counts and features"
      pattern: "40.*slash commands|10,000\\+.*lines|22 agent"
---

<objective>
Update CLAUDE.md to match current codebase reality and clean known-issues.md of all resolved entries.

Purpose: CLAUDE.md is the primary developer reference and currently contains 10+ stale numeric claims, missing command references, and a "Core Insight" section describing problems that have been solved in Phases 1-7. known-issues.md has 15 FIXED entries that should be removed per DOCS-03 requirements. This plan makes both files accurate.

Output: Updated CLAUDE.md with correct counts, new commands, rewritten Core Insight. Clean known-issues.md with only open issues.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-update/08-RESEARCH.md
@CLAUDE.md
@.aether/docs/known-issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md to match verified codebase state</name>
  <files>CLAUDE.md</files>
  <action>
Update CLAUDE.md with the following verified changes. Run `wc -l .aether/aether-utils.sh`, `ls .claude/commands/ant/ | wc -l`, `ls .opencode/commands/ant/ | wc -l` to confirm current values before editing.

**Stale numeric counts to fix:**
1. Quick Reference table: Change "Slash commands | 36 (Claude + OpenCode)" to "Slash commands | 40 (Claude) + 39 (OpenCode)"
2. Quick Reference table: Change "aether-utils.sh | ~9,808 lines, 150 subcommands" to "aether-utils.sh | 10,000+ lines, 110 subcommands"
3. Quick Reference table: Change "Tests | 490+ passing" to "Tests | 530+ passing"
4. Architecture diagram comment: Change "9,808 lines, 150 subcommands" to "10,000+ lines, 110 subcommands"
5. Architecture diagram: Change "36 slash commands (Claude Code)" to "40 slash commands (Claude Code)"
6. Architecture diagram: Update any "36 slash commands" or "37 slash commands" references to "40 slash commands" for Claude and "39 slash commands" for OpenCode
7. .claude/ section: Update "36 slash commands" to "40 slash commands"
8. Key Directories aether-utils.sh: Change "150 subcommands" to "110 subcommands"

Use exact values for stable counts (40 commands, 22 agents, 110 subcommands). Use "N+" floor values for growing counts (530+ tests, 10,000+ lines).

**Missing commands to add:**
Add these to the appropriate command tables:
- `/ant:data-clean` under Advanced or Lifecycle (Purpose: "Remove test artifacts from colony data files")
- `/ant:export-signals` under Pheromone Signals or Advanced (Purpose: "Export pheromone signals to XML")
- `/ant:import-signals` under Pheromone Signals or Advanced (Purpose: "Import pheromone signals from XML")

**Pheromone section update (lines ~196-222):**
Add a note after the existing signal table that colony-prime injects active signals into worker prompts via `prompt_section`. Mention that builder, watcher, and scout agents have `pheromone_protocol` sections (added Phase 4) that instruct them how to act on injected signals. Reference export/import commands.

**"The Core Insight" section rewrite:**
Replace the current content that lists 4 unsolved integration gaps with a section reflecting the current integrated state. Something like:

```
## The Core Insight

The system's pieces are now **connected**:
- Pheromones update context (colony-prime injects signals into worker prompts)
- Decisions become pheromones (auto-emit during builds)
- Learnings become instincts (observation → promotion pipeline)
- Midden affects behavior (threshold auto-REDIRECT)

**The ongoing challenge is maintenance** — keeping documentation accurate,
data files clean, and test coverage comprehensive as features evolve.
```

**Last Updated line:**
Change "2026-02-22" to "2026-03-19" and update the parenthetical to reflect this milestone.

**Version:**
Update to "v1.3.0" if current version text says "v1.1.0" (this milestone is v1.3 per ROADMAP title).
  </action>
  <verify>
Run these grep checks to confirm no stale values remain:
- `grep -n "9,808\|9808" CLAUDE.md` should return zero results
- `grep -n "150 subcommands" CLAUDE.md` should return zero results
- `grep -n "490+" CLAUDE.md` should return zero results
- `grep -n "36 slash" CLAUDE.md` should return zero results (unless in a different valid context)
- `grep -n "don't update context\|don't become pheromones\|don't become instincts\|doesn't affect behavior" CLAUDE.md` should return zero results (case insensitive)
- `grep -n "export-signals\|import-signals\|data-clean" CLAUDE.md` should return results (commands documented)
  </verify>
  <done>CLAUDE.md contains accurate counts (40 commands, 110 subcommands, 530+ tests, 10,000+ lines), documents all new commands (data-clean, export-signals, import-signals), describes the pheromone injection model, and the Core Insight section reflects solved integration rather than unsolved gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all FIXED entries from known-issues.md</name>
  <files>.aether/docs/known-issues.md</files>
  <action>
Read `.aether/docs/known-issues.md` and perform surgical removal of all resolved entries.

**Remove these 15 FIXED entries entirely (header through next header):**
1. BUG-005: Missing lock release in flag-auto-resolve (FIXED Phase 16)
2. BUG-011: Missing error handling in flag-auto-resolve jq (FIXED Phase 16)
3. BUG-002: Missing release_lock in flag-add error path (FIXED Phase 16)
4. BUG-003: Race condition in backup creation (FIXED Phase 16)
5. ISSUE-002: Missing exec error handling (FIXED Phase 18-02)
6. ISSUE-003: Incomplete help command (FIXED Phase 18-03)
7. ISSUE-004: Template path hardcoded to staging directory (FIXED Phase 20)
8. ISSUE-007: Feature detection race condition (FIXED Phase 18-01)
9. GAP-001: No schema version validation (FIXED Phase 18-04)
10. GAP-002: No cleanup for stale spawn-tree entries (FIXED Phase 18-01)
11. GAP-003: No retry logic for failed spawns (RESOLVED Phase 18-02)
12. GAP-004: Missing queen-* documentation (FIXED Phase 18-03)
13. GAP-005: No validation of queen-read JSON output (FIXED Phase 18-04)
14. GAP-006: Missing queen-* command documentation (FIXED Phase 18-03)
15. GAP-009: context-update has no file locking (FIXED Phase 16)

Also remove: "Checkpoint Allowlist System (Fixed 2026-02-15)" entry if present.

**Remove duplicate:** GAP-010 (duplicate of GAP-007 per research finding). Keep GAP-007.

**Retain these open entries:**
- BUG-004, BUG-006, BUG-007, BUG-008, BUG-009, BUG-010, BUG-012
- ISSUE-001, ISSUE-005, ISSUE-006
- GAP-007, GAP-008

**Update Workarounds Summary table:**
Remove struck-through (FIXED) rows. If the entire table is empty after removal, remove the table and its header.

**Update file footer:**
Change the date from "2026-02-15" to "2026-03-19" and note "Updated during v1.3 milestone documentation phase".
  </action>
  <verify>
Run these checks:
- `grep -ci "FIXED\|RESOLVED" .aether/docs/known-issues.md` should return 0
- `grep -c "BUG-004\|BUG-006\|BUG-007\|BUG-008\|BUG-009\|BUG-010\|BUG-012" .aether/docs/known-issues.md` should return 7 (all open bugs retained)
- `grep -c "ISSUE-001\|ISSUE-005\|ISSUE-006" .aether/docs/known-issues.md` should return 3 (all open issues retained)
- `grep -c "GAP-007\|GAP-008" .aether/docs/known-issues.md` should return 2 (open gaps retained)
- `grep -c "GAP-010" .aether/docs/known-issues.md` should return 0 (duplicate removed)
  </verify>
  <done>known-issues.md contains zero FIXED/RESOLVED entries, retains all genuinely open issues (BUG-004 through BUG-012, ISSUE-001/005/006, GAP-007/008), and has an updated footer date.</done>
</task>

</tasks>

<verification>
1. `grep -n "9,808\|9808\|150 subcommands\|490+\|36 slash" CLAUDE.md` returns no results
2. `grep -ci "FIXED\|RESOLVED" .aether/docs/known-issues.md` returns 0
3. `grep -n "export-signals\|import-signals\|data-clean" CLAUDE.md` returns results (new commands documented)
4. `grep -n "colony-prime\|injection\|inject" CLAUDE.md` returns results in pheromone section (injection model described)
</verification>

<success_criteria>
- CLAUDE.md has accurate numeric counts matching verified codebase state
- CLAUDE.md documents all commands added in Phases 2, 6, and 7
- CLAUDE.md pheromone section describes the injection model
- CLAUDE.md Core Insight reflects solved integration, not unsolved gaps
- known-issues.md has zero FIXED/RESOLVED entries
- known-issues.md retains all genuinely open issues
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-update/08-01-SUMMARY.md`
</output>
